# Multiplayer backlog

What the async audits and harness sweeps found, in one place. One line per item; the state
column moves as the fix lands and the bake proves it. Coverage rows live in MP-COVERAGE-MAP.md;
this file is the work queue.

States: **open** (found, not started) · **fixed** (committed on the branch, no live verdict yet)
· **baking** (in a Jenkins sweep) · **done** (green in a sweep, or unit-tested where the harness
cannot reach) · **wontfix** (design decision, reason given).

Branch: `test/posture-seen`. Sweeps: Jenkins `openmw-web-dev` #89 (9/17), #90 (13/14; s147 open as #100), #91 (27/31: s114 #183, s147 #100, s149 harness cmd timeout #185, s160 booted the example suite — fixed).

## Found by the 2026-09-14/15 audits

| # | Item | Where | State | Proof |
|---|---|---|---|---|
| 1 | Invisibility/Chameleon/Light invisible on other screens; late joiner not caught up | global.lua, playerstate.ts | done | s156, avatarstats.test.ts (green #90) |
| 2 | Dead NPCs replayed by wire key vs object id (no-op); holder never got cell record | actors.lua, worldstate.ts | done | s157, holderhears.test.ts (green #90) |
| 3 | Holder deaf to relays for far anchored cells (doors/locks/objects) | worldstate.ts | done (unit) | holderhears.test.ts |
| 4 | Scripted Lock/Unlock never travelled (activation-only watch); then own-cell-only poll | objects.lua | done | s158 (green #90) |
| 5 | Attribute damage lost on relog / not on avatar | identity.lua, avatar.lua | done | s159 PASS #89 |
| 6 | Companion dropped on the player's relog (claim keyed by session id) | worldstate.ts | done (unit) | actor.test.ts |
| 7 | Host's quest globals never reached the peer; guests/peer seeded from own doc | quests.ts | done (unit) | questpeer.test.ts |
| 8 | Enable/disable ping-pong between disagreeing engines | worldstate.ts | done (unit) | holderhears.test.ts |
| 9 | Rolling restart booted with the flipped mode | worlds.ts | done (unit) | worlds.test.ts |
| 10 | Drop then disconnect before the inventory diff: dupe | server.ts | done (unit) | provenance.test.ts |
| 11 | One character in two worlds: last-writer-wins doc | server.ts, socialstore.ts | done (unit) | onecharacteroneworld.test.ts |
| 12 | Deleted character resurrected by a stale process flush | playerstore.ts | done (unit) | staledoc.test.ts |
| 13 | Item dropped across a cell border invisible to a joiner | worldstate.ts, connection.ts | done (unit) | holderhears.test.ts |
| 14 | PlayerDeath trusted blindly while the peer reports alive | playerstate.ts | done (unit) | avatarstats.test.ts |
| 15 | Late resurrect left the engine's main menu up | luabindings.cpp | fixed | (needs a live death with a slow round-trip) |
| 16 | F5 mid-play bounced to the launcher (fresh session, not resume) | index.html | fixed | manual / s80-style |
| 17 | Resume with the token of a still-open socket fell to a page reboot | connection.ts | done (unit) | admin.test.ts |
| 18 | Merchant stock never restocked (gold only) | worldstate.ts | done (unit) | economy.test.ts |
| 19 | Spellmaker spell persisted under a local dynamic id | global.lua, identity.lua, playerstate.ts | fixed | (scenario: make a spell via the real menu, relog) |
| 20 | Invite toast repeated on every join within 2 min | social.lua | fixed | — |
| 21 | Travel fare dropped by the reach gate (sent from the destination cell) | worldstate.ts | done (unit) | economy.test.ts |
| 22 | Disposition lost on peer restart / handoff | actors.lua | fixed | (scenario: persuade, restart peer) |
| 23 | Local fall/drown damage killed the client before the peer's verdict | character.cpp, actors.cpp | baking | s147 |
| 24 | Peer avatar never drowned: OUT OF PROCESSING RANGE of a hand-started peer (harness) | harness | done | s149 PASS #90 (drowning cost 82 hp, both sides agree) |
| 25 | A heal in the frame a peer report lands was erased before measurement | identity.lua | done | s150, s146 (green #90) |
| 26 | Cloud-locker page could not change world a second time (mplocker dropped) | index.html | done | s141 PASS #90 |
| 27 | Guest death seen by the host (puppet falls, gets up) | s131 | done | s131 PASS #89 |
| 36 | Magic at a puppet: only Damage/Restore H/M/F forwarded; Calm/Soultrap/Paralyze/Levitate-on-NPC applied to the local copy only | spelleffects.cpp | fixed | needs a target-spell mint hook + scenario |
| 37 | PvP swing/kill read as assault/murder by witnesses | mechanicsmanagerimp.cpp | fixed | (scenario: pvp on, hit B before a guard, bounty stays 0) |
| 38 | Guest's welcome carried its own doc's bounty; peer's guards read the world's | connection.ts, quests.ts | done (unit) | standing.test.ts |
| 39 | A drop carried record+count only: a pristine copy for the friend (free recharge) | objects.lua, worldstate.ts | fixed | s160, worldstate.test.ts |
| 40 | Equipment relayed at join before RecordsSync; made item never re-declared under its net id | connection.ts, global.lua | fixed | — |
| 41 | Sweep #89: a heal in the frame a peer report lands was erased before measurement | identity.lua | done | s150 (green #90) |
| 42 | Sweep #89: second world change on a cloud-locker page died with "no locker session" | index.html | done | s141 (green #90) |
| 43 | Sweep #89: scripted-lock poll covered the player's own cell only | objects.lua | done | s158 (green #90) |
| 61 | Half of all real jumps never reached the avatar (one-frame trigger vs every-other-frame sender) | player.lua | fixed | scenario: tap-jump 10x, avatar rises 10/10 |
| 62 | Avatar follow-teleport gave up after 3 s (cold interior load): no puppet in the room | global.lua | fixed | — |
| 63 | Actor batches for far anchored cells refused as stale epoch: frozen NPCs everywhere the peer's avatar was not | authority.ts, worldstate.ts | done (unit) | holderhears.test.ts |
| 64 | The peer drew from a player's byte/msg budgets; three busy cells got it disconnected with RATE | connection.ts | fixed | — |
| 65 | A dead companion's follow claim replayed to a restarted peer | worldstate.ts | fixed | — |
| 66 | Every terminal modal read "could not be reached (CODE detail)" | index.html | fixed | s91 DOM assert wanted |
| 67 | Joining a friend mid-chargen rebooted into a refusing door; unfinished character lost | social.ts, social.lua | fixed | scenario s161 sketch |
| 68 | In-game Exit did not say it was leaving: guests waited out the 90 s grace | mainmenu.cpp, index.html | fixed | — |
| 69 | Refused rest (timeSkip=owner) still healed the guest's local body: free full heal | m7.ts, playerstate.ts | done (unit) | avatarstats.test.ts |
| 70 | Bound items declared in the doc: relog inside the spell granted a permanent one | identity.lua | fixed | — |
| 71 | dayspassed never written: day-based timers a day late or backwards | world.lua | fixed | — |
| 72 | Weather holder spoke only on change; a guest's rolled-back rest left the wrong sky | world.lua | fixed | — |
| 90 | Sneak-attack critical / weapon skill use / OnPCHitMe gated on attacker == getPlayer(): never the avatar | combat.cpp, npc.cpp, creature.cpp, actorutil | fixed | scenario: sneak behind an NPC, hit ≥ 4x |
| 91 | On-strike enchantment cast by the owner's dead swing AND the avatar: twice, charge drained twice | spelleffects.cpp | fixed | — |
| 92 | Peer hp-0 report gated for a non-driving (alt-tabbed) player: death never recorded, free resurrect | playerstate.ts | fixed | — |
| 93 | Container take in the last 2 s before a disconnect: debited from the container, never credited | connection.ts | fixed | — |
| 94 | Cell reset resync omitted locks and deaths | worldstate.ts | fixed | — |
| 95 | Guest never told whose world it is on a fresh page; Enter opens chat; chat key behind the reconnect banner; accept buttons twice; modal ignores Lua's sentence; toasts said (F) | global.lua, index.html, social.lua | fixed | — |
| 96 | Harness: no per-scenario ceiling (a hung scenario killed the sweep with no summary) | mp-harness.mjs | fixed | — |
| 97 | Harness: the peer's Lua errors were never scanned | mp-harness.mjs | fixed | — |
| 98 | Harness: worlds spawned on GW_PORT+200, onto other scenarios' gateway ports | _gateway.mjs, s47, s48 | fixed | — |
| 99 | Stat values unbounded, level +1 sixty times a second, a container's first open canonical however absurd | playerstate.ts, worldstate.ts | done (unit) | adversarial.test.ts |
| 116 | Arrows landed twice (owner's phantom + the peer's); knockdown not relayed (owner rubber-banded) | objects.lua, global.lua, player.lua, luabindings.cpp, playerstate.ts | done | avatarstats.test.ts (kd); s109 s110 green #91 |
| 117 | Companions lost on a world restart; memberVars persisted but never replayed | worldstate.ts, cellstore.ts, objects.lua | done (unit) | actor.test.ts, quests.test.ts |
| 118 | TR landmass installed before Tamriel Data loaded in that order: a quietly corrupt world on every engine | mods.ts | done (unit) | mods.test.ts |
| 119 | PROD: the inner Caddy stripped CF-Connecting-IP -- every player was the edge's IP (one login budget, one household, /ipban bans everyone) | deploy/Caddyfile | fixed | needs [limits] trustCloudflareIp = true in the prod config.toml + a deploy |
| 120 | Tamriel Data: 54k loose files, one blocking round trip each on first read -- multi-second freezes per new TR cell | mod-install.ts, bsa-pack.ts (pack >500 loose assets into <slug>.bsa at install) | done (unit) | bsa-pack.test.ts, mod-install.test.ts |
| 121 | Ban in world A never kicked the player from world B (ticket refused only later) | server.ts (ban poll on heartbeat) | done (unit) | admin.test.ts |
| 122 | Account delete left locker sessions/tickets behind (erased account could still play out the ticket) | persist/erase.ts, playerstore.ts erased tombstone | done (unit) | staledoc.test.ts |
| 123 | Ticket TTL (15 min) started at the SSO callback: idling on the tile screen booted with a dead ticket | launcher.html bootGame mints /auth/ticket right before navigating | done (unit) | authticket.test.ts |
| 124 | One session per ACCOUNT, not character: phone on char A and laptop on char B kicked each other | players.ts activeForChar, connection.ts | done (unit) | session.test.ts (resume-over-live still resolves by account: SessionIndex has no charId) |
| 129 | Puppet swing is always a chop, played on use RELEASE at 1.2x: the wind-up starts after the blow landed; swings shown in spell stance | puppet.lua showSwing: wind-up on press (start→min attack, held), blow + Weapon Swish on release (max attack→follow stop); spellcast self start→stop in the spell stance | fixed | lua-tests (edge check); needs a bake |
| 130 | Co-op melee is silent: the cancelled hit chain played the Health Damage / miss sound and the blood; the owner being hit gets no sound | puppet.lua onHitIntercept replays sound + I.Combat.spawnBloodEffect before `return false` (never Actor._onHit); player.lua MP_SelfStats plays Health Damage on an hp drop | fixed | red overlay left (C++ binding) |
| 131 | A friend casting shows nothing: CombatCast never sent, MP_CastFx dead | player.lua use edge in spell stance → mpCombatCast → combat.onCast → CombatCast{spellId,casterId,kind}; MP_CastFx plays the school's castSound + the effect's castStatic vfx | fixed | lua-tests (wire shape) |
| 132 | Levitate/slowfall/waterwalking not in VISIBLE_EFFECT: the observer's puppet pogos under a flying friend | global.lua VISIBLE_EFFECT | fixed | lua-tests |
| 133 | Puppet never looks up/down: peer streamed pitch=0, puppet ignored pitchChange | global.lua avatarStreamTick pitch; puppet.lua controls.pitchChange | fixed | needs a bake |
| 134 | Puppet runs at the template's Speed: a fast player's puppet lags 128 units then teleports | playerstate.ts stamps `speed` (doc attributes) on PlayerStatsDynamic; global.lua → MP_Stats.speed; puppet.lua sets attributes.speed.base | fixed | avatarstats.test.ts green; needs a bake |
| 135 | A friend's doors swing silently (World::activateDoor has no sound) | objects.lua MP_DoorState plays the record's openSound/closeSound | fixed | needs a bake |
| 136 | Degraded mode: poseFlags bit 3 = inAir on the sender, read as `use` by the puppet: every landing played a phantom chop | player.lua poseFlags bit 3 = use (inAir dropped: nothing read it); a flags change alone now triggers a pose send | fixed | lua-tests |
| 140 | Paying the fine never calmed the peer's guards/witnesses: recordCrimeId ran only on the owner's client; the peer only zeroed the registry bounty | luabindings.cpp `mp.recordCrimePaid()` (World::getPlayer().recordCrimeId()); quests.lua MP_CrimeUpdate peer branch calls it when a source's level drops (per byId / shared, so a joiner restoring 0 forgives nothing) | fixed | needs a tier2 build; no unit |
| 141 | A guest arrived with their HOME faction ranks and a rank earned here was gone on relog (shared.factions never sent at join) | quests.ts `guestFactions()`: host doc factions overlaid with shared.factions when factions are shared and the world has another owner; connection.ts welcome record carries it | fixed (unit) | standing.test.ts (host rank reaches the guest, home rank does not, guest's own promotion survives a relog) |
| 142 | Persuasion never reached a player entering the cell later (ActorDisposition send-on-change, no catch-up) | actors.lua `catchUpCell()` clears dispVal/equipFp for the held cell; global.lua MP_PlayerCellChange calls it, so the next tick re-sends | fixed | needs a bake; lua-tests load |
| 143 | Another player's body was a crime witness (canReportCrime excluded only getPlayer) | mechanicsmanagerimp.cpp canReportCrime: same avatar / content-less puppet predicate as canCommitCrimeAgainst | fixed | needs a bake + tier2 |
| 147 | Personal-crime mode seeded a guest with the HOST's bounty | quests.ts seedBounty reads the player's own doc when crime is not shared | fixed (unit) | standing.test.ts |
| 149 | Nothing ever respawned and corpses never cleared: the peer anchored with respawn=false (CellStore::respawn never ran), a death had no date and was replayed on every cell entry forever, each peer restart rolled one more live creature on top of the old corpse | worldstate.ts (deaths {deathNo, atH}, forgotten after fCorpseRespawnDelay 72 h, placed levelled spawn dropped with it), scene.cpp (peer anchors load with respawn=true) | fixed | actor.test.ts (death at 10 h still sent, at 80 h gone with its spawn; engine side needs a bake: kill a smuggler, rest 3 days, re-enter) |
| 150 | dayspassed was boot-relative (offset learned from what this engine booted with): every relog reset it, no two engines agreed; DaysPassed-stamped timers (vampire incubation, lycanthropy, mod timers) misfired. Refines #71 | world.lua daysPassedOf: days since 16 Last Seed 3E 427 (= day 1) in the absHours basis, clamped >= 1 | fixed | lua-tests (7 checks); needs a bake |
| 151 | PCVampire/PCWerewolf/counters shadowed to the CAMPAIGN doc: a guest turning vampire made the host a vampire on next login, and the peer's dummy a werewolf | quests.ts CHARACTER_GLOBALS (pcvampire, vampclan, vampkills, pcwerewolf, pcknownwerewolf, pcknownreset: the GLOB names in Morrowind.esm/Bloodmoon.esm): own char doc only, relayed to nobody, filtered out of the campaign seed and overlaid from the own doc on GlobalVarSync | done (unit) | questpeer.test.ts |
| 153 | Relog as a werewolf left negative attribute modifiers at dawn (setWerewolf(true) ran before the base writes in phase 2) | identity.lua: the form restore moved to the end of applyPhase2, after the base attribute/skill writes and before the diff-cache seed | fixed | needs a bake |
| 156 | Sleep ambush spawned a creature only the sleeper sees (spawnRandomCreature not gated on localSpawnsEnabled) | waitdialog.cpp onWaitingInterrupted gated on MWMP::localSpawnsEnabled(), as transformationextensions.cpp / creaturelevlist.cpp | fixed | needs a bake (the peer does not roll the ambush for an avatar: no ambush at all under a holder) |
| 158 | No "contracted disease" message when the avatar caught it | global.lua MP_SelfSpells: a Disease/Blight record added to the player goes through notice() (sMagicContractDisease with the record name); global context has no openmw.ui | fixed | needs a bake |
| 144 | Assault/murder of a guildmate on the peer never expelled (the owner's engine never ran commitCrime, which is where vanilla expels) | MWMP::Crime.mFaction (victim's primary faction, or the trespass faction) filled in commitCrime, out through mp.takeCrimes → PlayerCrime.faction → connection.ts relay → MP_PlayerCrime expels when the player holds a rank | fixed | avatarstate.test.ts (relay), lua-tests; needs a bake + tier2 |
| 152 | Werewolf avatar fought with human numbers (applyWerewolfStats ran for actor==player only); a transformation rebuilt the puppet mid-fight | mechanicsmanagerimp.cpp setWerewolf: an avatar gets the stat swap (no GUI/witness block); global.lua MP_PlayerAppearance: isWerewolf left out of the rebuild compare, types.NPC.setWerewolf on the standing body instead | fixed | lua-tests; needs a bake + tier2 |
| 154 | A vampire friend looked human on your screen (the head swap reads the Vampirism effect; the spellbook reaches the avatar only, never a puppet) | identity.lua snapAppearance vampireSpell (first own spell with a vampirism effect); playerstate.ts stores/relays it; spawnPuppet adds that one spell, an appearance change swaps it in place | fixed | lua-tests; needs a bake |
| 155 | Mark position lost on relog/world hop (NpcStats only; no binding, not in the doc): Recall did nothing | luabindings.cpp mp.getMark/setMark over NpcStats mMarkedCell/mMarkedPosition (serialized cell id); identity.lua sends PlayerMark on a change and restores in applyPhase2; playerstate.ts handleMark → doc.mark. One mark per character: a cell the current world lacks never resolves (vanilla's "no mark") | fixed | declaredstate.test.ts; needs a bake |
| 159 | Peer follow-teleport ignored vanilla follower rules (in combat, stayoutside, >800 units): a stay-outside guar rode the strider indoors | global.lua canFollowThroughDoor (actionteleport.cpp getFollowers rules); actors.inCombat tracks the follower's own mpActorCombat report since global context cannot read an AI stack; stayoutside read from the mirrored local (#107) | fixed | needs a bake |
| 182 | The native peer never carried companion.lua (missing from files/data/CMakeLists.txt; the browser bake copies the dir whole, the peer image is the CMake tree): as holder the peer never reported follow/combat/travel, handoff bars never applied. Hidden locally by the harness overwriting the image; under Jenkins the sync fails (EACCES) and the baked copy runs | CMakeLists.txt | fixed | lua-tests: every mp.omwscripts path is listed (#91 peer log: Resource scripts/mp/companion.lua not found) |
| 185 | Harness cmd timeout (20 s) fired under a streamed-load frame (s149 red in #89 and #91, green #90) and printed nothing of the client | mp-harness.mjs handle.cmd | fixed | 60 s deadline + console tail on timeout |
| 176 | BSA packing hard-failed above the u32 data limit (Tamriel_Data HD) and reported "folder may be full"; install rolled back | bsa-pack.ts writeBsa(out, files, maxData) splits the sorted list greedily into `<slug>-1.bsa`, `<slug>-2.bsa`… each under the cap, one archive keeps the plain name, returns every path; mod-install.ts pushes them all into files/archives and names the real error (ENOSPC alone says "full") | done (unit) | bsa-pack.test.ts (cap injected: 5 files at cap 10 → 3 archives, every file once, each under the cap) |
| 102 | Chat history lost on every world switch (page reboot); idle feed lines never fade; whisper dropdown listed friends in other worlds | index.html: `omwmp:chatlog` in sessionStorage next to `omwmp:bootfrag` (prior lines + the engine's rolling log, cap 200, cleared on exit to the launcher); `.feed.idle` fades the corner 20 s after the last line unless the panel is open; the whisper list is friends with a local playerId | fixed | needs a run-through |
| 103 | "join" offered for every online friend incl. one in your world / solo; "Joining X..." status never cleared | presence rows carry the world's mode (socialstore `presence.mode`, written on join + heartbeat from server.ts worldMode); FriendView.joinable = online, in another world, mode party; the page offers join on that alone; watchJoin clears on the joinFriendResult mirror or 30 s | done (unit) | socialux.test.ts "a friend row is joinable only from another world that is open to friends" |
| 104 | Lua and JS narrated social failures with two different sentences | index.html FAILURE table carries social.lua's wording (SOCIAL_FAIL + MP_JoinFriend codes), fallback `op: code` as Lua | fixed | — |
| 105 | No "invite" button in the page's social panel | index.html friend row `invite` → `social:InviteSend:<acct>` (the O-key hub's op); the ack shows "Invitation sent." or the refusal | fixed | needs a run-through |
| 128 | Dashboard-disabled expansion (Tribunal) refused every browser client (page load order included all masters present) | mwdata-routes.ts: mwdata-mods.json carries `disabled` (base entries switched off) and is served even with no mods when something is off; index.html buildLoadOrder skips those masters (archives stay registered, as on the peer) | done (unit) | mwdata-serve.test.ts "names the disabled official masters, even with no mods installed" |
| 169 | TR NIGHT BLOCKER: gateway worlds read modlist.json / config.dashboard.toml from their own EMPTY data dir: the peer ran vanilla, became the canonical content list, every player refused BAD_CONTENT; every dashboard setting ignored by worlds | server.ts readModDoc/orderedContent(sharedDir); config.ts loadConfig merges the first valid dashboardLayers(sharedDir) after the shared config.toml when sharedDir !== dataDir | fixed (unit) | mods.test.ts "a gateway world sees the shared modlist and the shared dashboard layer" |
| 170 | TR NIGHT BLOCKER: the MP image had no 7z; TR/Tamriel_Data ship as .7z | Dockerfile.simpeer tier2 apt p7zip-full (ubuntu base; sevenzip.ts spawns `7z`) | fixed | needs a tier2 build |
| 171 | TR NIGHT BLOCKER: front door `/` booted #mp=wss://host/ws on a gateway (only /w/<id> accepted → 502 forever); Caddy redirected /launcher.html to / | play.js hands the ticket to /launcher.html#mpticket in multiplayer mode; caddy-config.ts `multiplayer` keeps the launcher served (OMW_ENABLE_LAUNCHER=1 still a manual override); wired from server.ts, gateway/main.ts, routes.ts | fixed (unit) | play-front-door.test.ts, caddy-config.test.ts |
| 172 | "Internal / LAN party" plain HTTP is not a secure context: "Browser not supported" for everyone but the server box | app.js wizard hosting copy says so and names the outs (TLS domain, tunnel, chrome://flags insecure-origin exception per player). Copy only; caddy-config.ts:229 untouched | fixed (copy) | wizard text |
| 174 | Gateway HTTP server kept Node's 5-min requestTimeout: a 2.7 GB upload slower than ~9 MB/s died as "connection dropped" | directory.ts server.requestTimeout = UPLOAD_REQUEST_TIMEOUT_MS (exported from http.ts, 6 h); headersTimeout left default as in http.ts | fixed | — |
| 177 | Peer start timeout 120 s vs a TR-loaded cold peer: SIGKILL and an unsimulated cell | config.default.toml startTimeoutMs = 300000. Default only; peerCostMb 487 still a vanilla number (ops note) | fixed (default) | — |
| 178 | Re-installing a mod served the old .esm from the browser cache (/mwdata/* immutable, same slug URL) | mwdata-routes.ts: /mwdata/mods/* is `no-cache` + ETag (size-mtime), 304 on If-None-Match, stale If-Range falls back to 200; retail stays immutable; ranges unchanged | fixed (unit) | mwdata-serve.test.ts "a mod file revalidates by ETag" |
| 179 | MAX_ABS_COORD 512000 = 62.5 cells: TR at y≈-60 had 2.5 cells of headroom; past it invisible/frozen with no message | movement.ts MAX_ABS_COORD = 4,000,000 (float32 ULP 0.25 at 4M; no sibling constant in mwmp C++ or Lua, nothing derives a buffer size from it) | fixed (unit) | movement.test.ts out-of-bounds at 5M |
| 160 | A service window open longer than 15 s stopped syncing (live/barter watches shared the chest's 15 s expiry): stock dupes, the gold delta never sent, training paid into a per-client purse | objects.lua openContainerNow: `until_ = live and math.huge or (now + CONTAINER_WATCH_SECONDS)`; onBarterClose and the tick's invalid-object branch still end a live watch | fixed | lua-tests (source checks); s161 needs a bake |
| 161 | Player-made records (potions, enchanted items) never mapped toNet/toLocal on the container/barter wire: a friend saw your potion as their own `Generated:` record, restock re-added the bogus id forever | objects.lua: countsToItems and sendContainerOp go out through worldmp.toNet (pendingOps keep the net id so the Update echo still consumes); setContainerContents, applyContainerDelta and the refused-take undo come back through worldmp.toLocal -- the drop path's own rule | fixed | lua-tests (source checks); needs a bake |
| 162 | The dialogue lock was released the moment a service window opened (Dialogue→Barter fired mpDialogueClosed): bribe/admire after the trade never travelled; two players traded at once | player.lua UiModeChanged: `talking(m)` = Dialogue or GOLD_SERVICE_MODES[m]; mpDialogueClosed only on `talking(old) and not talking(new)` | fixed | lua-tests; s161 (B's dlg: refused while A trades) needs a bake |
| 163 | Peer item-state report was wholesale: a cast-when-used charge spend (or a repair) was refunded within 2 s | playerstate.ts handleAvatarItemStatesBatch merges per field against the doc: charge/condition = min(peer, doc), soul the peer's; the merged bucket is what the owner hears | done (unit) | avataritemstates.test.ts (doc 60 after a spend, peer 100 → 60 kept; peer 40 → 40) |
| 164 | Losing a barter race costs the item AND the gold (three independent ops, refusal does not know the price); unreachable once 162 holds the lock | worldstate.ts, objects.lua:908 | open | — |
| 165 | 24 h restock refills one-off stock (unique Creeper/Jobasha items come back daily), not just restocking entries | worldstate.ts:1171 | open | — |
| 166 | MAX_GOLD_DELTA 1,000,000 lets any client zero/fill a merchant purse; no moderation note on the gold branch | worldstate.ts gold branch: a delta beyond the merchant's goldOrigin is noted (`merchant_gold_delta` anomaly + warn log); the cap is unchanged | done (unit) | economy.test.ts (selling the whole purse: no flag; 10× the purse: flagged) |
| 167 | Custom record bodies unvalidated (chopMaxDamage=9999, Fortify Health 10000 for 10^6 s replayed to the peer, the avatar fights with it) | m7.ts recordWithinCaps: effect magnitude ≤ 400, duration ≤ 5760 s, weapon damage ≤ 200 (4× the spellmaker / retail maxima); refused like a malformed body, counted in `omwmp_records_refused_total` | done (unit) | worldtime.test.ts (godsword / immortality refused, fine blade / big heal acked) |
| 168 | Harness gap: no hook traded inside a real barter window — blocked proving 160/162/164 | player.lua `barter:open:<recordId>`, `barter:sell:<id>` / `barter:buy:<id>` → global.lua mpTestBarter (one item across, purse by its value); s161-barter-long-window.mjs | fixed | s161 needs a bake |
| 173 | The dashboard "update engine" fetches the v1.3.0 GitHub release; a branch peer against a v1.3.0 client (wire skew: speed, CombatCast, pitch, dayspassed) | update-engine.ts:32 — operator copies the Jenkins bake into play/ |
| 175 | Cloudflare in front of the dashboard: 100 MB body cap refuses Morrowind.bsa and TR archives; 100 s origin timeout 524s the commit — uploads over the LAN only | ops note; fsstorage.ts:200 |
| 180 | No self-serve sign-up (/auth/password signs in existing accounts only): the owner creates the friend in the dashboard; both need a public username on first launcher login | ops note; auth/routes.ts:322 |
| 181 | Internal hosting mints locker URLs on http://127.0.0.1:8080 (no domain): server-side saves/locker uploads from another machine fail; publicBase hidden from the dashboard | fsstorage.ts:166; api-settings.ts DERIVED_FIELDS |
| 183 | s114 red in #91: A's client went silent ~5 s after the second snapto (simpeer.avatar_stats_gated lastInputAgoMs=5668), A's copy of the companion never moved while B saw it; no Lua/JS error. Rerun alone with moveRx/actorBatchesIn/puppetRx/uiMode + A's full console | s114, player.lua input sender |
| 184 | Harness under Jenkins cannot sync mp scripts into the peer (EACCES on /usr/local/share/openmw/resources/vfs/scripts/mp) and only WARNS: the peer runs the image's baked Lua — honest only while the image is built from the branch under test | mp-harness.mjs:283 syncPeerScripts; run-harness.sh |
| 190 | Wire version handshake (proto=2) not maintained: 14 PROTOCOL.md commits since the last bump; a mismatched client connects and misbehaves silently (real shape of #173) | connection.ts:1200 bump proto per additive change or minClientBuild |
| 195 | First-run setup trusts XFF from any private peer: behind an operator's own proxy without XFF every internet client looks private during the setup window | routes.ts:338; document beside the LAN copy |
| 203 | GAP: scripted movers (mod elevators, SetPos/Move on activators) are per-engine and unsynced; no client emits ObjectMove | objects.lua:819 holder-authoritative relay when a mod needs it |
| 204 | Design ceiling: a WebSocket stall > 0.35 s collapses buffered inputs to the last one on the peer; the owner is dragged back by the stall length | avatar.lua:231 input queueing + #197 |
| 206 | Actor LOD is measured from the peer's parked dummy, not the streamed cell: the second friend in another exterior gets NPCs in their OWN cell at 1 Hz (park/stutter/teleport) | worldstate.ts handleActorMoveBatch: stride from the recipient's pose to the streamed cell's centre; the recipient's own cell is never strided | done (unit) | holderhears.test.ts (dummy parked a cell away, Bob in the streamed cell: 10 sent, 10 received) |
| 207 | PlayerInput frames (0x0102) draw from msgsPerSec: a >2 s stall then the burst empties the bucket and the player is kicked with terminal RATE | connection.ts: own `inputBucket` (60/s, burst 120), shed + counted (`input_shed`) instead of RATE | done (unit) | ratelimit.test.ts (130-frame burst: alive, >= 5 shed; the JSON flood still RATEs) |
| 208 | mLastMoveSeqIn survives an in-process reconnect while the new world process's broadcast counter restarts at 0: after a world restart every pose is dropped as stale for hours (password/self-hosted worlds, harness) | netmanager.cpp connect() resets mLastMoveSeqIn with mSeq | fixed | needs a bake; no unit |
| 209 | The actor pose relay is enqueued on the world mutation chain behind DB-awaiting ops: every cold cell read stalls the 20 Hz NPC stream for everyone | worldstate.ts handleActorMoveBatch is synchronous (holder/epoch reads are in-memory); noteActorFrame kept | done (unit) | actor/holderhears/worldstate suites green unchanged |
| 210 | Background tab: the 4 MB inbound cap drops whatever arrives next (events included) with no resync; a discarded tab that stops reading is kicked with terminal RATE "outbound buffer overflow" although a resume ticket was parked | netmanager.cpp onMessage sheds only 0x0101/0x0200/0x0103 past the cap (events queue); global.lua polls `mp.status().droppedInbound` once a second and sends ResyncRequest for the own cell when it moves; connection.ts overflow disconnects with `BACKLOG` (ticket still parked), net.lua TRANSIENT_DISCONNECT includes it | done (unit) | backpressure.test.ts (hard ceiling -> BACKLOG); C++/Lua side needs a bake |
| 197 | Reconciliation has no latency compensation: the peer's pose (as of lastInputSeq, one RTT old) is compared with the client's CURRENT position, so on a real link every stop/jump/doorway rubber-bands by RTT x speed (invisible on the LAN harness) | player.lua:299-330 ring of {seq,pos} in inputTick; divergence = e.pos - posAt(e.lastInputSeq); harness needs a debugStateDelayMs knob; player.lua posRing/posAt (64 x {seq,x,y,z}), reconcile against posAt(lastInputSeq) | fixed | lua-tests; s162 (150 ms link) needs a bake |
| 198 | One in ~three jumps never reaches the avatar: 30 Hz input vs the peer's 20 Hz tick, the second event of a tick overwrites the first (jump bit lost); observers latch it, so friends see a jump the avatar never made | avatar.lua:227-233 latch jump/use bits until consumed; avatar.lua jumpLatch/useLatch, consumed in onUpdate | fixed | lua-tests; s145 needs a bake |
| 199 | Doors relay ~1-1.5 s late (read waits for Idle): the owner walks through while the avatar is stopped by the closed door (rubber-band/SELF SNAP); rotateDoor rolls back while the avatar is in the arc and stacks AiAvoidDoor the avatar never runs (deadlock while pushing); a close in the window is reversed by the stale open | objects.lua:420, 1168-1188 send the intended state at activation; objects.lua onActivate sends the Opening/Closing intent, settle read resends only a change, MP_DoorState keeps our pending read for 1.5 s | fixed | lua-tests |
| 200 | Every exterior cell-border crossing follow-teleports the mover's puppet and avatar (pop for friends; fall height zeroed on the peer = no fall damage across a border, second trigger for #74) | global.lua:2356 skip the teleport when both keys are exterior and the body is within SNAP_DIST; global.lua MP_PlayerCellChange: prevCell captured; exterior->exterior within 256 units skips tryTeleport | fixed | lua-tests |
| 201 | Movement effects (Levitate/Slowfall/Water Walking/Fortify Acrobatics) reach/leave the avatar up to 0.5 s + RTT late (INTERVALS.active 0.5): the owner hangs after expiry, dips on cast | identity.lua:24 active diff 0.1 s or per frame for the movement subset; identity.lua INTERVALS.active 0.1 | fixed | lua-tests |
| 202 | Observer puppets miss ~25% of jump edges (20 Hz avatar stream sampled by the server's 66 ms tick); sub-66 ms use clicks lost too | connection.ts:930 OR the edge bits into p.pose.flags until broadcast once; movement.ts acceptPeerPose ORs bits 4|8 until poseSentVersion catches up (tick end) | fixed | movement.test.ts |
| 205 | Avatar input hold 0.35 s: a TCP retransmit stall (300 ms RTO, 1-3 s Wi-Fi roam) stops the avatar while the owner keeps running; the burst collapses to the newest input and the owner is snapped back by v x stall (sibling of #197/#204) | avatar.lua:34 INPUT_HOLD_S 1.0; avatar.lua INPUT_HOLD_S 1.0 | fixed | lua-tests |
| 211 | Puppets on a lossy link: near-tier snap 128 crossed by any stall ≥ 350 ms (teleport at the 1 s cooldown); run flag dropped/re-raised per burst (anim popping) | puppet.lua:47 snap 256; hold the run flag ~0.3 s; puppet.lua SNAP_BY_TIER[0]=256, run flag held 0.3 s (runUntil) | fixed | lua-tests |
| 212 | Harness has no latency/loss knob (raw CDP, no emulateNetworkConditions): every feel bug above is invisible on the LAN | connection.ts OMWMP_NET_DELAY_MS FIFO on send+receive (+ OMWMP_NET_STALL); net/netdelay.ts FIFO per connection, both directions, OMWMP_NET_DELAY_MS + OMWMP_NET_STALL=<ms>/<everyS>; scenarios pass it via serverEnv | fixed | netdelay.test.ts; s162 |
| 186 | Backup does nothing useful on a gateway: SIGUSR1 has no gateway handler (README cron flushes nothing, Node opens the inspector), tar copies db/wal/shm at different instants, README names the wrong container | gateway/main.ts SIGUSR1 flushes accounts and relays to every world (worlds.signalAll); sqlite.ts checkpoint() = `wal_checkpoint(TRUNCATE)` after players/cells/accounts/records/bans flush; ops.ts export checkpoints every *.db first and excludes gamedata/; README names `openmw-web` and describes the relay | done (unit) | worlds.test.ts (SIGUSR1 reaches both fakes, not a stop) |
| 187 | Orphaned sim peers after a world is SIGKILLed are never reaped (peers spawned detached:false, no pid file; a peer that once joined redials forever); no init in compose | worlds.ts spawns the WORLD detached (own process group; its peers stay attached, so they share it) and the SIGKILL escalation is `process.kill(-pid)` (child.kill fallback on Windows / a non-leader); compose `init: true`; net.lua a SYSTEM peer that had joined quits (`core.quit()`) after 30 redials | fixed | needs a tier2 rebuild for the Lua half |
| 188 | Dashboard delete-account in gateway mode: no kick, no discardForCharacter, erase.ts never writes the `erased` tombstone so the world's next flush resurrects the doc | gateway/admin.ts deleteAccount: kick relayed to every running game, discardForCharacter per character, then flush + erase; erase.ts erasePlayerDocs writes the `erased` tombstone per key (try/catch for pre-002); stale "runs OFFLINE" comment rewritten | done (unit) | erasure-completeness.test.ts (a flush from a store that still caches the doc does not resurrect it) |
| 189 | Cell docs have no cap on moved/locks/doors: ~11k ObjectMoves (3 min at 60 msg/s) makes WorldCellState exceed the LSER node ceiling and every entrant disconnects BAD_PROTO; memberVars capped on send only | cellstore.ts MAX_KEYS_PER_CELL 2000 + cellMapFull(): a NEW key past the cap is refused and logged `world.cell_map_full`, an existing key still updates; used by move/lock/door/enabled (worldstate.ts) and storeMemberVar (quests.ts) | done (unit) | worldstate.test.ts (seeded at the cap: new ref refused, existing ref moves) |
| 191 | Gateway shutdown exits 3 s after stopAll while the world stop grace is 20 s (PID 1 exit kills children mid-drain) | gateway/main.ts shutdown awaits worlds.allExited() (bounded by STOP_GRACE_MS + 5 s, the supervisor's own SIGKILL) before process.exit; compose `stop_grace_period: 30s` | done (unit) | worlds.test.ts (allExited resolves only after the last child exits) |
| 192 | Persistence failures (players.flush_failed, cell_flush_failed, world.crashed, cells_unsimulated, server.crash) not in the default notification event list | config.default.toml events += players.flush_failed, world.cell_flush_failed, world.crashed, world.at_cap, simpeer.cells_unsimulated, server.crash (notify.ts matches exact names; all six are logged under exactly those) | fixed | — |
| 193 | erase.ts opens SQLite without busy_timeout: a concurrent world write turns the dashboard delete into a 500 | erase.ts withDb: `PRAGMA busy_timeout = 5000` right after open | fixed | — |
| 194 | Ban rows attribute dashboard bans to "dashboard", not the operator | routes.ts AdminDeps.action gains `by` (ctx.accountName); server.ts banAccount(target, by); unban logs `moderation.unban` with the operator | fixed | — |
| 196 | Slow growth: erased never pruned; quests.ts peerMemberAt grows per cell|ref|name; Export streams gamedata/ (multi-GB); the 1 Hz "avatar submerged" info line ships in production Lua with a sync append per line | PARTIAL: ops.ts export excludes gamedata/ (with 186); global.lua avatarDrownProbe once per 10 s (DROWN_PROBE_EVERY). Still open: erased pruning (playerstore.ts:156), peerMemberAt growth (quests.ts) | open | — |
| 217 | Script state the peer advances is never relayed/persisted (MemberVarUpdate only 6 s after a LOCAL activation): the Heart's countHits invisible to clients and lost on a peer restart mid-fight; Dagoth Ur's Say lines and PlayGroup play on the peer only | quests.lua:396 long-lived memberWatch on the peer; OpSay on system → ActorSay relay |
| 220 | OnDeath "player->" afflictions apply on every client in the cell: the GUEST gets Corprus from Dagoth Gares and can never be cured at home (no A2_2 journal) — needs a decision | identity.lua strip campaign-script spells on WorldClosed? |
| 221 | GetAiPackageDone pollers stall on clients (puppet AiSequence never executes): Fargoth's lookout, MehraMiloScript, HentusTravel state machines never advance; StartCombat Player from a peer-run branch targets the dummy (generalises #78/#145) | actors.cpp:1712 |
| 222 | NPC-local writes from a dialogue result are lost after 6 s of talking (memberWatch never re-armed on a topic click) and MemberVarUpdate relays with the old cellsVisible rule, so the holder is deaf to it outside the dummy's cell | quests.lua:398 diff locals at releaseLock; quests.ts:151 relayCell uses hears() | done (unit) | quests.test.ts (peer parked a cell away hears the MemberVarUpdate); lua-tests source check for the conversation-long watch; needs a bake |
| 223 | Player reputation is not on the wire nor in the doc: resets to 0 on every relog (PcReputation topics vanish, derived disposition drops) | identity.lua snapProgression reputation; playerstate.ts clamp 0..255 | done (unit) | declaredstate.test.ts (rides on PlayerLevel, 300 refused, doc keeps 12); Lua side needs a bake |
| 224 | A human's dialogue-set quest global never reaches the other human live (only the peer + doc): the guest's Global filters and local scripts are stale until relog although the journal advanced (s62 pins the old per-character model) | quests.ts:394 relayAll for humans; flip s62 | done (unit) | questpeer.test.ts (the other human hears it), quests.test.ts flipped; s62 flipped, needs a bake |
| 225 | SetJournalIndex to a LOWER stage (quest restart) is dropped, not persisted, and reverted on next login; later advances below the old max are silently dropped too | quests.ts:239 record() before the regress check for owner/peer origin | done (unit) | questpeer.test.ts (owner 50→10→20, peer →5, guest's 1 blocked, doc follows) |
| 226 | ForceGreeting dialogues take no lock: their AITravel/StartCombat/ModDisposition results are dropped ("without the conversation") — also the #44 arrest path | player.lua:1325 UiModeChanged → mpDialogueForced → requestLock | fixed | lua-tests source checks; a denied forced greeting closes the window; needs a bake |
| 227 | NPC combat is silent on clients: no attack shout, hit grunt, flee/thief bark (every say() is on peer-run paths) | mp.say binding; MP_ActorAI combat + onHitIntercept |
| 228 | An NPC killed by a friend mid-conversation leaves the window open; results execute on the corpse (world not paused in MP) | MP_ActorDeath for lockHeld → removeMode Dialogue + releaseLock | fixed | lua-tests source checks (actors.lua actorDeathFn → quests.onActorDeath → MP_CloseDialogue); needs a bake |
| 229 | Persuasion/result-script Fight/Flee/Alarm changes stay on the talking client (no AI-setting field on ActorStats) | actors.lua |
| 230 | TalkedToPc per engine and never persisted: every NPC greets a returning player as a stranger | needs a binding |
| 231 | Any teleport of the avatar (SELF SNAP → PlayerCellChange → tryTeleport → teleportNotPlayer) calls stats.land(false): a snap mid-fall makes the rest of the fall free (exploit; the snap fires on every levitate release because the client's stream lags the climb) | objectbindings.cpp:122; global.lua:2361 skip land() for same-cell teleports or carry fall height |
| 232 | s147 (#100) still open after 3 sweeps: peer bar unmoved after a 1300-unit avatar fall; #93 carries a peer fall probe ("avatar #n landed from z=... fell N hp=...") to tell "never airborne" from "landed, not charged" (suspect: mJumpState never InAir for the dynamically-created avatar record on the headless peer — refreshJumpAnims resets when the jump group is missing) | global.lua avatarFallProbe; character.cpp:2240/517-522 |
| 233 | Tools and lights never deplete while the peer rules: lockpick/probe/repair uses and torch burn are client-only, the raise-only rule refuses the drop and the avatar's untouched copy refills it every ≤10 s (free lockpicks, infinite torches) | identity.lua itemState() tags own=true for Lockpick/Probe/Repair/Light; playerstate.ts copies a tagged condition wholesale (client->doc) and keeps the doc's under the peer report | fixed | avataritemstates.test.ts; lua-tests; s163 (needs a bake) |
| 234 | Per-item state is positional over stateful instances but applied to whole stacks: one Soultrap kill on the peer fills all 3 gems of a stack (persisted); two daggers of one record swap/lose conditions per relog and per peer report (index is not an identity; #56 is the relog face) | every entry carries n (stateless stacks send {n}); server keeps empties; global.lua applyItemStates walks the stacks and split()s a bigger one before writing (state set on the piece so it does not restack); avatar apply, relog restore and MP_SelfItemStates (now applied in global.lua) share it; the peer reports one entry per stack | fixed | avataritemstates.test.ts; lua-tests (split + report round-trip) |
| 235 | A refused container take leaves its acquisition credit in pendingAcquired; a disconnect folds it into the doc (dup with the race winner) | worldstate.ts containerOp: a `gone` take refusal debits pendingAcquired by (itemId, n) | done (unit) | worldstate.test.ts |
| 236 | The drop debit trims doc.inventory but not doc.itemStates: a disconnect within 2 s of dropping one of two same-record items restores the survivor with the dropped one's state | server.ts drop debit pops the record's tail entries (whole ones while they fit, the last shrinks by n) in the same doc update | done (unit) | provenance.test.ts |
| 213 | `Startup` never runs for a returning character, a guest or the peer (--skip-menu bypass sets CharGenState -1): 103 quest refs (Dagoth Gares, the Incarnate ghosts, Wulf, Staada, sleepers...) are visible from day one for everyone but the host's first session; the peer simulates an enabled Dagoth Gares against a host whose copy is disabled | worldimp.cpp enable/disable choke → MWMP::recordScriptNote (puppets.hpp ScriptNote, drained by `mp.takeScriptNotes`); objects.lua onScriptNote sends ObjectEnabled under the OBJECT's cell key (own-cell poll kept as fallback); worldstate.ts docAndRef: no reach gate for ObjectEnabled | done (unit) | worldstate.test.ts (a human's far-cell disable persists and replays to a later entrant); C++/Lua side needs a bake |
| 214 | Client-triggered `PlaceAtPC <actor>` never spawns anywhere (the client skip at transformationextensions.cpp:625 assumes the peer runs the script; the peer never sees GetPCSleep/OnActivate/dialogue): no sleeper ambush, no Tashpi ghost, no Mark summons; quest-repair.ts is a one-rule hand table | transformationextensions.cpp OpPlaceAt skip site → ScriptNote{spawn, recordId, count, cellKey, pos}; objects.lua sends ObjectSpawnRequest{actor:true}; worldstate.ts spawn(): a human's actor request is forwarded as QuestSpawn{x,y,z,count,forId} to the holder (else back to the asker), 10/min per player; global.lua mpQuestSpawn places at the spot (the naming sweep nets it); quest-repair.ts DEFAULT_SPAWN_RULES emptied (mechanism kept) | done (unit) | worldstate.test.ts (forwarded to the holder; the 11th in a minute refused `rate`), questrepair.test.ts (peer vs no-peer routing), actor.test.ts; C++/Lua side needs a bake |
| 215 | The peer's dummy runs PCRaiseRank from OnDeath scripts (Bolvyn, Trebonius, Eno Hlaalu...) and its FactionUpdate writes the host's campaign doc (host demoted when factions are not shared) | quests.ts faction(): return if player.system | done (unit) | standing.test.ts (a system peer's FactionUpdate leaves the campaign doc untouched) |
| 216 | Player-gated scripts (GetDistance Player, CellChanged, OnActivate) run only on clients, on AI-off puppets: Dagoth Ur never PositionCells to the Heart on the peer; the Arena duel (Bolvyn) is a fully local fight per screen (not puppeted, damage erased, kill never tallied, alive on relog); EndGame's SetHealth 0 / blight cloud Disable / ChangeWeather never reach the peer; Mehra never moves for the guest | transformationextensions.cpp OpPositionCell/OpPosition on a client (MWMP::isClient) → ScriptNote{position, ref, source cellKey, dest cellName, pos}; actors.notePosition sends ActorAI{position={cell,x,y,z}} unless we hold the cell; worldstate.ts sends it to the holder only (human, near, finite); MP_ActorAI teleports. Arena duel (local fight) / EndGame SetHealth / ChangeWeather halves still open | done (unit) | worldstate.test.ts (position from a non-holder reaches the holder; not from afar); C++/Lua side needs a bake |
| 218 | `Enable` relayed live but never persisted and only for the own cell: every far-cell Enable from dialogue (dagoth gares, notes, stronghold stages) is client-local; becomes a permanent hole once #213 persists disables | worldstate.ts enabled(): `doc.enabled[key] = on` (a stored true is a scripted reveal); WorldCellState/CellSnapshotReplace carry `enabled` beside `disabled`; objects.lua applies both; the far-cell path is #213's | done (unit) | worldstate.test.ts (an Enable after a persisted disable persists and replays as `enabled`); holderhears ping-pong guard unchanged |
| 219 | Running global scripts are not persisted across a relog: Sleepers stops at the host's first relog (no dreams/ambush), VampireCheck never runs for a returning character (vampirism unreachable after one relog — refines #150), MoveMehra/All_Nerevarine lost | luabindings.cpp `mp.runningGlobalScripts()` / `mp.startGlobalScript(id)`; quests.lua diffs the running list every 5 s (chargen*/startup never travel) → GlobalScriptsUpdate{started,stopped}; quests.ts keeps the union as `scripts` on the campaign doc (journalTarget) and sends GlobalScriptsSync at join; the client starts what it lacks (nil-guarded) | done (unit) | quests.test.ts (persist + sync at rejoin; another character gets none), lua-tests (diff/started/stopped/chargen filter/sync); C++/Lua side needs a bake |
| 238 | No scenario kills an NPC with a REAL melee swing: combat.lua:50 forwards hits only when mpTest; s51/s58/s109/s111/s118/s119/s120/s128/s157 prove the synthetic relay, the avatar-swing path players take is unproven; s52's PvP-off gate likewise | new s164 real-melee kill (equip/stance/attack loop, s138 idiom); s52 rewrite | fixed | s164 (new: iron longsword, longblade 100, stance, attack loop beside a stung -2,-7 creature; hitFwd must stay undefined; B sees the death); s52 rewritten (managedPeer, real swings at B's puppet for 12 s with pvp off, B's selfStats unmoved) -- both need a sweep |
| 239 | s72 merchant purse passes with purse sync absent (two untouched merchants read the ESM default; may even be two different NPCs) | s72 → s161 idiom or delete | done | s72 deleted (s161 already opens the same merchant on B after A's sale and asserts A's post-sale purse); MP-COVERAGE-MAP notes the deletion |
| 240 | Weak asserts: s137 never asserts the enemies-nearby bit was CLEAR before; s149 counts any damage as drowning (s999:153 breath check missing); s110/s112/s66/s116 compare current<base instead of current<before; s143's roll assert is inside `if (rolls.length>0)`; s140:41 waits on a stale socialResult; s999 compares a value with itself | per row | fixed (s999 open) | s137 asserts the bit clear before; s149 asserts firstHurtAt >= 15 s; s110/s112/s66 compare to the bars before the poke (s116 already did, now a real cast); s143 asserts leaderRolls.length > 0; s140 clears socialResult before the private flip. s999 is an untracked control in the main checkout, not on this branch: its `cur` should read `bars(a)` (selfStats), not hp twice |
| 241 | Deaths by sethp:0 (s22/s77/s131; s131 has NO peer in a peer-ruled world): death-from-damage never exercised | managedPeer + fall/drown idiom | fixed | s22/s77/s131 managedPeer + retail, drowned on the -3,-9 sea bed (_death.mjs); s22/s77 assert the server's respawn.sent line (ctx.serverLogTail, new), s131 the host's puppet falling. Depends on the peer actually drowning an avatar (s149 has not been green yet: #185) |
| 242 | Spell scenarios park effects (castat:/castp:/healp:/selfcast: = activeSpells:add) instead of casting: s59/s144/s116/s115 stay green with the cast path broken | learnspell/mintspell + stance:spell + face: + press: (s146 idiom) | fixed | _spell.mjs (learnspell of a RETAIL spell, skill 100, setmp, stance:spell, face:, press:): s59 Fire Bite on touch beside the NPC, s116 Fire Bite at the victim's puppet, s144 Heal Companion at the friend's puppet, s115 Summon Scamp pressed until `actives` lists it. No hook fallback kept: a touch cast from 60 units targets a puppet headlessly (the engine picks the actor in front) |
| 243 | s117 companion-indoors: hand peer never holds the interior, door:enter teleports by hook — no holder asserted | managedPeer + authorityHolder assert (s118:94 idiom); door:enter without the hook teleport | fixed (partial) | s117 managedPeer, asserts authorityHolder !== 'none' and isHolder === 'false' inside. Still door:enter (mpDoorEnter teleports by hook); a real activation of the load door needs a Lua hook (player.lua: activate the nearest door object instead of teleporting) -- listed, not done |
| 244 | Harness: skips exit 0 (mp-harness.mjs:1038 ok = !err; #114 still open); load-gated convergence skips in s10/s40/s42/s43 turn a real divergence into SKIP on a loaded builder; s62 marks itself SKIP mid-run; Lua errors are printed, never fail | mp-harness.mjs exit code; fail on divergence regardless of load | fixed | mp-harness.mjs: any SKIP exits 1 unless OMW_ALLOW_SKIP=1; a client or peer Lua error FAILS the scenario unless it exports `allowLuaErrors = true` (only s125 printed one in #91: mpPuppetDetached on a creature without puppet.lua -- global.lua now checks hasScript first, so a second detach is a no-op); s10/s40/s42/s43 assert convergence/roster whatever the load (loadavg stays in the message); s62's partial branch logs `note:` |
| 245 | Diagnostic scenarios (s11, s65, s74, s98) assert nothing and count as PASS | `diagnostic` export reported separately | fixed | s11/s65/s74/s98 export `diagnostic = true`; the harness prints them as DIAG and a separate "diagnostic:" line, outside the PASS count |
| 246 | s20 asserts the sender's own mirror (proves nothing about the wire); s31/s111 chesttake: is a direct sendContainerOpByNet (the UI take diff is never raced); s32 door:lock: sends ObjectLock itself (s158's watch is the real path) | per row | fixed (s32 open) | s20 now asserts on B's puppets mirror: A's sethp:0 must show as `dead` on B's puppet and clear after the respawn (the mirror carries no hp; adding `hp` to global.lua's puppets mirror at :1242 would let it assert a non-lethal value); s31/s111 take race noted server-transaction-only in MP-COVERAGE-MAP; s32 left, needs a Lua hook that picks/unlocks through the engine rather than sending ObjectLock |
| 247 | Coverage map overstates: topics "OK" (s73 injects into testLearned; s75 says organic UNPROVEN); barter:sell writes the purse directly (TradeWindow not drivable headless) — say so | MP-COVERAGE-MAP.md rows | done | topics row → UNPROVEN organically; merchants row → watch + purse write, not the TradeWindow |
| 248 | Restore-over-time and Damage Magicka land TWICE on a peer-ruled body: the bar channel carries the local result AND both active-spell mirrors carry the effect (a Cheap Potion of Healing heals ~40, a 50-pt self heal ~100; a helper's durational heal 2x) — invisible to s146/s112/s144 which never assert a magnitude | global.lua withoutBarCarried (restore H/M, damage M dropped on both mirrors; fatigue kept) | fixed | lua-tests (helper run + both call sites), s165 heal-once (new, unbaked) |
| 249 | PvP-off veto bypassed by any spell record with one non-Harmful effect (forwardMagicHits merges a frame's hits with beneficial = OR; the receiver applies the whole record) | puppet.lua beneficial = every hit; combat.lua drops the harmful effects under PvP off; combat.ts vetoes a cast with any harmful entry | done (unit) | combat.test.ts (mixed cast vetoed, heal crosses), lua-tests |
| 250 | The receiver applies the WHOLE record ignoring which effects hit (range/area/resist): a Self+Touch record cast at an NPC gives the NPC the caster's Fortify | MagicHit.mEffectIndex (spelleffects.cpp seam) → puppet.lua effects[].index → CombatSpellHit.indexes (combat.ts validates) → combat.lua applies only those | fixed | combat.test.ts (indexes relayed/validated), lua-tests (owner applies the named indexes); C++ unbuilt |
| 251 | Scrolls and cast-when-used items at a puppet/NPC land NOTHING (source id is the ITEM record; combat.lua:241 gates on spells.records → nil); spellId never toNet/toLocal at that seam so a spellmaker spell at a puppet lands nothing either | combat.lua sourceEffects (spell, enchantment, or item→enchant); spellId toNet on send, toLocal on receive | fixed | lua-tests (scroll resolves by item id; id mapping source check) |
| 252 | CombatSpellHit unvalidated beyond shape + cell adjacency: a modified client lands any retail spell on any player/NPC in a neighbouring cell at 8/s with no magicka | combat.ts knowsSource: doc.spells or inventory must hold the id (sim peer exempt: it forwards the world's casts); the castat: hook now declares the spell first | done (unit) | combat.test.ts (unknown spell/scroll refused, known routed) |
| 253 | Magicka raises have no budget (hp has RESTORE_BUDGET): a modified client refills magicka at will | playerstate.ts: the same window/budget per bar (restoreMpInWindow) | done (unit) | avatarstats.test.ts (sustained refills refused) |
| 254 | Reflect ping-pong between two IDLE Reflect-wearers (the reflected copy lands on the caster's puppet and is forwarded without ignoreReflect) | MagicHit.mReflected from Flag_Ignore_Reflect (activespells.cpp stamps the bounced copy) → CombatSpellHit.ignoreReflect → add({ignoreReflect}) | fixed | lua-tests (flag relayed and applied); C++ unbuilt |
| 256 | A guest's level-up loses its health gain permanently: raise() returns early under restRefused (4 s window) before newBase() is consulted; identity never re-claims the base while the peer rules; relog restores the old base | playerstate.ts:209 keep the base step under restRefused; levelup: hook + mp.applyLevelup binding for the harness | done (unit) | playerstate.ts raise(): under restRefused the base step still lands (c capped at the old current, no heal); avatarstats.test.ts 'a base raise behind a refused rest lands the base'. Harness: `levelup:strength,endurance,luck` hook → mp.applyLevelup (mwmp/luabindings.cpp over MWMechanics::applyLevelup); scenario not yet written |
| 257 | The journal after any relog: one entry per quest (current stage only), hash order, all dated today (doc.journal is questId→index) | quests.ts record() append doc.journalLog {q,i,d} (cap 2000); JournalSync in order; mp.addJournalEntryAt binding for dates | done (unit) | quests.ts logEntry(): doc.journalLog {q,i,d,m,dm} (d = dayspassed as world.lua computes it, cap 2000, one line per stage) on the journalTarget doc; JournalSync carries journalLog in order; quests.lua replays it with ipairs through mp.addJournalEntryAt (Journal::addEntryAt, stamped) before the quest map, falling back to quest:addJournalEntry; quests.test.ts 'the journal sync carries the entries in the order they were earned' |
| 258 | Sneaking beside a friend: their puppet counts as an observer (updateSneaking) — the eye hides and Sneak never trains | actors.cpp:2091 skip avatars/puppets (the #143 predicate) | fixed | actors.cpp updateSneaking: observers that are avatars, or puppets without a content file, are skipped (canReportCrime's predicate). Needs a bake |
| 259 | Shared map exploration paints wilderness markers named "3" (unnamed exterior keys fall back to the key; vanilla adds visited locations for named cells only) | world.lua:627 skip unnamed cells | fixed | world.lua MP_WorldMapExplored: setMapExplored only when the exterior cell has a name (lua-tests source check) |
| 260 | Map exploration persists nothing and never catches up (relayed to the current roster only): a returning host has a blank world map; a guest sees only what is discovered from now on (refines #50) | m7.ts:325 doc.explored (cap 1024) in the welcome record; replay via MP_WorldMapExplored | done (unit) | m7.ts mapExplored(): exterior keys on the explorer's doc.explored (and the owner's when the map is shared; cap 1024, deduped); connection.ts welcome record carries `explored` (m7.exploredFor: the owner's when shared, else own); global.lua restoreTick replays it through MP_WorldMapExplored; worldtime.test.ts 'explored cells come back in the welcome record on a rejoin' |
| 263 | `use` held into a menu keeps the avatar wound up for the whole menu (playercontrols leaves controls.use) | player.lua inputTick mask use in a UI mode | fixed | player.lua inputTick: the use bit is masked while I.UI.getMode() is non-nil (the harness's press:/attack: force is kept) |
| 237 | Two equipped rings of one record collapse to one on the avatar/relog (record-id slot resolution = first match) | mwlua actor.cpp:52 |
| 255 | No hit VFX/sound on a puppet for the caster (the seam returns before playEffects) | spelleffects.cpp:1456 |
| 261 | Finding the host (#139): no map-marker Lua API; smallest real design = mp.setPartyMarker over WindowManager custom markers (minimap + map, active 3x3 only) + the friend's cell name in the social panel for the far case | luabindings.cpp, global.lua mirrorPuppets |
| 262 | Guest Rest UX: full wait animation, sky fast-forwards, then refused, sky slews back, heal undone by the next peer report ("the bed lied"); a pre-emptive refusal must NOT remove the sleep or guests stop levelling (#256) | timeSkip rule in WorldMode; UiModeChanged Rest for a non-owner → notice + level-up check |
| 264 | SCALE BLOCKER: the peer's actor stream draws from actorMoveBucket (60/s): ≥4 populated held cells starve the rest (same 3 cells pass every tick, the other cells' NPCs freeze) — certain in a TR city, already reachable in Balmora | connection.ts:485 exempt the authed peer as bytes/msgs already are | fixed | connection.ts: the authed peer is exempt from actorMoveBucket as from bytes/msgs; backpressure.test.ts: the peer holding 5 cells at 20 Hz for 2 s, zero actor_shed, every cell's stream flows; a player's actor batches still shed |
| 265 | Co-anchored interiors share one Bullet world and one navmesh at overlapping coordinates: NPCs in interior A collide with B's walls; anchored-interior pathfinding is straight-line | scene.cpp:700 skip the navigator add for anchors; collision groups per cell (engine work) — do not anchor >1 interior until then |
| 266 | Exterior navmesh covers only ~6,100 u around the dummy: AiFollow/Escort/pursuit in another anchored exterior walk straight through buildings | navmeshmanager.cpp:75 multi-centre ranges (engine work) |
| 267 | Peer tick budget: fixed dt, no catch-up → overload = world-wide slow motion; engageCombat N² per second; predictAndAvoidCollisions every tick; physics on the main thread; actors.lua scans ALL activeActors once PER HELD CELL per tick with pcall closures per actor | actors.lua:94 bucket by cell once per tick, hoist closures; gamedata.ts buildPeerSettings `[Physics] async num threads = 1`; osgStatsFile to measure | fixed (partial) | Lua + config only: actors.lua buckets activeActors by cell once per tick (actorsByCell) and hands each held cell its list, actorPose pcalls module-level functions; gamedata.ts buildPeerSettings `[Physics] async num threads = 1` (gamedata.test.ts). Engine side stays open: fixed dt/no catch-up, engageCombat N² and predictAndAvoidCollisions every tick, osgStatsFile |
| 268 | Client apply path: one LSER serialize→queue→deserialize→pcall per NPC per batch (2,000+/s at 100 NPCs), one extra frame of latency; NPC puppets never get a render tier so far-cell 1 Hz NPCs are steered every frame toward a stale target | netmanager.cpp decode 0x0200 into per-RefNum pose slots polled by puppet.lua (takeMagicHits pattern); stamp tier on NPC entries |
| 269 | WorldCellState can exceed the LSER node ceiling with every map merely AT its cap (placed 38k + moved 20k + containers uncapped) → every entrant BAD_PROTO permanently; a 9-cell entry burst of fat TR docs trips the 1 MB hard kill at the city gate | worldstate.ts:30 total-node budget at send (trim moved → containers.origin → memberVars); exempt the entry burst from the hard check ~2 s | done (unit) | worldstate.ts cellStateBody: one builder for WorldCellState/CellSnapshotReplace, counts nodes with lserNodeCount (exact, mirrors the decoder) and trims moved → containers → memberVars → newest placed until under 60,000, logging world.cell_state_trimmed; connection.ts skips the 1 MB hard kill for 2 s after sendCellStateAround (Player.cellStateBurstAt). worldstate.test.ts: a doc at every cap decodes; backpressure.test.ts: the entry window survives the ceiling, then the kill lands |
| 270 | Gateway governor is a static price (peerCount is always 1 now, peerCostMb dead, stale comments); no RSS/CPU sampling: 3 TR worlds on a 4-core box are admitted until OOM | worlds.ts:191 price by measured RSS (/proc VmRSS via child.pid) + loadavg reason 'cpu' |
| 271 | Small: netmanager caps a batch at 255 entries and actors.lua never chunks (actor 256+ never moves); unpackActorMoveBatch fully decodes N entries to read the epoch on the relay path | netmanager.cpp:321, actors.lua chunk; worldstate.ts:755 read the epoch header | fixed | actors.lua broadcastCell sends chunks of 255; worldstate.ts reads the epoch with peekActorMoveBatchEpoch (header + length check, no entry decode; actor.test.ts) |
| 272 | NIGHT BLOCKER: joining a friend from the launcher spawns the guest's own home world without entering it; sweep() discards a world nobody connected to within 15 min by rmSync of its whole dir — the guest's SOLO world cell state (looted containers, drops, doors, deaths) is deleted. Same for a returning character whose first TR download exceeds 15 min | worlds.ts:506-519 discard only when no world/world.db exists (else stop); or do not spawn the home world on a launcher join | done (unit) | worlds.ts sweep: never-joined discards only a dir without world/world.db, else stop(); worlds.test.ts revived-world-kept; launcher.html bootGame skips POST /worlds on a join when the character has a position under its home id (revive-on-dial brings it back) |
| 273 | Every in-place Solo↔Party click shows a 30 s black "Waiting for the world to be simulated" (holdUntilSimulated zeroes simReady; SimReady is only sent at join) — the harness bypasses the DOM handler | index.html:4610 drop the hold for the inOwn flip | fixed | index.html: the inOwn Solo/Party flip no longer shows the loading screen or holds for SimReady; the SocialResult status line is the ack |
| 274 | Cold-peer starts (TR: 2-5 min, after every idle reap) drop the player into a frozen world after the 30 s settle ceiling with no message | index.html:1391/5113 settle ceiling = peer startTimeoutMs, or a dismissible "world still starting" banner | fixed | index.html: settle ceiling 300 s (a page constant; nothing the page reads carries simPeer.startTimeoutMs), dead-man release at ceiling+10 s, dismissible "still starting" banner past the ceiling, cleared on simReady |
| 275 | A friend request or invite sent to someone in ANOTHER world produces no toast (live events only in-world; the 10 s FriendList snapshot rebuilds silently) — the night's starting state | social.lua MP_FriendList notice() for newly present requests/invites | done (unit) | social.lua MP_FriendList notices newly present requests/invites once per acct (requestsSaid/invitesSaid); index.html status line names them; lua-tests source check |
| 276 | The terminal modal says "wrong password for this account name" for every AUTH_FAILED (expired ticket, wrong character, chargen, sign in again); UNREACHABLE doubles its detail; the title is always "Can't join multiplayer" even for KICKED/SUPERSEDED; friendlyMpErrorBase is dead code | global.lua:316-328 | done (unit) | global.lua FAIL_TEXT AUTH_FAILED='sign-in was refused' (detail says why), no duplicate UNREACHABLE detail; index.html title Disconnected for KICKED/SUPERSEDED/BANNED/SHUTDOWN, friendlyMpErrorBase folded into friendlyMpError; lua-tests source check |
| 277 | "Go back to sign in" forces a fresh login although the session is valid (no #characters hash); SUPERSEDED copy says "account" while it is per character | index.html:1546 | fixed | index.html: the modal button goes to launcher.html#characters when omw-mp-session is set; SUPERSEDED reads "this character" |
| 278 | Continue within ~20 s of the idle reap says "The server is full" (ensure() null while draining → 503) | worlds.ts:292 / directory.ts:405 distinct restarting error + one retry in bootGame | fixed | worlds.ts isStopping + directory.ts POST /worlds answers 503 {error:'restarting'} for a draining world; launcher.html retries once after 3 s, then a distinct sentence; the cap message is unchanged |
| 279 | GET /worlds?account=<key> is unauthenticated: private world ids, mode and player count for any guessed login name | directory.ts:276 |
| 280 | Guest spawn is one-shot: if the host has no pose at the joiner's auth (loading, peer restart) the guest boots at the engine default start and is never moved; an auth-rescue reboot re-teleports a guest beside the host | server.ts:928 | done (unit) | server.ts guestSpawn: a host without a pose defers the InviteAccepted teleport to their first pose (1 s poll, bounded by the peer start timeout); a guest with a stored position in this world is not re-spawned; onecharacteroneworld.test.ts |
| 281 | KICKED race on "sent home": the 5 s disconnect can beat a slow ticket mint → "you were kicked" modal instead of going home | server.ts:500 | fixed | server.ts sendGuestHome: KICKED grace 30 s (guestKickGraceMs test seam; flipworld.test.ts uses 500 ms) |
| 282 | Sign-in error copy: every #mperror code renders "<code>. Please try again." (registration_disabled, banned, invite_required...) | launcher.html:1524 | fixed | launcher.html: #mperror code -> sentence table for every routes.ts/oidc.ts code; "Please try again" only where retrying can help |
| 283 | Copy/gate mismatches: handle hint vs server rule; launcher says Chrome/Edge/Brave while index says Firefox 134+ is fine; touch laptops < 820 px tall get "needs a desktop browser"; no_such_character followed by "server may be unreachable" | launcher.html:404, 873, 1415 |
| 284 | Two-device: presence PK per account (two characters flap), localStorage resume shared across tabs, deleting a character being played elsewhere stops+removes its world and the other device reconnects forever | socialstore.ts:176, index.html:1445, frontdoor.ts:148 |
| 285 | HIGH: every AI path (wander walk, chase, pursue, follow, escort, travel, cast) freezes for NPCs ≥2 cells from the peer's dummy and in every anchored interior while the dummy stands outdoors: pathTo → isNearInactiveCell converts positions into the DUMMY's cell frame and zeroes them at the grid edge | aipackage.cpp:113/406-425 take the actor, false for interiors, test the actor's cell against Scene::isWithinActiveGrids (anchor-aware) fixed | aipackage.cpp isNearInactiveCell(actor, position): false in an interior; an exterior tests the cells at position ± 200 against World::isWithinActiveGrids (scene.cpp, anchor-aware; new MWBase::World accessor). No anchors = the player's 3x3, vanilla. s166: a provoked creature two cells from the dummy closes 450 u in 15 s |
| 286 | NPC puppets never run: actors.lua reads a non-existent types.Actor.isRunning so bit 0 is never set; a running guard outpaces the walking puppet and snaps every cooldown | actors.lua:121 flags bit0 = animVel > 1.05 fixed | actors.lua actorPose: bit 0 = animVel > 1.05 (speed vs walk speed); the isRunning read is gone. lua-tests |
| 287 | Respawn-flagged NPCs (guards) are re-killed at every re-anchor after the 72 h expiry (actorOverrides/lastSnapshot still say dead, the grant loop teleports + writes hp 0), kill tally climbs | actors.lua:421 skip a.dead in the grant loop (deaths re-assert via WorldCellState) fixed | actors.lua MP_ActorAuthorityGrant skips a.dead entries; WorldCellState → noteCellDeaths re-asserts a standing death. lua-tests |
| 288 | NPC/creature attacks, casts and idles are invisible on clients: holder sets no bit 3, puppet.lua has no creature attack branch, MP_CastFx only from player.lua, wander idle variants never travel | companion.lua use-edge → bit 3; puppet.lua creature attack1 branch + spell-stance cast |
| 289 | Doors opened by NPCs on the peer never reach clients (only the activating PLAYER relays; the poll covers locks only, dummy's cell only): the puppet hits the shut door and snaps through | objects.lua:1140 poll getDoorState over held cells on the peer fixed | objects.lua 1 Hz poll: on the peer the cell list is the 3x3 ∪ actors.heldCells(); a non-teleport door read Idle with a changed open state sends DoorState; MP_DoorState writes doorStateWatch before applying (echo mute). lua-tests |
| 290 | Puppets take local fall/drowning damage (the gate is isPlayer only); a local kill of an NPC puppet is permanent on that client (no MP_Revive sender for NPCs) | character.cpp:2258, actors.cpp:1031 add MWMP::isPuppet fixed | character.cpp fall damage and actors.cpp drowning skip MWMP::isPuppet(refnum) bodies (the holder takes it). NPC MP_Revive sender not added — no local damage means no local kill |
| 291 | Flying/swimming creature puppets move level (pitch streamed as 0; steering is 2D): a cliff racer above its target hovers until the 256-unit teleport | actors.lua:137 pitch = rotation:getPitch() fixed | actors.lua actorPose pitch = obj.rotation:getPitch(); puppet.lua already applies it (#133). lua-tests |
| 292 | NPC combat against an avatar is dropped where vanilla holds it for the player (target in deep water/flying/invisible): wade into water to reset a retaliating NPC; avatar companions are not player allies in engageCombat | aicombat.cpp:195 isPlayerOrAvatar; actors.cpp:675 fixed | aicombat.cpp keeps the fight when the target is isPlayerOrAvatar or its ally set holds one; actors.cpp engageCombat: "sides with a player" = any_of(isPlayerOrAvatar) over the actor's own siding set (siding is symmetric, so no avatar registry walk) |
| 293 | Scripted Resurrect on the peer never travels (no ActorDeath inverse) | actors.lua:281, worldstate.ts:792 |
| 294 | Followers cross a door only when the avatar's teleport succeeded synchronously; the 30 s retry carries the avatar alone | global.lua:2483, :203 |
| 295 | Holder-lost Info goes to that cell's occupants only and detaches that cell only: neighbour-cell puppets stay AI-off/frozen through a peer outage | authority.ts:308, actors.lua:458 |
| 296 | NPC magic state never mirrors to puppets (Paralyze, Invisibility, Chameleon, Sanctuary, Levitate on NPCs — VISIBLE_EFFECT is player-only); knockdown/hit-recoil not carried | global.lua VISIBLE_EFFECT for actors; ActorMoveBatch flags |
| 297 | Corpse loot is canonical from the OPENING client's copy: potions the peer NPC drank / arrows it fired remain; per-engine levelled-item rolls differ; ActorEquip names records the puppet lacks (fight weapon ≠ loot weapon) | worldstate.ts container first-open from the holder for NPC corpses |
| 298 | CRITICAL: cell docs are keyed by content-file INDEX (c:<index>:<contentFile>): install/uninstall/reorder/disable ANY plugin (or a Lua .omwscripts mod, or Tribunal off) and every tombstone deletes a different object, every moved/lock/door/memberVar/death applies to the wrong ref — silently, with no reset affordance | ref.ts:13, cellstore.ts:73: stamp the canonical content list in world_global; remap c: keys by name→index on a list change (drop gone files) or refuse to start; long-term key by file name | done (unit) | cellstore.ts: world_global.content = the peer's manifest names in idx order, stamped by WorldState.adoptContentList (connection.ts handleHello, right after setAuthoritative). A boot under a different list rewrites every cell doc (deleted, moved, locks, doors, containers, actorDeaths, memberVars, enabled, follows) by file name → new index in one sync sqlite tx, drops keys whose file is gone and the opaque actorOverrides blob, logs world.content_remapped {changed, dropped}; no stored list (legacy world) = stamp and continue. celllifecycle tests: Tribunal removed, Bloodmoon inserted |
| 299 | Version skew of a same-named plugin is undetectable (manifest carries size=0 and no sha256 for anything; strict ≡ names; hashContentFiles has no callers): a locker player with TR 24 vs the server's 25 passes the gate and desyncs every ref | net.lua:137, manifest.ts:87, gamedata.ts:312: fill sha256 from the locker (index.html:3125) and hash server plugins; or refuse locker boots on modded worlds | fixed (partial) | Server half: gamedata.ts hashContentFiles hashes gamedata/ plugins AND enabled mods/<slug>/ plugins (lowercase name → sha256); ContentGate.hashes is read lazily at setAuthoritative so the peer's pinned entries carry sha256 and `strict` refuses a same-named different-version plugin. Locker side (index.html:3125 sending sha256) is the follow-up; under `names` nothing changes. manifest/gamedata tests |
| 300 | Top-level .omwaddon/.omwscripts in gamedata/ load on the peer but not in the browser (buildLoadOrder takes esm/esp only) → every client BAD_CONTENT | index.html:2181 accept the same set as gamedata.ts:51 | done (unit) | index.html buildLoadOrder: .esm → masters; .esp/.omwaddon/.omwgame/.omwscripts → plugins; .bsa/.ba2 → archives — the same split as gamedata.ts. play-front-door test |
| 302 | Missing master: the browser cascades the plugin out and boots, the peer aborts (esmloader.cpp:48 throws) → peer crash-loop, unsimulated world, no error naming the cause | mods.ts:172 apply the same cascade in resolveMods | done (unit) | mods.ts resolveMods: after orderByMasters, a plugin whose declared master is neither an (enabled) official master nor in the list is dropped, cascading until a pass drops nothing; logs mods.missing_master {plugin, master}. data= lines stay. mods test |
| 303 | fallback-archive order differs for TOP-LEVEL archives (peer sorts all alphabetically incl. official; browser official first then mods): a top-level replacer BSA wins in the browser and loses on the peer → different meshes AND collision | gamedata.ts:143 official first in OFFICIAL order, then the rest | done (unit) | gamedata.ts: archives sort official-first in OFFICIAL order, then the rest by name — the browser's order. gamedata test |
| 306 | The persistent chunk cache keys by pathname@size: a re-installed mod file of identical size serves old bytes, bypassing #178 | streamfs.js:241 add mtime from mwdata-mods.json files[].m | done (unit) | mwdata-routes.ts walk() adds `m` (integer mtime ms) to every files[] entry; index.html mountServerMods passes f.m to StreamFS.mount, whose persistent key is pathname@size[@mtime]@CHUNK. Retail mounts pass no mtime (immutable, keeps every cached base game). mwdata-serve + play-front-door tests |
| 301 | No groundcover= support: a grass plugin becomes content= on both engines (tens of thousands of addressable refs per exterior on the peer, rendered as statics in the browser) | mod-install.ts:414 flag + buildPeerCfg/index.html groundcover=, excluded from the manifest |
| 304 | A mod's addHandlerForObject / lazily registered activation handler pre-empts the dialogue lock and pickup veto (per-object handlers run before type handlers); no Lua-mod guidance | activationhandlers.lua:40; docs |
| 305 | Lua mods that create objects on a client are booked as player drops (onItemActive → requestSpawn fromInventory) — N clients = N spawn requests, each debited/refused | objects.lua:581 gate on a recent local drop event; document unsupported |
| 307 | Armor/Unarmored/Block skills NEVER progress while the peer rules (hits land on the avatar: I.SkillProgression is player-only, block use gated on == getPlayer()) — permanent progression loss for tanks | combat.cpp:172 isPlayerOrAvatar; local.lua:186 + avatar.lua _onSkillUse → AvatarSkillUse → owner's I.SkillProgression (block/armor only) fixed | combat.cpp block use on isPlayerOrAvatar; omw/combat/local.lua (+ fsroot mirror) calls I.MPAvatar.skillUsed when SkillProgression is absent; avatar.lua exposes MPAvatar + _onSkillUse and forwards the armour/block family only (mpAvatarSkillUse) → global.lua AvatarSkillUse {id, skill, useType} → playerstate.ts handleAvatarSkillUse (world peer only, family + useType 0..3 validated, 10/s per owner) → SelfSkillUse → player.lua MP_SelfSkillUse → I.SkillProgression.skillUsed. avatarskilluse.test.ts; lua-tests |
| 308 | Melee sneak-attack critical still gated on the local player (npc.cpp:662 wraps skill use AND the crit; wave-4 fixed ranged only — row 90 overstated) | npc.cpp:662 split the gate: crit on isPlayerOrAvatar fixed | npc.cpp: skillUsageSucceeded stays == getPlayer(); the unaware check + fCombatCriticalStrikeMult + critical sound run on isPlayerOrAvatar; the sTargetCriticalStrike message box stays player-only |
| 309 | The owner's taps are lost and the owner rubber-bands while the avatar is in hit recovery (useLatch consumed in one frame; kd report covers knockdown only, not hit recovery) | luabindings.cpp:274 kd = knockedDown or hitRecovery; avatar.lua keep useLatch armed while kd fixed | mp.isKnockedDown returns getKnockedDown() or getHitRecovery() (the stats report's kd rides it, so player.lua holds still through a stagger too); avatar.lua clears useLatch only when mp.isKnockedDown is false. lua-tests |
| 310 | Owner hit/miss feedback is an independent coin flip (local roll vs the avatar's roll: 50% mismatch at 50% chance); a landed punch plays "miss" (fatigue-only damage fails the health check) | puppet.lua:239 play on an hp/ft DROP in MP_Stats instead of the local roll fixed | puppet.lua onHitIntercept only records the swing time/hitPos; MP_Stats plays 'Health Damage' on an hp drop (blood at the swing's hitPos if within 1 s, else no blood) and 'Hand To Hand Hit'/'Hand To Hand Hit 2' on a fatigue-only drop within 1 s of a swing (running drains fatigue too, hence the window); 'miss' never plays. lua-tests |
| 311 | Elemental-shield reflection applies locally to a peer-ruled body on the dead swing (no peerRulesBody gate, unlike fall damage) — can kill the local body ahead of the peer | combat.cpp:402 gate like character.cpp:2260 fixed | combat.cpp applyElementalShields skips the setHealth when the attacker is the player and MWMP::peerRulesBody(); the Health Damage sound stays |
| 312 | Blocked hits are silent and invisible for the owner; armor-type hit sounds play on the peer only; block state consumed per frame (needs a per-avatar counter drained with the stats report) | character.cpp:399 |
| 313 | Dead-swing side effects on local NPC copies (arrow-in-inventory roll, shield charge/fatigue drain, unequip) become canonical loot via #297 | combat.cpp:299, npc.cpp:696 |
| 314 | Werewolf kill count lands on the avatar's NpcStats, never in the doc | npc.cpp:826; identity.lua |
| 315 | HIGH: custom-record ids are per WORLD (mp_<kind>_<n> from 1 in each world's RecordStore) while the player doc crosses worlds: a friend's potion/enchanted item taken home is transmuted into another world's record or silently dropped from the doc | server.ts:225 open RecordStore on sharedDir (one registry per deployment) |
| 316 | During the host's 90 s crash grace the world thinks it has no owner (journalTarget from the roster): peer-driven journal/globals land in every GUEST's home doc; a guest's quest writes persist nowhere | server.ts:587 latch ownerCharId at the owner's first auth |
| 317 | A character parked in a removed mod's interior (or a disabled expansion's cell) cannot launch (engine aborts at --start); the F5 path drops them at exterior 0,0 = open sea, then persists the drowning | global.lua:1370 validate the cell, fall back to rules.respawn*; index.html reboot once without mpstart on "cell is not found" |
| 318 | Tombstones fill with useless n: keys (summons, levelled spawns); at 2000 every take in the cell replies ok but never persists — the cell's loot dupes on every reload | worldstate.ts:1118/1509 net refs need no tombstone; content refs reply cell_full instead of a non-persistent ok |
| 319 | [sharing] factions=false in an owned world: a guest's rank writes still target the HOST's doc (the isShared check comes after the write) — the host logs in demoted | quests.ts:606 target = shared ? journalTarget : player.charId |
| 320 | Journal sharing flips: individual mode is a hybrid (host's journalLog + own map, writes to the host); shared→individual→shared replays a stale shared map and regresses the owner's stages every login | quests.ts:359-394 merge shared.journal = max always; route log/borrowed by charId in individual mode |
| 321 | journalLog cap 2000 drops the BEGINNING of the campaign (TR passes it) | quests.ts:324 raise to ~5000 or chunk JournalSync |
| 322 | never_joined discard never fires: world/world.db exists the moment a world boots (sqlite constructor) — abandoned sign-ins leak directories permanently | worlds.ts:529 test a .played marker written on the first human join |
| 323 | Custom records never cleaned up; at 10,000 the creator's brew silently fails and the item does not survive a relog | m7.ts MAX_CUSTOM_RECORDS; dedupe (#60) |

## Open (found, not fixed)

| # | Item | Notes |
|---|---|---|
| 28 | Magnitude re-roll on the avatar (spell with a range is rolled again on the peer) | no engine API to inject a magnitude; would need a binding or a per-cast fixed record |
| 29 | "Your guests were sent home while you were away" narration for a returning host | server.ts onPlayerLeftWorld + WorldMode |
| 30 | Gateway restart with a party: revived as private, guests get 502 then not_open | design-adjacent: host flips Party again |
| 31 | Half-open socket: client has no pong watchdog | net.lua |
| 32 | Social OK notices ("Invitation sent.", "Sent home.") never mirrored to the feed | social.lua status |
| 33 | Dying inside a dialogue: lock release unverified | quests.lua |
| 34 | No harness hook opens Training/Travel/SpellCreation/Enchanting (`svc:open:<Mode>`) | player.lua |
| 35 | PvP kill: no attribution; no scenario kills a player with PvP on | combat.ts |
| 44 | Resist arrest: MP_OpenDialogue bypasses the dialogue lock; guard puppets never report "fights me" | global.lua, puppet.lua, quests.lua | 
| 45 | Jail: time skip is a local advanceTime, not through the server clock; skill loss/confiscation unverified | jailscreen path, world.lua |
| 46 | Pickpocket caught: victim's startCombat happens on an AI-off puppet, never reported | puppet.lua |
| 47 | Guest rest under timeSkip=owner: the clock is refused but the local heal is claimed (free full heal) | identity.lua / world.lua timeRefused |
| 48 | Guest's local advanceTime changes weather; holder re-sends only on change | world.lua tickWeather (clear lastWeatherSent every ~60 s) |
| 49 | dayspassed never written by writeLocalTime: TimeStamp goes backwards (powers/day, corpse timers, disease) | world.lua TIME_FIELDS |
| 50 | Map exploration/markers not persisted (blank map every login, solo included); custom markers not shared | identity.lua, playerstore.ts |
| 51 | Dialogue topics relayed, never persisted | quests.ts |
| 52 | Cell reset: vanilla loot never reappears on a running engine (only future joiners) | objects.lua |
| 53 | Weather region freezes when the holder goes indoors (no handoff) | weather.ts |
| 54 | Item condition: peer wholesale report can undo a fresh repair in the merge window | playerstate.ts handleAvatarItemStatesBatch |
| 55 | Enchant charge oscillates (both engines run passive recharge) | cosmetic |
| 56 | Mixed stacks (one damaged of five) collapse into one stack on relog | global.lua restore (create one object per state entry) |
| 57 | Container put/take carries no item state | objects.lua snapshotContainer, worldstate.ts |
| 58 | Bound items recorded in the inventory doc; a relog inside the window grants a permanent one | identity.lua snapInventory (skip sMagicBound*ID) |
| 59 | Summon: holder loss mid-effect leaves no local creature until recast | global.lua |
| 60 | Alchemy: identical potions brewed by two players are two records (doc bloat) | m7.ts (dedupe by content) |
| 73 | Knockdown/hit-recoil on the avatar not relayed: owner rubber-bands instead of "cannot move" | global.lua stats entry + player.lua controls override |
| 74 | Long fall: self-snap → PlayerCellChange → avatar teleported mid-air → fall height reset → free fall | player.lua jump detector vs falling |
| 75 | Peer restart mid-swim spawns the avatar at the last cell-change point; owner snapped there | connection.ts teleportPose gate |
| 76 | threat.lua is dead code (relayed CombatHit shape never matches); NPCs strobe between two attackers | combat.lua, threat.lua |
| 77 | Dialogue holder's NPC keeps wandering on the peer while talking; the other player watches it walk off | actors.lua (Wander distance 0 on lock) |
| 78 | Greeting/idle on the peer targets the parked dummy: NPCs near the park spot face nothing | actors.cpp (skip when peerRulesBody) |
| 79 | Escort claim needs no dialogue lock: any client can send any NPC walking | worldstate.ts |
| 80 | "npc"->AddItem in a dialogue result lands on the local puppet only | no relay for non-holder NPC inventory |
| 81 | Level +1 per message at 60/s reaches 255 in 4 s; attributes/skills unbounded | playerstate.ts clamp + one step per 10 s |
| 82 | Gold placement with fromInventory=false is counted, never refused (party worlds) | worldstate.ts |
| 83 | Fabricated first ContainerOpen becomes canonical (poison every container in a town) | worldstate.ts plausibility cap |
| 84 | ActorAI travel destination bounded only by MAX_ABS_COORD | worldstate.ts (±2 cells) |
| 85 | Guest can set the host's quest globals (by design of the shared journal) | quests.ts — wontfix? |
| 86 | Memory governor prices a world at 640 MB regardless of anchors/party size | worlds.ts /status anchors |
| 87 | No harness run beyond 4 browsers; soak bots never touch containers or a peer | bots/soak.ts |
| 88 | BAD_CONTENT terminal: nothing tells the player that a reload after the mod mounts is the remedy | index.html mpErrorModal |
| 89 | Launcher "friends playing now" lists party+occupied only; an online-but-solo friend is invisible | launcher.html |
| 100 | s147 on the managed peer: avatar follow-teleported to z=1453 on release and no fall damage reported; server logged avatar_stats_gated (client sent no input for 6 s mid-ritual) -- why does a standing client stop driving? | player.lua inputTick / harness |
| 101 | Nametags: none rendered over puppets (only the crosshair tooltip); stale name after a rename | needs an OSG text node |
| 106 | Companions not persisted across a world restart (follow claims are memory only) | worldstate.ts followedBy → cell doc |
| 107 | memberVars (per-object MWScript locals) persisted but never replayed to joiners/peer | quests.ts, WorldCellState |
| 108 | Player and cell docs sweep on independent 45 s timers: a crash can dupe or lose a container take | playerstore/cellstore |
| 109 | Arrows land twice (owner's local miss + the peer's); missed arrows stored in the puppet copy | objects.lua onItemActive / launchProjectile gate |
| 110 | Essential-NPC message shown on the peer, never the owner | actors.cpp |
| 111 | hitn: bypasses the real swing path (combat.lua ignores non-test Hits); most kill scenarios prove the relay, not the swing | scenarios → attack:/press: |
| 112 | sethp:/rest: hooks are direct writes; no scenario exercises death-from-damage, the wait dialog, training, level-up dialog | hooks |
| 113 | Mirrors that are never cleared (hitFwd, spellFwd, castAt, doorEnter, takeOwned, chestOp) let a second wait pass instantly | scenarios: clear before waiting |
| 114 | SKIP detection by log text; a sweep of skips exits 0 | mp-harness.mjs |
| 115 | Peer clock is load-dependent (fixed dt 1/20 per tick): duration-based asserts compare two clocks | engine.cpp headless |
| 125 | /auth/link puts the session token in a query the edge logs; dead in gateway mode | routes.ts |
| 126 | ActorBatch is one uncapped message per cell per frame: a TR metropolis exterior is ~100 KB/s per client | actors.lua |
| 127 | No TR scenario in the harness; builder gamedata has no mods | harness |
| 137 | Puppet bow: `shoot attach` runs, `shoot release` gated on mReadyToHit: the arrow may stay glued to the hand after a shot | character.cpp:1141-1145 |
| 138 | Revive is despawn+spawn: no get-up animation on the puppet | global.lua:2383 |
| 139 | Feature gaps: no chat bubbles, emotes, party marker on map/compass, friend highlight beyond the crosshair name | — |
| 145 | Peer NPC aggression toward an avatar is computed against the parked dummy (race/Personality/faction of the dummy, not the player) | mechanicsmanagerimp.cpp:493 getDerivedDisposition per target; apply doc.factions to the avatar |
| 146 | Theft/trespass victim reactions (startCombat, disposition drop) land on the thief's AI-off puppet copy; a Fight-70 NPC that would attack a thief just barks | generalises #46 |
| 148 | mStolenItems not persisted: after a relog stolen goods sell back to the victim and are not confiscated on arrest | needs a binding |
| 157 | Corprus worsening resets on relog (worsen count not on the wire) | activespells.cpp:188 |

## Wontfix / by design

| Item | Reason |
|---|---|
| Guests keep loot, not quests | product decision (mp-overhaul) |
| MP tile hidden on prod (`?experimental=1`) | until the human playtest |
