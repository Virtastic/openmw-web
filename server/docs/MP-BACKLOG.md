# Multiplayer backlog

What the async audits and harness sweeps found, in one place. One line per item; the state
column moves as the fix lands and the bake proves it. Coverage rows live in MP-COVERAGE-MAP.md;
this file is the work queue.

States: **open** (found, not started) · **fixed** (committed on the branch, no live verdict yet)
· **baking** (in a Jenkins sweep) · **done** (green in a sweep, or unit-tested where the harness
cannot reach) · **wontfix** (design decision, reason given).

Branch: `test/posture-seen`. Sweeps: Jenkins `openmw-web-dev` #89 (9/17), #90 (13/14; s147 open as #100), #91 (27/31: s114 #183, s147 #100, s149 harness cmd timeout #185, s160 booted the example suite — fixed).

## Found by the 2026-09-14/15 audits

| # | Item | Where | State | Proof |
|---|---|---|---|---|
| 1 | Invisibility/Chameleon/Light invisible on other screens; late joiner not caught up | global.lua, playerstate.ts | done | s156, avatarstats.test.ts (green #90) |
| 2 | Dead NPCs replayed by wire key vs object id (no-op); holder never got cell record | actors.lua, worldstate.ts | done | s157, holderhears.test.ts (green #90) |
| 3 | Holder deaf to relays for far anchored cells (doors/locks/objects) | worldstate.ts | done (unit) | holderhears.test.ts |
| 4 | Scripted Lock/Unlock never travelled (activation-only watch); then own-cell-only poll | objects.lua | done | s158 (green #90) |
| 5 | Attribute damage lost on relog / not on avatar | identity.lua, avatar.lua | done | s159 PASS #89 |
| 6 | Companion dropped on the player's relog (claim keyed by session id) | worldstate.ts | done (unit) | actor.test.ts |
| 7 | Host's quest globals never reached the peer; guests/peer seeded from own doc | quests.ts | done (unit) | questpeer.test.ts |
| 8 | Enable/disable ping-pong between disagreeing engines | worldstate.ts | done (unit) | holderhears.test.ts |
| 9 | Rolling restart booted with the flipped mode | worlds.ts | done (unit) | worlds.test.ts |
| 10 | Drop then disconnect before the inventory diff: dupe | server.ts | done (unit) | provenance.test.ts |
| 11 | One character in two worlds: last-writer-wins doc | server.ts, socialstore.ts | done (unit) | onecharacteroneworld.test.ts |
| 12 | Deleted character resurrected by a stale process flush | playerstore.ts | done (unit) | staledoc.test.ts |
| 13 | Item dropped across a cell border invisible to a joiner | worldstate.ts, connection.ts | done (unit) | holderhears.test.ts |
| 14 | PlayerDeath trusted blindly while the peer reports alive | playerstate.ts | done (unit) | avatarstats.test.ts |
| 15 | Late resurrect left the engine's main menu up | luabindings.cpp | fixed | (needs a live death with a slow round-trip) |
| 16 | F5 mid-play bounced to the launcher (fresh session, not resume) | index.html | fixed | manual / s80-style |
| 17 | Resume with the token of a still-open socket fell to a page reboot | connection.ts | done (unit) | admin.test.ts |
| 18 | Merchant stock never restocked (gold only) | worldstate.ts | done (unit) | economy.test.ts |
| 19 | Spellmaker spell persisted under a local dynamic id | global.lua, identity.lua, playerstate.ts | fixed | (scenario: make a spell via the real menu, relog) |
| 20 | Invite toast repeated on every join within 2 min | social.lua | fixed | — |
| 21 | Travel fare dropped by the reach gate (sent from the destination cell) | worldstate.ts | done (unit) | economy.test.ts |
| 22 | Disposition lost on peer restart / handoff | actors.lua | fixed | (scenario: persuade, restart peer) |
| 23 | Local fall/drown damage killed the client before the peer's verdict | character.cpp, actors.cpp | fixed | 7c91a2aa + 4a10d985: character.cpp:2260 / actors.cpp:1036 gates; verification stays under row 232 |
| 24 | Peer avatar never drowned: OUT OF PROCESSING RANGE of a hand-started peer (harness) | harness | done | s149 PASS #90 (drowning cost 82 hp, both sides agree) |
| 25 | A heal in the frame a peer report lands was erased before measurement | identity.lua | done | s150, s146 (green #90) |
| 26 | Cloud-locker page could not change world a second time (mplocker dropped) | index.html | done | s141 PASS #90 |
| 27 | Guest death seen by the host (puppet falls, gets up) | s131 | done | s131 PASS #89 |
| 36 | Magic at a puppet: only Damage/Restore H/M/F forwarded; Calm/Soultrap/Paralyze/Levitate-on-NPC applied to the local copy only | spelleffects.cpp | fixed | needs a target-spell mint hook + scenario |
| 37 | PvP swing/kill read as assault/murder by witnesses | mechanicsmanagerimp.cpp | fixed | (scenario: pvp on, hit B before a guard, bounty stays 0) |
| 38 | Guest's welcome carried its own doc's bounty; peer's guards read the world's | connection.ts, quests.ts | done (unit) | standing.test.ts |
| 39 | A drop carried record+count only: a pristine copy for the friend (free recharge) | objects.lua, worldstate.ts | fixed | s160, worldstate.test.ts |
| 40 | Equipment relayed at join before RecordsSync; made item never re-declared under its net id | connection.ts, global.lua | fixed | — |
| 41 | Sweep #89: a heal in the frame a peer report lands was erased before measurement | identity.lua | done | s150 (green #90) |
| 42 | Sweep #89: second world change on a cloud-locker page died with "no locker session" | index.html | done | s141 (green #90) |
| 43 | Sweep #89: scripted-lock poll covered the player's own cell only | objects.lua | done | s158 (green #90) |
| 61 | Half of all real jumps never reached the avatar (one-frame trigger vs every-other-frame sender) | player.lua | fixed | scenario: tap-jump 10x, avatar rises 10/10 |
| 62 | Avatar follow-teleport gave up after 3 s (cold interior load): no puppet in the room | global.lua | fixed | — |
| 63 | Actor batches for far anchored cells refused as stale epoch: frozen NPCs everywhere the peer's avatar was not | authority.ts, worldstate.ts | done (unit) | holderhears.test.ts |
| 64 | The peer drew from a player's byte/msg budgets; three busy cells got it disconnected with RATE | connection.ts | fixed | — |
| 65 | A dead companion's follow claim replayed to a restarted peer | worldstate.ts | fixed | — |
| 66 | Every terminal modal read "could not be reached (CODE detail)" | index.html | fixed | s91 DOM assert wanted |
| 67 | Joining a friend mid-chargen rebooted into a refusing door; unfinished character lost | social.ts, social.lua | fixed | scenario s161 sketch |
| 68 | In-game Exit did not say it was leaving: guests waited out the 90 s grace | mainmenu.cpp, index.html | fixed | — |
| 69 | Refused rest (timeSkip=owner) still healed the guest's local body: free full heal | m7.ts, playerstate.ts | done (unit) | avatarstats.test.ts |
| 70 | Bound items declared in the doc: relog inside the spell granted a permanent one | identity.lua | fixed | — |
| 71 | dayspassed never written: day-based timers a day late or backwards | world.lua | fixed | — |
| 72 | Weather holder spoke only on change; a guest's rolled-back rest left the wrong sky | world.lua | fixed | — |
| 90 | Sneak-attack critical / weapon skill use / OnPCHitMe gated on attacker == getPlayer(): never the avatar | combat.cpp, npc.cpp, creature.cpp, actorutil | fixed | scenario: sneak behind an NPC, hit ≥ 4x |
| 91 | On-strike enchantment cast by the owner's dead swing AND the avatar: twice, charge drained twice | spelleffects.cpp | fixed | — |
| 92 | Peer hp-0 report gated for a non-driving (alt-tabbed) player: death never recorded, free resurrect | playerstate.ts | fixed | — |
| 93 | Container take in the last 2 s before a disconnect: debited from the container, never credited | connection.ts | fixed | — |
| 94 | Cell reset resync omitted locks and deaths | worldstate.ts | fixed | — |
| 95 | Guest never told whose world it is on a fresh page; Enter opens chat; chat key behind the reconnect banner; accept buttons twice; modal ignores Lua's sentence; toasts said (F) | global.lua, index.html, social.lua | fixed | — |
| 96 | Harness: no per-scenario ceiling (a hung scenario killed the sweep with no summary) | mp-harness.mjs | fixed | — |
| 97 | Harness: the peer's Lua errors were never scanned | mp-harness.mjs | fixed | — |
| 98 | Harness: worlds spawned on GW_PORT+200, onto other scenarios' gateway ports | _gateway.mjs, s47, s48 | fixed | — |
| 99 | Stat values unbounded, level +1 sixty times a second, a container's first open canonical however absurd | playerstate.ts, worldstate.ts | done (unit) | adversarial.test.ts |
| 116 | Arrows landed twice (owner's phantom + the peer's); knockdown not relayed (owner rubber-banded) | objects.lua, global.lua, player.lua, luabindings.cpp, playerstate.ts | done | avatarstats.test.ts (kd); s109 s110 green #91 |
| 117 | Companions lost on a world restart; memberVars persisted but never replayed | worldstate.ts, cellstore.ts, objects.lua | done (unit) | actor.test.ts, quests.test.ts |
| 118 | TR landmass installed before Tamriel Data loaded in that order: a quietly corrupt world on every engine | mods.ts | done (unit) | mods.test.ts |
| 119 | PROD: the inner Caddy stripped CF-Connecting-IP -- every player was the edge's IP (one login budget, one household, /ipban bans everyone) | deploy/Caddyfile | fixed | needs [limits] trustCloudflareIp = true in the prod config.toml + a deploy |
| 120 | Tamriel Data: 54k loose files, one blocking round trip each on first read -- multi-second freezes per new TR cell | mod-install.ts, bsa-pack.ts (pack >500 loose assets into <slug>.bsa at install) | done (unit) | bsa-pack.test.ts, mod-install.test.ts |
| 121 | Ban in world A never kicked the player from world B (ticket refused only later) | server.ts (ban poll on heartbeat) | done (unit) | admin.test.ts |
| 122 | Account delete left locker sessions/tickets behind (erased account could still play out the ticket) | persist/erase.ts, playerstore.ts erased tombstone | done (unit) | staledoc.test.ts |
| 123 | Ticket TTL (15 min) started at the SSO callback: idling on the tile screen booted with a dead ticket | launcher.html bootGame mints /auth/ticket right before navigating | done (unit) | authticket.test.ts |
| 124 | One session per ACCOUNT, not character: phone on char A and laptop on char B kicked each other | players.ts activeForChar, connection.ts | done (unit) | session.test.ts (resume-over-live still resolves by account: SessionIndex has no charId) |
| 129 | Puppet swing is always a chop, played on use RELEASE at 1.2x: the wind-up starts after the blow landed; swings shown in spell stance | puppet.lua showSwing: wind-up on press (start→min attack, held), blow + Weapon Swish on release (max attack→follow stop); spellcast self start→stop in the spell stance | fixed | lua-tests (edge check); needs a bake |
| 130 | Co-op melee is silent: the cancelled hit chain played the Health Damage / miss sound and the blood; the owner being hit gets no sound | puppet.lua onHitIntercept replays sound + I.Combat.spawnBloodEffect before `return false` (never Actor._onHit); player.lua MP_SelfStats plays Health Damage on an hp drop | fixed | red overlay left (C++ binding) |
| 131 | A friend casting shows nothing: CombatCast never sent, MP_CastFx dead | player.lua use edge in spell stance → mpCombatCast → combat.onCast → CombatCast{spellId,casterId,kind}; MP_CastFx plays the school's castSound + the effect's castStatic vfx | fixed | lua-tests (wire shape) |
| 132 | Levitate/slowfall/waterwalking not in VISIBLE_EFFECT: the observer's puppet pogos under a flying friend | global.lua VISIBLE_EFFECT | fixed | lua-tests |
| 133 | Puppet never looks up/down: peer streamed pitch=0, puppet ignored pitchChange | global.lua avatarStreamTick pitch; puppet.lua controls.pitchChange | fixed | needs a bake |
| 134 | Puppet runs at the template's Speed: a fast player's puppet lags 128 units then teleports | playerstate.ts stamps `speed` (doc attributes) on PlayerStatsDynamic; global.lua → MP_Stats.speed; puppet.lua sets attributes.speed.base | fixed | avatarstats.test.ts green; needs a bake |
| 135 | A friend's doors swing silently (World::activateDoor has no sound) | objects.lua MP_DoorState plays the record's openSound/closeSound | fixed | needs a bake |
| 136 | Degraded mode: poseFlags bit 3 = inAir on the sender, read as `use` by the puppet: every landing played a phantom chop | player.lua poseFlags bit 3 = use (inAir dropped: nothing read it); a flags change alone now triggers a pose send | fixed | lua-tests |
| 140 | Paying the fine never calmed the peer's guards/witnesses: recordCrimeId ran only on the owner's client; the peer only zeroed the registry bounty | luabindings.cpp `mp.recordCrimePaid()` (World::getPlayer().recordCrimeId()); quests.lua MP_CrimeUpdate peer branch calls it when a source's level drops (per byId / shared, so a joiner restoring 0 forgives nothing) | fixed | needs a tier2 build; no unit |
| 141 | A guest arrived with their HOME faction ranks and a rank earned here was gone on relog (shared.factions never sent at join) | quests.ts `guestFactions()`: host doc factions overlaid with shared.factions when factions are shared and the world has another owner; connection.ts welcome record carries it | fixed (unit) | standing.test.ts (host rank reaches the guest, home rank does not, guest's own promotion survives a relog) |
| 142 | Persuasion never reached a player entering the cell later (ActorDisposition send-on-change, no catch-up) | actors.lua `catchUpCell()` clears dispVal/equipFp for the held cell; global.lua MP_PlayerCellChange calls it, so the next tick re-sends | fixed | needs a bake; lua-tests load |
| 143 | Another player's body was a crime witness (canReportCrime excluded only getPlayer) | mechanicsmanagerimp.cpp canReportCrime: same avatar / content-less puppet predicate as canCommitCrimeAgainst | fixed | needs a bake + tier2 |
| 147 | Personal-crime mode seeded a guest with the HOST's bounty | quests.ts seedBounty reads the player's own doc when crime is not shared | fixed (unit) | standing.test.ts |
| 149 | Nothing ever respawned and corpses never cleared: the peer anchored with respawn=false (CellStore::respawn never ran), a death had no date and was replayed on every cell entry forever, each peer restart rolled one more live creature on top of the old corpse | worldstate.ts (deaths {deathNo, atH}, forgotten after fCorpseRespawnDelay 72 h, placed levelled spawn dropped with it), scene.cpp (peer anchors load with respawn=true) | fixed | actor.test.ts (death at 10 h still sent, at 80 h gone with its spawn; engine side needs a bake: kill a smuggler, rest 3 days, re-enter) |
| 150 | dayspassed was boot-relative (offset learned from what this engine booted with): every relog reset it, no two engines agreed; DaysPassed-stamped timers (vampire incubation, lycanthropy, mod timers) misfired. Refines #71 | world.lua daysPassedOf: days since 16 Last Seed 3E 427 (= day 1) in the absHours basis, clamped >= 1 | fixed | lua-tests (7 checks); needs a bake |
| 151 | PCVampire/PCWerewolf/counters shadowed to the CAMPAIGN doc: a guest turning vampire made the host a vampire on next login, and the peer's dummy a werewolf | quests.ts CHARACTER_GLOBALS (pcvampire, vampclan, vampkills, pcwerewolf, pcknownwerewolf, pcknownreset: the GLOB names in Morrowind.esm/Bloodmoon.esm): own char doc only, relayed to nobody, filtered out of the campaign seed and overlaid from the own doc on GlobalVarSync | done (unit) | questpeer.test.ts |
| 153 | Relog as a werewolf left negative attribute modifiers at dawn (setWerewolf(true) ran before the base writes in phase 2) | identity.lua: the form restore moved to the end of applyPhase2, after the base attribute/skill writes and before the diff-cache seed | fixed | needs a bake |
| 156 | Sleep ambush spawned a creature only the sleeper sees (spawnRandomCreature not gated on localSpawnsEnabled) | waitdialog.cpp onWaitingInterrupted gated on MWMP::localSpawnsEnabled(), as transformationextensions.cpp / creaturelevlist.cpp | fixed | needs a bake (the peer does not roll the ambush for an avatar: no ambush at all under a holder) |
| 158 | No "contracted disease" message when the avatar caught it | global.lua MP_SelfSpells: a Disease/Blight record added to the player goes through notice() (sMagicContractDisease with the record name); global context has no openmw.ui | fixed | needs a bake |
| 144 | Assault/murder of a guildmate on the peer never expelled (the owner's engine never ran commitCrime, which is where vanilla expels) | MWMP::Crime.mFaction (victim's primary faction, or the trespass faction) filled in commitCrime, out through mp.takeCrimes → PlayerCrime.faction → connection.ts relay → MP_PlayerCrime expels when the player holds a rank | fixed | avatarstate.test.ts (relay), lua-tests; needs a bake + tier2 |
| 152 | Werewolf avatar fought with human numbers (applyWerewolfStats ran for actor==player only); a transformation rebuilt the puppet mid-fight | mechanicsmanagerimp.cpp setWerewolf: an avatar gets the stat swap (no GUI/witness block); global.lua MP_PlayerAppearance: isWerewolf left out of the rebuild compare, types.NPC.setWerewolf on the standing body instead | fixed | lua-tests; needs a bake + tier2 |
| 154 | A vampire friend looked human on your screen (the head swap reads the Vampirism effect; the spellbook reaches the avatar only, never a puppet) | identity.lua snapAppearance vampireSpell (first own spell with a vampirism effect); playerstate.ts stores/relays it; spawnPuppet adds that one spell, an appearance change swaps it in place | fixed | lua-tests; needs a bake |
| 155 | Mark position lost on relog/world hop (NpcStats only; no binding, not in the doc): Recall did nothing | luabindings.cpp mp.getMark/setMark over NpcStats mMarkedCell/mMarkedPosition (serialized cell id); identity.lua sends PlayerMark on a change and restores in applyPhase2; playerstate.ts handleMark → doc.mark. One mark per character: a cell the current world lacks never resolves (vanilla's "no mark") | fixed | declaredstate.test.ts; needs a bake |
| 159 | Peer follow-teleport ignored vanilla follower rules (in combat, stayoutside, >800 units): a stay-outside guar rode the strider indoors | global.lua canFollowThroughDoor (actionteleport.cpp getFollowers rules); actors.inCombat tracks the follower's own mpActorCombat report since global context cannot read an AI stack; stayoutside read from the mirrored local (#107) | fixed | needs a bake |
| 182 | The native peer never carried companion.lua (missing from files/data/CMakeLists.txt; the browser bake copies the dir whole, the peer image is the CMake tree): as holder the peer never reported follow/combat/travel, handoff bars never applied. Hidden locally by the harness overwriting the image; under Jenkins the sync fails (EACCES) and the baked copy runs | CMakeLists.txt | fixed | lua-tests: every mp.omwscripts path is listed (#91 peer log: Resource scripts/mp/companion.lua not found) |
| 185 | Harness cmd timeout (20 s) fired under a streamed-load frame (s149 red in #89 and #91, green #90) and printed nothing of the client | mp-harness.mjs handle.cmd | fixed | 60 s deadline + console tail on timeout |
| 176 | BSA packing hard-failed above the u32 data limit (Tamriel_Data HD) and reported "folder may be full"; install rolled back | bsa-pack.ts writeBsa(out, files, maxData) splits the sorted list greedily into `<slug>-1.bsa`, `<slug>-2.bsa`… each under the cap, one archive keeps the plain name, returns every path; mod-install.ts pushes them all into files/archives and names the real error (ENOSPC alone says "full") | done (unit) | bsa-pack.test.ts (cap injected: 5 files at cap 10 → 3 archives, every file once, each under the cap) |
| 102 | Chat history lost on every world switch (page reboot); idle feed lines never fade; whisper dropdown listed friends in other worlds | index.html: `omwmp:chatlog` in sessionStorage next to `omwmp:bootfrag` (prior lines + the engine's rolling log, cap 200, cleared on exit to the launcher); `.feed.idle` fades the corner 20 s after the last line unless the panel is open; the whisper list is friends with a local playerId | fixed | needs a run-through |
| 103 | "join" offered for every online friend incl. one in your world / solo; "Joining X..." status never cleared | presence rows carry the world's mode (socialstore `presence.mode`, written on join + heartbeat from server.ts worldMode); FriendView.joinable = online, in another world, mode party; the page offers join on that alone; watchJoin clears on the joinFriendResult mirror or 30 s | done (unit) | socialux.test.ts "a friend row is joinable only from another world that is open to friends" |
| 104 | Lua and JS narrated social failures with two different sentences | index.html FAILURE table carries social.lua's wording (SOCIAL_FAIL + MP_JoinFriend codes), fallback `op: code` as Lua | fixed | — |
| 105 | No "invite" button in the page's social panel | index.html friend row `invite` → `social:InviteSend:<acct>` (the O-key hub's op); the ack shows "Invitation sent." or the refusal | fixed | needs a run-through |
| 128 | Dashboard-disabled expansion (Tribunal) refused every browser client (page load order included all masters present) | mwdata-routes.ts: mwdata-mods.json carries `disabled` (base entries switched off) and is served even with no mods when something is off; index.html buildLoadOrder skips those masters (archives stay registered, as on the peer) | done (unit) | mwdata-serve.test.ts "names the disabled official masters, even with no mods installed" |
| 169 | TR NIGHT BLOCKER: gateway worlds read modlist.json / config.dashboard.toml from their own EMPTY data dir: the peer ran vanilla, became the canonical content list, every player refused BAD_CONTENT; every dashboard setting ignored by worlds | server.ts readModDoc/orderedContent(sharedDir); config.ts loadConfig merges the first valid dashboardLayers(sharedDir) after the shared config.toml when sharedDir !== dataDir | fixed (unit) | mods.test.ts "a gateway world sees the shared modlist and the shared dashboard layer" |
| 170 | TR NIGHT BLOCKER: the MP image had no 7z; TR/Tamriel_Data ship as .7z | Dockerfile.simpeer tier2 apt p7zip-full (ubuntu base; sevenzip.ts spawns `7z`) | fixed | needs a tier2 build |
| 171 | TR NIGHT BLOCKER: front door `/` booted #mp=wss://host/ws on a gateway (only /w/<id> accepted → 502 forever); Caddy redirected /launcher.html to / | play.js hands the ticket to /launcher.html#mpticket in multiplayer mode; caddy-config.ts `multiplayer` keeps the launcher served (OMW_ENABLE_LAUNCHER=1 still a manual override); wired from server.ts, gateway/main.ts, routes.ts | fixed (unit) | play-front-door.test.ts, caddy-config.test.ts |
| 172 | "Internal / LAN party" plain HTTP is not a secure context: "Browser not supported" for everyone but the server box | app.js wizard hosting copy says so and names the outs (TLS domain, tunnel, chrome://flags insecure-origin exception per player). Copy only; caddy-config.ts:229 untouched | fixed (copy) | wizard text |
| 174 | Gateway HTTP server kept Node's 5-min requestTimeout: a 2.7 GB upload slower than ~9 MB/s died as "connection dropped" | directory.ts server.requestTimeout = UPLOAD_REQUEST_TIMEOUT_MS (exported from http.ts, 6 h); headersTimeout left default as in http.ts | fixed | — |
| 177 | Peer start timeout 120 s vs a TR-loaded cold peer: SIGKILL and an unsimulated cell | config.default.toml startTimeoutMs = 300000. Default only; peerCostMb 487 still a vanilla number (ops note) | fixed (default) | — |
| 178 | Re-installing a mod served the old .esm from the browser cache (/mwdata/* immutable, same slug URL) | mwdata-routes.ts: /mwdata/mods/* is `no-cache` + ETag (size-mtime), 304 on If-None-Match, stale If-Range falls back to 200; retail stays immutable; ranges unchanged | fixed (unit) | mwdata-serve.test.ts "a mod file revalidates by ETag" |
| 179 | MAX_ABS_COORD 512000 = 62.5 cells: TR at y≈-60 had 2.5 cells of headroom; past it invisible/frozen with no message | movement.ts MAX_ABS_COORD = 4,000,000 (float32 ULP 0.25 at 4M; no sibling constant in mwmp C++ or Lua, nothing derives a buffer size from it) | fixed (unit) | movement.test.ts out-of-bounds at 5M |
| 160 | A service window open longer than 15 s stopped syncing (live/barter watches shared the chest's 15 s expiry): stock dupes, the gold delta never sent, training paid into a per-client purse | objects.lua openContainerNow: `until_ = live and math.huge or (now + CONTAINER_WATCH_SECONDS)`; onBarterClose and the tick's invalid-object branch still end a live watch | fixed | lua-tests (source checks); s161 needs a bake |
| 161 | Player-made records (potions, enchanted items) never mapped toNet/toLocal on the container/barter wire: a friend saw your potion as their own `Generated:` record, restock re-added the bogus id forever | objects.lua: countsToItems and sendContainerOp go out through worldmp.toNet (pendingOps keep the net id so the Update echo still consumes); setContainerContents, applyContainerDelta and the refused-take undo come back through worldmp.toLocal -- the drop path's own rule | fixed | lua-tests (source checks); needs a bake |
| 162 | The dialogue lock was released the moment a service window opened (Dialogue→Barter fired mpDialogueClosed): bribe/admire after the trade never travelled; two players traded at once | player.lua UiModeChanged: `talking(m)` = Dialogue or GOLD_SERVICE_MODES[m]; mpDialogueClosed only on `talking(old) and not talking(new)` | fixed | lua-tests; s161 (B's dlg: refused while A trades) needs a bake |
| 163 | Peer item-state report was wholesale: a cast-when-used charge spend (or a repair) was refunded within 2 s | playerstate.ts handleAvatarItemStatesBatch merges per field against the doc: charge/condition = min(peer, doc), soul the peer's; the merged bucket is what the owner hears | done (unit) | avataritemstates.test.ts (doc 60 after a spend, peer 100 → 60 kept; peer 40 → 40) |
| 164 | Losing a barter race costs the item AND the gold (three independent ops, refusal does not know the price); unreachable once 162 holds the lock | worldstate.ts, objects.lua:908 | moot (by #162) | — |
| 165 | 24 h restock refills one-off stock (unique Creeper/Jobasha items come back daily), not just restocking entries | worldstate.ts containerOpen restock: GOLD only; the stock refill is removed (the server cannot tell a restocking entry -- a negative count on the record -- from a one-off, so a whole-list refill duped every unique). Bought stock stays bought; a client binding reporting which entries restock is the future path for (b) | fixed (unit) | economy.test.ts 24-hour restock (purse back, dagger stays bought) |
| 166 | MAX_GOLD_DELTA 1,000,000 lets any client zero/fill a merchant purse; no moderation note on the gold branch | worldstate.ts gold branch: a delta beyond the merchant's goldOrigin is noted (`merchant_gold_delta` anomaly + warn log); the cap is unchanged | done (unit) | economy.test.ts (selling the whole purse: no flag; 10× the purse: flagged) |
| 167 | Custom record bodies unvalidated (chopMaxDamage=9999, Fortify Health 10000 for 10^6 s replayed to the peer, the avatar fights with it) | m7.ts recordWithinCaps: effect magnitude ≤ 400, duration ≤ 5760 s, weapon damage ≤ 200 (4× the spellmaker / retail maxima); refused like a malformed body, counted in `omwmp_records_refused_total` | done (unit) | worldtime.test.ts (godsword / immortality refused, fine blade / big heal acked) |
| 168 | Harness gap: no hook traded inside a real barter window — blocked proving 160/162/164 | player.lua `barter:open:<recordId>`, `barter:sell:<id>` / `barter:buy:<id>` → global.lua mpTestBarter (one item across, purse by its value); s161-barter-long-window.mjs | fixed | s161 needs a bake |
| 173 | The dashboard "update engine" fetches the v1.3.0 GitHub release; a branch peer against a v1.3.0 client (wire skew: speed, CombatCast, pitch, dayspassed) | update-engine.ts:32 — operator copies the Jenkins bake into play/ |
| 175 | Cloudflare in front of the dashboard: 100 MB body cap refuses Morrowind.bsa and TR archives; 100 s origin timeout 524s the commit — uploads over the LAN only | ops note; fsstorage.ts:200 | ops note | TR-NIGHT-RUNBOOK.md §3 Data: archives go up over the LAN, never through Cloudflare (100 MB body cap, 100 s origin timeout) |
| 180 | No self-serve sign-up (/auth/password signs in existing accounts only): the owner creates the friend in the dashboard; both need a public username on first launcher login | ops note; auth/routes.ts:322 | ops note | TR-NIGHT-RUNBOOK.md §4 Accounts: the owner creates the friend in the dashboard; both pick a public username on first launcher login |
| 181 | Internal hosting mints locker URLs on http://127.0.0.1:8080 (no domain): server-side saves/locker uploads from another machine fail; publicBase hidden from the dashboard | fsstorage.ts:166; api-settings.ts DERIVED_FIELDS |
| 183 | s114 red in #91: A's client went silent ~5 s after the second snapto (simpeer.avatar_stats_gated lastInputAgoMs=5668), A's copy of the companion never moved while B saw it; no Lua/JS error. Rerun alone with moveRx/actorBatchesIn/puppetRx/uiMode + A's full console | s114, player.lua input sender; defensive: mp-harness.mjs launches Chrome with --disable-renderer-backgrounding --disable-background-timer-throttling so a hidden tab cannot stop driving the input tier |
| 184 | Harness under Jenkins cannot sync mp scripts into the peer (EACCES on /usr/local/share/openmw/resources/vfs/scripts/mp) and only WARNS: the peer runs the image's baked Lua — honest only while the image is built from the branch under test | mp-harness.mjs:283 syncPeerScripts; run-harness.sh | fixed | run-harness.sh bind-mounts the checkout's scripts/mp and mp.omwscripts over the image path (:ro); syncPeerScripts copies when it can, then hashes both trees (dir + list) and THROWS on any difference — a peer running Lua other than the repo's is a FAIL, not a warning. Needs a Jenkins run to see the mount live (unbaked) |
| 190 | Wire version handshake (proto=2) not maintained: 14 PROTOCOL.md commits since the last bump; a mismatched client connects and misbehaves silently (real shape of #173) | connection.ts:1200 bump proto per additive change or minClientBuild |
| 195 | First-run setup trusts XFF from any private peer: behind an operator's own proxy without XFF every internet client looks private during the setup window | routes.ts:338; document beside the LAN copy | ops note | TR-NIGHT-RUNBOOK.md §2 Prod config: the first-run setup window trusts XFF from private peers, so run setup from the LAN before the proxy is public |
| 203 | GAP: scripted movers (mod elevators, SetPos/Move on activators) are per-engine and unsynced; no client emits ObjectMove | objects.lua:819 holder-authoritative relay when a mod needs it | documented | MP-COVERAGE-MAP.md §7 "Lua mods: unsupported patterns" |
| 204 | Design ceiling: a WebSocket stall > 0.35 s collapses buffered inputs to the last one on the peer; the owner is dragged back by the stall length | avatar.lua:231 input queueing + #197 |
| 206 | Actor LOD is measured from the peer's parked dummy, not the streamed cell: the second friend in another exterior gets NPCs in their OWN cell at 1 Hz (park/stutter/teleport) | worldstate.ts handleActorMoveBatch: stride from the recipient's pose to the streamed cell's centre; the recipient's own cell is never strided | done (unit) | holderhears.test.ts (dummy parked a cell away, Bob in the streamed cell: 10 sent, 10 received) |
| 207 | PlayerInput frames (0x0102) draw from msgsPerSec: a >2 s stall then the burst empties the bucket and the player is kicked with terminal RATE | connection.ts: own `inputBucket` (60/s, burst 120), shed + counted (`input_shed`) instead of RATE | done (unit) | ratelimit.test.ts (130-frame burst: alive, >= 5 shed; the JSON flood still RATEs) |
| 208 | mLastMoveSeqIn survives an in-process reconnect while the new world process's broadcast counter restarts at 0: after a world restart every pose is dropped as stale for hours (password/self-hosted worlds, harness) | netmanager.cpp connect() resets mLastMoveSeqIn with mSeq | fixed | needs a bake; no unit |
| 209 | The actor pose relay is enqueued on the world mutation chain behind DB-awaiting ops: every cold cell read stalls the 20 Hz NPC stream for everyone | worldstate.ts handleActorMoveBatch is synchronous (holder/epoch reads are in-memory); noteActorFrame kept | done (unit) | actor/holderhears/worldstate suites green unchanged |
| 210 | Background tab: the 4 MB inbound cap drops whatever arrives next (events included) with no resync; a discarded tab that stops reading is kicked with terminal RATE "outbound buffer overflow" although a resume ticket was parked | netmanager.cpp onMessage sheds only 0x0101/0x0200/0x0103 past the cap (events queue); global.lua polls `mp.status().droppedInbound` once a second and sends ResyncRequest for the own cell when it moves; connection.ts overflow disconnects with `BACKLOG` (ticket still parked), net.lua TRANSIENT_DISCONNECT includes it | done (unit) | backpressure.test.ts (hard ceiling -> BACKLOG); C++/Lua side needs a bake |
| 197 | Reconciliation has no latency compensation: the peer's pose (as of lastInputSeq, one RTT old) is compared with the client's CURRENT position, so on a real link every stop/jump/doorway rubber-bands by RTT x speed (invisible on the LAN harness) | player.lua:299-330 ring of {seq,pos} in inputTick; divergence = e.pos - posAt(e.lastInputSeq); harness needs a debugStateDelayMs knob; player.lua posRing/posAt (64 x {seq,x,y,z}), reconcile against posAt(lastInputSeq) | fixed | lua-tests; s162 (150 ms link) needs a bake |
| 198 | One in ~three jumps never reaches the avatar: 30 Hz input vs the peer's 20 Hz tick, the second event of a tick overwrites the first (jump bit lost); observers latch it, so friends see a jump the avatar never made | avatar.lua:227-233 latch jump/use bits until consumed; avatar.lua jumpLatch/useLatch, consumed in onUpdate | fixed | lua-tests; s145 needs a bake |
| 199 | Doors relay ~1-1.5 s late (read waits for Idle): the owner walks through while the avatar is stopped by the closed door (rubber-band/SELF SNAP); rotateDoor rolls back while the avatar is in the arc and stacks AiAvoidDoor the avatar never runs (deadlock while pushing); a close in the window is reversed by the stale open | objects.lua:420, 1168-1188 send the intended state at activation; objects.lua onActivate sends the Opening/Closing intent, settle read resends only a change, MP_DoorState keeps our pending read for 1.5 s | fixed | lua-tests |
| 200 | Every exterior cell-border crossing follow-teleports the mover's puppet and avatar (pop for friends; fall height zeroed on the peer = no fall damage across a border, second trigger for #74) | global.lua:2356 skip the teleport when both keys are exterior and the body is within SNAP_DIST; global.lua MP_PlayerCellChange: prevCell captured; exterior->exterior within 256 units skips tryTeleport | fixed | lua-tests |
| 201 | Movement effects (Levitate/Slowfall/Water Walking/Fortify Acrobatics) reach/leave the avatar up to 0.5 s + RTT late (INTERVALS.active 0.5): the owner hangs after expiry, dips on cast | identity.lua:24 active diff 0.1 s or per frame for the movement subset; identity.lua INTERVALS.active 0.1 | fixed | lua-tests |
| 202 | Observer puppets miss ~25% of jump edges (20 Hz avatar stream sampled by the server's 66 ms tick); sub-66 ms use clicks lost too | connection.ts:930 OR the edge bits into p.pose.flags until broadcast once; movement.ts acceptPeerPose ORs bits 4|8 until poseSentVersion catches up (tick end) | fixed | movement.test.ts |
| 205 | Avatar input hold 0.35 s: a TCP retransmit stall (300 ms RTO, 1-3 s Wi-Fi roam) stops the avatar while the owner keeps running; the burst collapses to the newest input and the owner is snapped back by v x stall (sibling of #197/#204) | avatar.lua:34 INPUT_HOLD_S 1.0; avatar.lua INPUT_HOLD_S 1.0 | fixed | lua-tests |
| 211 | Puppets on a lossy link: near-tier snap 128 crossed by any stall ≥ 350 ms (teleport at the 1 s cooldown); run flag dropped/re-raised per burst (anim popping) | puppet.lua:47 snap 256; hold the run flag ~0.3 s; puppet.lua SNAP_BY_TIER[0]=256, run flag held 0.3 s (runUntil) | fixed | lua-tests |
| 212 | Harness has no latency/loss knob (raw CDP, no emulateNetworkConditions): every feel bug above is invisible on the LAN | connection.ts OMWMP_NET_DELAY_MS FIFO on send+receive (+ OMWMP_NET_STALL); net/netdelay.ts FIFO per connection, both directions, OMWMP_NET_DELAY_MS + OMWMP_NET_STALL=<ms>/<everyS>; scenarios pass it via serverEnv | fixed | netdelay.test.ts; s162 |
| 186 | Backup does nothing useful on a gateway: SIGUSR1 has no gateway handler (README cron flushes nothing, Node opens the inspector), tar copies db/wal/shm at different instants, README names the wrong container | gateway/main.ts SIGUSR1 flushes accounts and relays to every world (worlds.signalAll); sqlite.ts checkpoint() = `wal_checkpoint(TRUNCATE)` after players/cells/accounts/records/bans flush; ops.ts export checkpoints every *.db first and excludes gamedata/; README names `openmw-web` and describes the relay | done (unit) | worlds.test.ts (SIGUSR1 reaches both fakes, not a stop) |
| 187 | Orphaned sim peers after a world is SIGKILLed are never reaped (peers spawned detached:false, no pid file; a peer that once joined redials forever); no init in compose | worlds.ts spawns the WORLD detached (own process group; its peers stay attached, so they share it) and the SIGKILL escalation is `process.kill(-pid)` (child.kill fallback on Windows / a non-leader); compose `init: true`; net.lua a SYSTEM peer that had joined quits (`core.quit()`) after 30 redials | fixed | needs a tier2 rebuild for the Lua half |
| 188 | Dashboard delete-account in gateway mode: no kick, no discardForCharacter, erase.ts never writes the `erased` tombstone so the world's next flush resurrects the doc | gateway/admin.ts deleteAccount: kick relayed to every running game, discardForCharacter per character, then flush + erase; erase.ts erasePlayerDocs writes the `erased` tombstone per key (try/catch for pre-002); stale "runs OFFLINE" comment rewritten | done (unit) | erasure-completeness.test.ts (a flush from a store that still caches the doc does not resurrect it) |
| 189 | Cell docs have no cap on moved/locks/doors: ~11k ObjectMoves (3 min at 60 msg/s) makes WorldCellState exceed the LSER node ceiling and every entrant disconnects BAD_PROTO; memberVars capped on send only | cellstore.ts MAX_KEYS_PER_CELL 2000 + cellMapFull(): a NEW key past the cap is refused and logged `world.cell_map_full`, an existing key still updates; used by move/lock/door/enabled (worldstate.ts) and storeMemberVar (quests.ts) | done (unit) | worldstate.test.ts (seeded at the cap: new ref refused, existing ref moves) |
| 191 | Gateway shutdown exits 3 s after stopAll while the world stop grace is 20 s (PID 1 exit kills children mid-drain) | gateway/main.ts shutdown awaits worlds.allExited() (bounded by STOP_GRACE_MS + 5 s, the supervisor's own SIGKILL) before process.exit; compose `stop_grace_period: 30s` | done (unit) | worlds.test.ts (allExited resolves only after the last child exits) |
| 192 | Persistence failures (players.flush_failed, cell_flush_failed, world.crashed, cells_unsimulated, server.crash) not in the default notification event list | config.default.toml events += players.flush_failed, world.cell_flush_failed, world.crashed, world.at_cap, simpeer.cells_unsimulated, server.crash (notify.ts matches exact names; all six are logged under exactly those) | fixed | — |
| 193 | erase.ts opens SQLite without busy_timeout: a concurrent world write turns the dashboard delete into a 500 | erase.ts withDb: `PRAGMA busy_timeout = 5000` right after open | fixed | — |
| 194 | Ban rows attribute dashboard bans to "dashboard", not the operator | routes.ts AdminDeps.action gains `by` (ctx.accountName); server.ts banAccount(target, by); unban logs `moderation.unban` with the operator | fixed | — |
| 196 | Slow growth: erased never pruned; quests.ts peerMemberAt grows per cell|ref|name; Export streams gamedata/ (multi-GB); the 1 Hz "avatar submerged" info line ships in production Lua with a sync append per line | PARTIAL: ops.ts export excludes gamedata/ (with 186); global.lua avatarDrownProbe once per 10 s (DROWN_PROBE_EVERY). Then: PlayerStore boot deletes erased tombstones older than 90 days (ERASED_TTL_MS); quests.ts prunes peerMemberAt entries older than 10 min (PEER_MEMBER_TTL_MS) every 4096 peer writes | fixed | existing suites (behaviour unchanged inside the windows) |
| 217 | Script state the peer advances is never relayed/persisted (MemberVarUpdate only 6 s after a LOCAL activation): the Heart's countHits invisible to clients and lost on a peer restart mid-fight; Dagoth Ur's Say lines and PlayGroup play on the peer only | quests.lua:396 long-lived memberWatch on the peer; OpSay on system → ActorSay relay | fixed | quests.lua armHeldWatches: the peer arms an open-ended memberWatch (1 s poll, cellKey on the wire) on every scripted object with locals in every held cell; quests.ts memberVar takes the peer's cellKey. OpSay (soundextensions.cpp) records a 'say' ScriptNote on the peer; actors.noteSay -> ActorSay (holder relay set) -> MP_ActorSay core.sound.say on puppets. PlayGroup not carried; needs a bake + tier2 |
| 220 | OnDeath "player->" afflictions apply on every client in the cell: the GUEST gets Corprus from Dagoth Gares and can never be cured at home (no A2_2 journal) — needs a decision | identity.lua strip campaign-script spells on WorldClosed? |
| 221 | GetAiPackageDone pollers stall on clients (puppet AiSequence never executes): Fargoth's lookout, MehraMiloScript, HentusTravel state machines never advance; StartCombat Player from a peer-run branch targets the dummy (generalises #78/#145) | actors.cpp:1712 | fixed | aisequence.cpp records an 'aidone' ScriptNote where mDone is set; actors.noteAiDone -> ActorAI {done} from the holder; MP_ActorAI writes it onto the puppet with mp.setAiPackageDone (AiSequence::setPackageDone, sticky until AI resumes). StartCombat-from-peer-branch half open; needs a bake + tier2 |
| 222 | NPC-local writes from a dialogue result are lost after 6 s of talking (memberWatch never re-armed on a topic click) and MemberVarUpdate relays with the old cellsVisible rule, so the holder is deaf to it outside the dummy's cell | quests.lua:398 diff locals at releaseLock; quests.ts:151 relayCell uses hears() | done (unit) | quests.test.ts (peer parked a cell away hears the MemberVarUpdate); lua-tests source check for the conversation-long watch; needs a bake |
| 223 | Player reputation is not on the wire nor in the doc: resets to 0 on every relog (PcReputation topics vanish, derived disposition drops) | identity.lua snapProgression reputation; playerstate.ts clamp 0..255 | done (unit) | declaredstate.test.ts (rides on PlayerLevel, 300 refused, doc keeps 12); Lua side needs a bake |
| 224 | A human's dialogue-set quest global never reaches the other human live (only the peer + doc): the guest's Global filters and local scripts are stale until relog although the journal advanced (s62 pins the old per-character model) | quests.ts:394 relayAll for humans; flip s62 | done (unit) | questpeer.test.ts (the other human hears it), quests.test.ts flipped; s62 flipped, needs a bake |
| 225 | SetJournalIndex to a LOWER stage (quest restart) is dropped, not persisted, and reverted on next login; later advances below the old max are silently dropped too | quests.ts:239 record() before the regress check for owner/peer origin | done (unit) | questpeer.test.ts (owner 50→10→20, peer →5, guest's 1 blocked, doc follows) |
| 226 | ForceGreeting dialogues take no lock: their AITravel/StartCombat/ModDisposition results are dropped ("without the conversation") — also the #44 arrest path | player.lua:1325 UiModeChanged → mpDialogueForced → requestLock | fixed | lua-tests source checks; a denied forced greeting closes the window; needs a bake |
| 227 | NPC combat is silent on clients: no attack shout, hit grunt, flee/thief bark (every say() is on peer-run paths) | mp.say binding; MP_ActorAI combat + onHitIntercept | fixed | mp.say(obj, topic) binding (luabindings.cpp, queued DialogueManager::say); actors.lua MP_ActorAI combat=true rolls iVoiceAttackOdds and barks 'attack' on the puppet. Dying words ('hit') already play locally: MP_Kill zeroes health and the puppet's own Actors loop runs kill(). Flee has no holder signal yet; needs a bake |
| 229 | Persuasion/result-script Fight/Flee/Alarm changes stay on the talking client (no AI-setting field on ActorStats) | actors.lua | fixed | Fight/Flee/Alarm base ride ActorDisposition as `ai` from both senders: the holder's beat fingerprints them beside disposition (actors.lua aiSettings), the talking client snapshots them at lock grant and reports a change at release (quests.lua lockAi); worldstate.ts bounds each to [0,100] on the dialogue-holder path; MP_ActorDisposition writes types.Actor.stats.ai[k](obj).base. lua-tests source checks; needs a bake |
| 279 | GET /worlds?account=<key> is unauthenticated: private world ids, mode and player count for any guessed login name | directory.ts:276 | done (unit) | directory.ts GET /worlds?account= answers 401 unless the Bearer resolves to that account or is the server credential; worldbrowser.ts list/ownerWorld send the server token; s47/s48 and directory.test.ts pass it (a guessed name is asserted 401) |
| 283 | Copy/gate mismatches: handle hint vs server rule; launcher says Chrome/Edge/Brave while index says Firefox 134+ is fine; touch laptops < 820 px tall get "needs a desktop browser"; no_such_character followed by "server may be unreachable" | launcher.html:404, 873, 1415 | fixed | launcher.html: the gate names Chrome/Edge 133+, Brave or Firefox 134+ (the FSA copy stays Chromium-only, correctly); the username hint says 3-20 letters and numbers, no spaces (accounts.ts USERNAME_RE); a touch device is a phone only when its LONG side is under 1100 too (a 1366x768 touch laptop passes); no_such_character is a PlainError so the 'may be unreachable' guess is not appended |
| 284 | Two-device: presence PK per account (two characters flap), localStorage resume shared across tabs, deleting a character being played elsewhere stops+removes its world and the other device reconnects forever | socialstore.ts:176, index.html:1445, frontdoor.ts:148 | partial | resume token moved to sessionStorage (per tab; index.html clears the old localStorage key for one release); DELETE /auth/characters refuses a character whose own priv- world is up with players (frontdoor.ts isPlayed via worldsNow; a guest in someone else's world is not detected). The presence PK (per character) is left to the #334 work on presence |
| 293 | Scripted Resurrect on the peer never travels (no ActorDeath inverse) | actors.lua:281, worldstate.ts:792 | fixed | actors.lua revive edge (tracked.dead and not dead) sends ActorRevive; worldstate.ts deletes doc.actorDeaths[ref] and relays; MP_ActorRevive calls mp.resurrect(obj) (the binding now takes an optional actor: MechanicsManager::resurrect on it) and MP_Revive on the puppet. Needs a bake + peer rebuild |
| 294 | Followers cross a door only when the avatar's teleport succeeded synchronously; the 30 s retry carries the avatar alone | global.lua:2483, :203 | fixed | global.lua tryTeleport takes onLanded, kept on the retry entry and fired when the retried teleport lands; the door path hangs the follower carry on it, so a cold-load retry moves the companions too. lua-tests source check; needs a bake |
| 295 | Holder-lost Info goes to that cell's occupants only and detaches that cell only: neighbour-cell puppets stay AI-off/frozen through a peer outage | authority.ts:308, actors.lua:458 | fixed | client side: a holder-lost Info calls actors.reset() -- every puppet detaches and the whole mirror clears, since the only holder is the peer and losing it in one cell is losing it everywhere (neighbour-cell puppets attached off ActorMoveBatch were never in the mirror). The server already releases every held cell on disconnect (authorityReleaseEverything) and Infos each cell's occupants, which reaches every in-world player |
| 296 | NPC magic state never mirrors to puppets (Paralyze, Invisibility, Chameleon, Sanctuary, Levitate on NPCs — VISIBLE_EFFECT is player-only); knockdown/hit-recoil not carried | global.lua VISIBLE_EFFECT for actors; ActorMoveBatch flags | fixed | actors.lua: the holder diffs each NPC's temporary actives by instance on the equipment beat, keeping invisibility/chameleon/light/levitate/slowfall/waterwalking/paralyze/sanctuary effect indexes, and sends ActorEffects {add,remove} (wire record ids); worldstate.ts relays it; MP_ActorEffects adds/removes the same actives on the puppet (global-context activeSpells:add, stackable, resistances ignored). Knockdown/hit-recoil flags not carried; needs a bake |
| 297 | Corpse loot is canonical from the OPENING client's copy: potions the peer NPC drank / arrows it fired remain; per-engine levelled-item rolls differ; ActorEquip names records the puppet lacks (fight weapon ≠ loot weapon) | worldstate.ts container first-open from the holder for NPC corpses | fixed | the holder reports a corpse the moment it dies: actors.lua death edge -> objects.onCorpse -> the deferred ContainerOpen path (no delay: NPC inventories are populated at load), so the peer's copy is the first opener and canonical; a human's later open is served ContainerState from it. No server change: first-open already wins and the peer is exempt from the reach gate. Race: a human who activates the corpse inside the peer's next tick still wins; needs a bake |
| 312 | Blocked hits are silent and invisible for the owner; armor-type hit sounds play on the peer only; block state consumed per frame (needs a per-avatar counter drained with the stats report) | character.cpp:399 | fixed | combat.cpp blockMeleeAttack records the shield sound for an avatar blocker (puppets.cpp noteBlock/takeBlockFor, RefNum-keyed); mp.takeBlock(obj) drains it in avatarStatsTick as `blk` (a block always sends); playerstate.ts passes a whitelisted name on SelfStats; player.lua plays it. Block animation on the owner not carried; needs a peer rebuild + bake |
| 313 | Dead-swing side effects on local NPC copies (arrow-in-inventory roll, shield charge/fatigue drain, unequip) become canonical loot via #297 | combat.cpp:299, npc.cpp:696 | done — bake needed | combat.cpp projectileHit: the arrow-store roll skips a puppet victim (MWMP::isPuppet); shield charge/fatigue/unequip untouched. note: unchanged -- with #297 the corpse is now canonical from the HOLDER's copy, so a non-holder's dead-swing side effects no longer become loot; the holder's own dead-swing rolls still do |
| 314 | Werewolf kill count lands on the avatar's NpcStats, never in the doc | npc.cpp:826; identity.lua | open | note: left -- needs a werewolf kill hook in npc.cpp and a doc field; not trivial |
| 351 | Battle music never plays for NPC-vs-companion / NPC-vs-NPC fights (holder sends combat=false for a non-player foe); up to 1 s late | actors.lua:645 note only | open | note: left -- needs the holder to report NPC-vs-NPC combat (combat=<actor addr>), a wire shape change |
| 228 | An NPC killed by a friend mid-conversation leaves the window open; results execute on the corpse (world not paused in MP) | MP_ActorDeath for lockHeld → removeMode Dialogue + releaseLock | fixed | lua-tests source checks (actors.lua actorDeathFn → quests.onActorDeath → MP_CloseDialogue); needs a bake |
| 230 | TalkedToPc per engine and never persisted: every NPC greets a returning player as a stranger | needs a binding | fixed | mp.talkedTo/setTalkedTo over CreatureStats::hasTalkedToPlayer/talkedToPlayer; identity.lua talkedToTick reads nearby.actors at 1 Hz, sends PlayerTalkedTo {list}, re-flags NPCs in the set; playerstate.ts stores doc.talkedTo (ref keys, cap 2048); connection.ts sends SelfTalkedTo at ready -> global.lua -> identity.applyTalkedTo. Needs a bake |
| 231 | Any teleport of the avatar (SELF SNAP → PlayerCellChange → tryTeleport → teleportNotPlayer) calls stats.land(false): a snap mid-fall makes the rest of the fall free (exploit; the snap fires on every levitate release because the client's stream lags the climb) | objectbindings.cpp:122; global.lua:2361 skip land() for same-cell teleports or carry fall height | done (unit) — bake needed | global.lua MP_PlayerCellChange `walked` also covers a same-cell snap within SNAP_DIST (no tryTeleport, no land(false)); #74 is a dup; lua-tests source check |
| 232 | s147 (#100) still open after 3 sweeps: peer bar unmoved after a 1300-unit avatar fall; #93 carries a peer fall probe ("avatar #n landed from z=... fell N hp=...") to tell "never airborne" from "landed, not charged" (suspect: mJumpState never InAir for the dynamically-created avatar record on the headless peer — refreshJumpAnims resets when the jump group is missing). root-cause diag: run s147 and grep `avatar #.* landed from z=` | global.lua avatarFallProbe; character.cpp:2240/517-522 |
| 233 | Tools and lights never deplete while the peer rules: lockpick/probe/repair uses and torch burn are client-only, the raise-only rule refuses the drop and the avatar's untouched copy refills it every ≤10 s (free lockpicks, infinite torches) | identity.lua itemState() tags own=true for Lockpick/Probe/Repair/Light; playerstate.ts copies a tagged condition wholesale (client->doc) and keeps the doc's under the peer report | fixed | avataritemstates.test.ts; lua-tests; s163 (needs a bake) |
| 234 | Per-item state is positional over stateful instances but applied to whole stacks: one Soultrap kill on the peer fills all 3 gems of a stack (persisted); two daggers of one record swap/lose conditions per relog and per peer report (index is not an identity; #56 is the relog face) | every entry carries n (stateless stacks send {n}); server keeps empties; global.lua applyItemStates walks the stacks and split()s a bigger one before writing (state set on the piece so it does not restack); avatar apply, relog restore and MP_SelfItemStates (now applied in global.lua) share it; the peer reports one entry per stack | fixed | avataritemstates.test.ts; lua-tests (split + report round-trip) |
| 235 | A refused container take leaves its acquisition credit in pendingAcquired; a disconnect folds it into the doc (dup with the race winner) | worldstate.ts containerOp: a `gone` take refusal debits pendingAcquired by (itemId, n) | done (unit) | worldstate.test.ts |
| 236 | The drop debit trims doc.inventory but not doc.itemStates: a disconnect within 2 s of dropping one of two same-record items restores the survivor with the dropped one's state | server.ts drop debit pops the record's tail entries (whole ones while they fit, the last shrinks by n) in the same doc update | done (unit) | provenance.test.ts |
| 213 | `Startup` never runs for a returning character, a guest or the peer (--skip-menu bypass sets CharGenState -1): 103 quest refs (Dagoth Gares, the Incarnate ghosts, Wulf, Staada, sleepers...) are visible from day one for everyone but the host's first session; the peer simulates an enabled Dagoth Gares against a host whose copy is disabled | worldimp.cpp enable/disable choke → MWMP::recordScriptNote (puppets.hpp ScriptNote, drained by `mp.takeScriptNotes`); objects.lua onScriptNote sends ObjectEnabled under the OBJECT's cell key (own-cell poll kept as fallback); worldstate.ts docAndRef: no reach gate for ObjectEnabled | done (unit) | worldstate.test.ts (a human's far-cell disable persists and replays to a later entrant); C++/Lua side needs a bake |
| 214 | Client-triggered `PlaceAtPC <actor>` never spawns anywhere (the client skip at transformationextensions.cpp:625 assumes the peer runs the script; the peer never sees GetPCSleep/OnActivate/dialogue): no sleeper ambush, no Tashpi ghost, no Mark summons; quest-repair.ts is a one-rule hand table | transformationextensions.cpp OpPlaceAt skip site → ScriptNote{spawn, recordId, count, cellKey, pos}; objects.lua sends ObjectSpawnRequest{actor:true}; worldstate.ts spawn(): a human's actor request is forwarded as QuestSpawn{x,y,z,count,forId} to the holder (else back to the asker), 10/min per player; global.lua mpQuestSpawn places at the spot (the naming sweep nets it); quest-repair.ts DEFAULT_SPAWN_RULES emptied (mechanism kept) | done (unit) | worldstate.test.ts (forwarded to the holder; the 11th in a minute refused `rate`), questrepair.test.ts (peer vs no-peer routing), actor.test.ts; C++/Lua side needs a bake |
| 215 | The peer's dummy runs PCRaiseRank from OnDeath scripts (Bolvyn, Trebonius, Eno Hlaalu...) and its FactionUpdate writes the host's campaign doc (host demoted when factions are not shared) | quests.ts faction(): return if player.system | done (unit) | standing.test.ts (a system peer's FactionUpdate leaves the campaign doc untouched) |
| 216 | Player-gated scripts (GetDistance Player, CellChanged, OnActivate) run only on clients, on AI-off puppets: Dagoth Ur never PositionCells to the Heart on the peer; the Arena duel (Bolvyn) is a fully local fight per screen (not puppeted, damage erased, kill never tallied, alive on relog); EndGame's SetHealth 0 / blight cloud Disable / ChangeWeather never reach the peer; Mehra never moves for the guest | transformationextensions.cpp OpPositionCell/OpPosition on a client (MWMP::isClient) → ScriptNote{position, ref, source cellKey, dest cellName, pos}; actors.notePosition sends ActorAI{position={cell,x,y,z}} unless we hold the cell; worldstate.ts sends it to the holder only (human, near, finite); MP_ActorAI teleports. Arena duel (local fight) / EndGame SetHealth / ChangeWeather halves still open | done (unit) | worldstate.test.ts (position from a non-holder reaches the holder; not from afar); C++/Lua side needs a bake |
| 218 | `Enable` relayed live but never persisted and only for the own cell: every far-cell Enable from dialogue (dagoth gares, notes, stronghold stages) is client-local; becomes a permanent hole once #213 persists disables | worldstate.ts enabled(): `doc.enabled[key] = on` (a stored true is a scripted reveal); WorldCellState/CellSnapshotReplace carry `enabled` beside `disabled`; objects.lua applies both; the far-cell path is #213's | done (unit) | worldstate.test.ts (an Enable after a persisted disable persists and replays as `enabled`); holderhears ping-pong guard unchanged |
| 219 | Running global scripts are not persisted across a relog: Sleepers stops at the host's first relog (no dreams/ambush), VampireCheck never runs for a returning character (vampirism unreachable after one relog — refines #150), MoveMehra/All_Nerevarine lost | luabindings.cpp `mp.runningGlobalScripts()` / `mp.startGlobalScript(id)`; quests.lua diffs the running list every 5 s (chargen*/startup never travel) → GlobalScriptsUpdate{started,stopped}; quests.ts keeps the union as `scripts` on the campaign doc (journalTarget) and sends GlobalScriptsSync at join; the client starts what it lacks (nil-guarded) | done (unit) | quests.test.ts (persist + sync at rejoin; another character gets none), lua-tests (diff/started/stopped/chargen filter/sync); C++/Lua side needs a bake |
| 238 | No scenario kills an NPC with a REAL melee swing: combat.lua:50 forwards hits only when mpTest; s51/s58/s109/s111/s118/s119/s120/s128/s157 prove the synthetic relay, the avatar-swing path players take is unproven; s52's PvP-off gate likewise | new s164 real-melee kill (equip/stance/attack loop, s138 idiom); s52 rewrite | fixed | s164 (new: iron longsword, longblade 100, stance, attack loop beside a stung -2,-7 creature; hitFwd must stay undefined; B sees the death); s52 rewritten (managedPeer, real swings at B's puppet for 12 s with pvp off, B's selfStats unmoved) -- both need a sweep |
| 239 | s72 merchant purse passes with purse sync absent (two untouched merchants read the ESM default; may even be two different NPCs) | s72 → s161 idiom or delete | done | s72 deleted (s161 already opens the same merchant on B after A's sale and asserts A's post-sale purse); MP-COVERAGE-MAP notes the deletion |
| 240 | Weak asserts: s137 never asserts the enemies-nearby bit was CLEAR before; s149 counts any damage as drowning (s999:153 breath check missing); s110/s112/s66/s116 compare current<base instead of current<before; s143's roll assert is inside `if (rolls.length>0)`; s140:41 waits on a stale socialResult; s999 compares a value with itself | per row | fixed (s999 open) | s137 asserts the bit clear before; s149 asserts firstHurtAt >= 15 s; s110/s112/s66 compare to the bars before the poke (s116 already did, now a real cast); s143 asserts leaderRolls.length > 0; s140 clears socialResult before the private flip. s999 is an untracked control in the main checkout, not on this branch: its `cur` should read `bars(a)` (selfStats), not hp twice |
| 241 | Deaths by sethp:0 (s22/s77/s131; s131 has NO peer in a peer-ruled world): death-from-damage never exercised | managedPeer + fall/drown idiom | fixed | s22/s77/s131 managedPeer + retail, drowned on the -3,-9 sea bed (_death.mjs); s22/s77 assert the server's respawn.sent line (ctx.serverLogTail, new), s131 the host's puppet falling. Depends on the peer actually drowning an avatar (s149 has not been green yet: #185) |
| 242 | Spell scenarios park effects (castat:/castp:/healp:/selfcast: = activeSpells:add) instead of casting: s59/s144/s116/s115 stay green with the cast path broken | learnspell/mintspell + stance:spell + face: + press: (s146 idiom) | fixed | _spell.mjs (learnspell of a RETAIL spell, skill 100, setmp, stance:spell, face:, press:): s59 Fire Bite on touch beside the NPC, s116 Fire Bite at the victim's puppet, s144 Heal Companion at the friend's puppet, s115 Summon Scamp pressed until `actives` lists it. No hook fallback kept: a touch cast from 60 units targets a puppet headlessly (the engine picks the actor in front) |
| 243 | s117 companion-indoors: hand peer never holds the interior, door:enter teleports by hook — no holder asserted | managedPeer + authorityHolder assert (s118:94 idiom); door:enter without the hook teleport | fixed (partial) | s117 managedPeer, asserts authorityHolder !== 'none' and isHolder === 'false' inside. Still door:enter (mpDoorEnter teleports by hook); a real activation of the load door needs a Lua hook (player.lua: activate the nearest door object instead of teleporting) -- listed, not done |
| 244 | Harness: skips exit 0 (mp-harness.mjs:1038 ok = !err; #114 still open); load-gated convergence skips in s10/s40/s42/s43 turn a real divergence into SKIP on a loaded builder; s62 marks itself SKIP mid-run; Lua errors are printed, never fail | mp-harness.mjs exit code; fail on divergence regardless of load | fixed | mp-harness.mjs: any SKIP exits 1 unless OMW_ALLOW_SKIP=1; a client or peer Lua error FAILS the scenario unless it exports `allowLuaErrors = true` (only s125 printed one in #91: mpPuppetDetached on a creature without puppet.lua -- global.lua now checks hasScript first, so a second detach is a no-op); s10/s40/s42/s43 assert convergence/roster whatever the load (loadavg stays in the message); s62's partial branch logs `note:` |
| 245 | Diagnostic scenarios (s11, s65, s74, s98) assert nothing and count as PASS | `diagnostic` export reported separately | fixed | s11/s65/s74/s98 export `diagnostic = true`; the harness prints them as DIAG and a separate "diagnostic:" line, outside the PASS count |
| 246 | s20 asserts the sender's own mirror (proves nothing about the wire); s31/s111 chesttake: is a direct sendContainerOpByNet (the UI take diff is never raced); s32 door:lock: sends ObjectLock itself (s158's watch is the real path) | per row | fixed (s32 open) | s20 now asserts on B's puppets mirror: A's sethp:0 must show as `dead` on B's puppet and clear after the respawn (the mirror carries no hp; adding `hp` to global.lua's puppets mirror at :1242 would let it assert a non-lethal value); s31/s111 take race noted server-transaction-only in MP-COVERAGE-MAP; s32 left, needs a Lua hook that picks/unlocks through the engine rather than sending ObjectLock |
| 247 | Coverage map overstates: topics "OK" (s73 injects into testLearned; s75 says organic UNPROVEN); barter:sell writes the purse directly (TradeWindow not drivable headless) — say so | MP-COVERAGE-MAP.md rows | done | topics row → UNPROVEN organically; merchants row → watch + purse write, not the TradeWindow |
| 248 | Restore-over-time and Damage Magicka land TWICE on a peer-ruled body: the bar channel carries the local result AND both active-spell mirrors carry the effect (a Cheap Potion of Healing heals ~40, a 50-pt self heal ~100; a helper's durational heal 2x) — invisible to s146/s112/s144 which never assert a magnitude | global.lua withoutBarCarried (restore H/M, damage M dropped on both mirrors; fatigue kept) | fixed | lua-tests (helper run + both call sites), s165 heal-once (new, unbaked) |
| 249 | PvP-off veto bypassed by any spell record with one non-Harmful effect (forwardMagicHits merges a frame's hits with beneficial = OR; the receiver applies the whole record) | puppet.lua beneficial = every hit; combat.lua drops the harmful effects under PvP off; combat.ts vetoes a cast with any harmful entry | done (unit) | combat.test.ts (mixed cast vetoed, heal crosses), lua-tests |
| 250 | The receiver applies the WHOLE record ignoring which effects hit (range/area/resist): a Self+Touch record cast at an NPC gives the NPC the caster's Fortify | MagicHit.mEffectIndex (spelleffects.cpp seam) → puppet.lua effects[].index → CombatSpellHit.indexes (combat.ts validates) → combat.lua applies only those | fixed | combat.test.ts (indexes relayed/validated), lua-tests (owner applies the named indexes); C++ unbuilt |
| 251 | Scrolls and cast-when-used items at a puppet/NPC land NOTHING (source id is the ITEM record; combat.lua:241 gates on spells.records → nil); spellId never toNet/toLocal at that seam so a spellmaker spell at a puppet lands nothing either | combat.lua sourceEffects (spell, enchantment, or item→enchant); spellId toNet on send, toLocal on receive | fixed | lua-tests (scroll resolves by item id; id mapping source check) |
| 252 | CombatSpellHit unvalidated beyond shape + cell adjacency: a modified client lands any retail spell on any player/NPC in a neighbouring cell at 8/s with no magicka | combat.ts knowsSource: doc.spells or inventory must hold the id (sim peer exempt: it forwards the world's casts); the castat: hook now declares the spell first | done (unit) | combat.test.ts (unknown spell/scroll refused, known routed) |
| 253 | Magicka raises have no budget (hp has RESTORE_BUDGET): a modified client refills magicka at will | playerstate.ts: the same window/budget per bar (restoreMpInWindow) | done (unit) | avatarstats.test.ts (sustained refills refused) |
| 254 | Reflect ping-pong between two IDLE Reflect-wearers (the reflected copy lands on the caster's puppet and is forwarded without ignoreReflect) | MagicHit.mReflected from Flag_Ignore_Reflect (activespells.cpp stamps the bounced copy) → CombatSpellHit.ignoreReflect → add({ignoreReflect}) | fixed | lua-tests (flag relayed and applied); C++ unbuilt |
| 256 | A guest's level-up loses its health gain permanently: raise() returns early under restRefused (4 s window) before newBase() is consulted; identity never re-claims the base while the peer rules; relog restores the old base | playerstate.ts:209 keep the base step under restRefused; levelup: hook + mp.applyLevelup binding for the harness | done (unit) | playerstate.ts raise(): under restRefused the base step still lands (c capped at the old current, no heal); avatarstats.test.ts 'a base raise behind a refused rest lands the base'. Harness: `levelup:strength,endurance,luck` hook → mp.applyLevelup (mwmp/luabindings.cpp over MWMechanics::applyLevelup); scenario not yet written |
| 257 | The journal after any relog: one entry per quest (current stage only), hash order, all dated today (doc.journal is questId→index) | quests.ts record() append doc.journalLog {q,i,d} (cap 2000); JournalSync in order; mp.addJournalEntryAt binding for dates | done (unit) | quests.ts logEntry(): doc.journalLog {q,i,d,m,dm} (d = dayspassed as world.lua computes it, cap 2000, one line per stage) on the journalTarget doc; JournalSync carries journalLog in order; quests.lua replays it with ipairs through mp.addJournalEntryAt (Journal::addEntryAt, stamped) before the quest map, falling back to quest:addJournalEntry; quests.test.ts 'the journal sync carries the entries in the order they were earned' |
| 258 | Sneaking beside a friend: their puppet counts as an observer (updateSneaking) — the eye hides and Sneak never trains | actors.cpp:2091 skip avatars/puppets (the #143 predicate) | fixed | actors.cpp updateSneaking: observers that are avatars, or puppets without a content file, are skipped (canReportCrime's predicate). Needs a bake |
| 259 | Shared map exploration paints wilderness markers named "3" (unnamed exterior keys fall back to the key; vanilla adds visited locations for named cells only) | world.lua:627 skip unnamed cells | fixed | world.lua MP_WorldMapExplored: setMapExplored only when the exterior cell has a name (lua-tests source check) |
| 260 | Map exploration persists nothing and never catches up (relayed to the current roster only): a returning host has a blank world map; a guest sees only what is discovered from now on (refines #50) | m7.ts:325 doc.explored (cap 1024) in the welcome record; replay via MP_WorldMapExplored | done (unit) | m7.ts mapExplored(): exterior keys on the explorer's doc.explored (and the owner's when the map is shared; cap 1024, deduped); connection.ts welcome record carries `explored` (m7.exploredFor: the owner's when shared, else own); global.lua restoreTick replays it through MP_WorldMapExplored; worldtime.test.ts 'explored cells come back in the welcome record on a rejoin' |
| 263 | `use` held into a menu keeps the avatar wound up for the whole menu (playercontrols leaves controls.use) | player.lua inputTick mask use in a UI mode | fixed | player.lua inputTick: the use bit is masked while I.UI.getMode() is non-nil (the harness's press:/attack: force is kept) |
| 237 | Two equipped rings of one record collapse to one on the avatar/relog (record-id slot resolution = first match) | mwlua actor.cpp:52 | done — bake needed | actor.cpp findInInventory record-id branch walks the store and prefers a copy not already equipped (InventoryStore::isEquipped) or a stack with count > 1; falls back to search() |
| 255 | No hit VFX/sound on a puppet for the caster (the seam returns before playEffects) | spelleffects.cpp:1456 | done — bake needed | spelleffects.cpp puppet seam calls playEffects once on first application (non-Lua actives; same temporary/equipment non-looping rule as the desktop path) |
| 261 | Finding the host (#139): no map-marker Lua API; smallest real design = mp.setPartyMarker over WindowManager custom markers (minimap + map, active 3x3 only) + the friend's cell name in the social panel for the far case | luabindings.cpp, global.lua mirrorPuppets |
| 262 | Guest Rest UX: full wait animation, sky fast-forwards, then refused, sky slews back, heal undone by the next peer report ("the bed lied"); a pre-emptive refusal must NOT remove the sleep or guests stop levelling (#256) | server.ts hostOf timeSkip on WorldMode; global.lua forwards MP_WorldMode to player.lua; player.lua UiModeChanged Rest for a non-owner under owner/party removes the mode, notices, and I.UI.addMode(LevelUp) when progress >= iLevelUpTotal (no new binding: the LevelUp ui mode already exists) | fixed | ownerleft.test.ts (timeSkip on WorldMode); lua-tests (rule executed + wiring); needs a bake |
| 264 | SCALE BLOCKER: the peer's actor stream draws from actorMoveBucket (60/s): ≥4 populated held cells starve the rest (same 3 cells pass every tick, the other cells' NPCs freeze) — certain in a TR city, already reachable in Balmora | connection.ts:485 exempt the authed peer as bytes/msgs already are | fixed | connection.ts: the authed peer is exempt from actorMoveBucket as from bytes/msgs; backpressure.test.ts: the peer holding 5 cells at 20 Hz for 2 s, zero actor_shed, every cell's stream flows; a player's actor batches still shed |
| 265 | Co-anchored interiors share one Bullet world and one navmesh at overlapping coordinates: NPCs in interior A collide with B's walls; anchored-interior pathfinding is straight-line | scene.cpp:700 skip the navigator add for anchors; collision groups per cell (engine work) — do not anchor >1 interior until then |
| 266 | Exterior navmesh covers only ~6,100 u around the dummy: AiFollow/Escort/pursuit in another anchored exterior walk straight through buildings | navmeshmanager.cpp:75 multi-centre ranges (engine work) |
| 267 | Peer tick budget: fixed dt, no catch-up → overload = world-wide slow motion; engageCombat N² per second; predictAndAvoidCollisions every tick; physics on the main thread; actors.lua scans ALL activeActors once PER HELD CELL per tick with pcall closures per actor | actors.lua:94 bucket by cell once per tick, hoist closures; gamedata.ts buildPeerSettings `[Physics] async num threads = 1`; osgStatsFile to measure | fixed (partial) | Lua + config only: actors.lua buckets activeActors by cell once per tick (actorsByCell) and hands each held cell its list, actorPose pcalls module-level functions; gamedata.ts buildPeerSettings `[Physics] async num threads = 1` (gamedata.test.ts). Engine side stays open: fixed dt/no catch-up, engageCombat N² and predictAndAvoidCollisions every tick, osgStatsFile |
| 268 | Client apply path: one LSER serialize→queue→deserialize→pcall per NPC per batch (2,000+/s at 100 NPCs), one extra frame of latency; NPC puppets never get a render tier so far-cell 1 Hz NPCs are steered every frame toward a stale target | netmanager.cpp decode 0x0200 into per-RefNum pose slots polled by puppet.lua (takeMagicHits pattern); stamp tier on NPC entries |
| 269 | WorldCellState can exceed the LSER node ceiling with every map merely AT its cap (placed 38k + moved 20k + containers uncapped) → every entrant BAD_PROTO permanently; a 9-cell entry burst of fat TR docs trips the 1 MB hard kill at the city gate | worldstate.ts:30 total-node budget at send (trim moved → containers.origin → memberVars); exempt the entry burst from the hard check ~2 s | done (unit) | worldstate.ts cellStateBody: one builder for WorldCellState/CellSnapshotReplace, counts nodes with lserNodeCount (exact, mirrors the decoder) and trims moved → containers → memberVars → newest placed until under 60,000, logging world.cell_state_trimmed; connection.ts skips the 1 MB hard kill for 2 s after sendCellStateAround (Player.cellStateBurstAt). worldstate.test.ts: a doc at every cap decodes; backpressure.test.ts: the entry window survives the ceiling, then the kill lands |
| 270 | Gateway governor is a static price (peerCount is always 1 now, peerCostMb dead, stale comments); no RSS/CPU sampling: 3 TR worlds on a 4-core box are admitted until OOM | worlds.ts:191 price by measured RSS (/proc VmRSS via child.pid) + loadavg reason 'cpu' | done (unit) | worlds.ts capacity(): WorldDeps.sample(pids) (default: gateway rss + /proc/<pid>/status VmRSS per world child, os.loadavg()[0]) is read first; measured RSS past the usable budget caps at what is running with reason 'mem', loadavg past the core count with reason 'cpu' (never below 1); the static price + count cap follow unchanged. worlds.test.ts (injected sampler; static price admits 3, rss 2100 refuses with 'mem', load refuses with 'cpu', pressure gone admits again). Stale peerCount/peerCostMb comments left as they are |
| 271 | Small: netmanager caps a batch at 255 entries and actors.lua never chunks (actor 256+ never moves); unpackActorMoveBatch fully decodes N entries to read the epoch on the relay path | netmanager.cpp:321, actors.lua chunk; worldstate.ts:755 read the epoch header | fixed | actors.lua broadcastCell sends chunks of 255; worldstate.ts reads the epoch with peekActorMoveBatchEpoch (header + length check, no entry decode; actor.test.ts) |
| 272 | NIGHT BLOCKER: joining a friend from the launcher spawns the guest's own home world without entering it; sweep() discards a world nobody connected to within 15 min by rmSync of its whole dir — the guest's SOLO world cell state (looted containers, drops, doors, deaths) is deleted. Same for a returning character whose first TR download exceeds 15 min | worlds.ts:506-519 discard only when no world/world.db exists (else stop); or do not spawn the home world on a launcher join | done (unit) | worlds.ts sweep: never-joined discards only a dir without world/world.db, else stop(); worlds.test.ts revived-world-kept; launcher.html bootGame skips POST /worlds on a join when the character has a position under its home id (revive-on-dial brings it back) |
| 273 | Every in-place Solo↔Party click shows a 30 s black "Waiting for the world to be simulated" (holdUntilSimulated zeroes simReady; SimReady is only sent at join) — the harness bypasses the DOM handler | index.html:4610 drop the hold for the inOwn flip | fixed | index.html: the inOwn Solo/Party flip no longer shows the loading screen or holds for SimReady; the SocialResult status line is the ack |
| 274 | Cold-peer starts (TR: 2-5 min, after every idle reap) drop the player into a frozen world after the 30 s settle ceiling with no message | index.html:1391/5113 settle ceiling = peer startTimeoutMs, or a dismissible "world still starting" banner | fixed | index.html: settle ceiling 300 s (a page constant; nothing the page reads carries simPeer.startTimeoutMs), dead-man release at ceiling+10 s, dismissible "still starting" banner past the ceiling, cleared on simReady |
| 275 | A friend request or invite sent to someone in ANOTHER world produces no toast (live events only in-world; the 10 s FriendList snapshot rebuilds silently) — the night's starting state | social.lua MP_FriendList notice() for newly present requests/invites | done (unit) | social.lua MP_FriendList notices newly present requests/invites once per acct (requestsSaid/invitesSaid); index.html status line names them; lua-tests source check |
| 276 | The terminal modal says "wrong password for this account name" for every AUTH_FAILED (expired ticket, wrong character, chargen, sign in again); UNREACHABLE doubles its detail; the title is always "Can't join multiplayer" even for KICKED/SUPERSEDED; friendlyMpErrorBase is dead code | global.lua:316-328 | done (unit) | global.lua FAIL_TEXT AUTH_FAILED='sign-in was refused' (detail says why), no duplicate UNREACHABLE detail; index.html title Disconnected for KICKED/SUPERSEDED/BANNED/SHUTDOWN, friendlyMpErrorBase folded into friendlyMpError; lua-tests source check |
| 277 | "Go back to sign in" forces a fresh login although the session is valid (no #characters hash); SUPERSEDED copy says "account" while it is per character | index.html:1546 | fixed | index.html: the modal button goes to launcher.html#characters when omw-mp-session is set; SUPERSEDED reads "this character" |
| 278 | Continue within ~20 s of the idle reap says "The server is full" (ensure() null while draining → 503) | worlds.ts:292 / directory.ts:405 distinct restarting error + one retry in bootGame | fixed | worlds.ts isStopping + directory.ts POST /worlds answers 503 {error:'restarting'} for a draining world; launcher.html retries once after 3 s, then a distinct sentence; the cap message is unchanged |
| 280 | Guest spawn is one-shot: if the host has no pose at the joiner's auth (loading, peer restart) the guest boots at the engine default start and is never moved; an auth-rescue reboot re-teleports a guest beside the host | server.ts:928 | done (unit) | server.ts guestSpawn: a host without a pose defers the InviteAccepted teleport to their first pose (1 s poll, bounded by the peer start timeout); a guest with a stored position in this world is not re-spawned; onecharacteroneworld.test.ts |
| 281 | KICKED race on "sent home": the 5 s disconnect can beat a slow ticket mint → "you were kicked" modal instead of going home | server.ts:500 | fixed | server.ts sendGuestHome: KICKED grace 30 s (guestKickGraceMs test seam; flipworld.test.ts uses 500 ms) |
| 282 | Sign-in error copy: every #mperror code renders "<code>. Please try again." (registration_disabled, banned, invite_required...) | launcher.html:1524 | fixed | launcher.html: #mperror code -> sentence table for every routes.ts/oidc.ts code; "Please try again" only where retrying can help |
| 285 | HIGH: every AI path (wander walk, chase, pursue, follow, escort, travel, cast) freezes for NPCs ≥2 cells from the peer's dummy and in every anchored interior while the dummy stands outdoors: pathTo → isNearInactiveCell converts positions into the DUMMY's cell frame and zeroes them at the grid edge | aipackage.cpp:113/406-425 take the actor, false for interiors, test the actor's cell against Scene::isWithinActiveGrids (anchor-aware) fixed | aipackage.cpp isNearInactiveCell(actor, position): false in an interior; an exterior tests the cells at position ± 200 against World::isWithinActiveGrids (scene.cpp, anchor-aware; new MWBase::World accessor). No anchors = the player's 3x3, vanilla. s166: a provoked creature two cells from the dummy closes 450 u in 15 s |
| 286 | NPC puppets never run: actors.lua reads a non-existent types.Actor.isRunning so bit 0 is never set; a running guard outpaces the walking puppet and snaps every cooldown | actors.lua:121 flags bit0 = animVel > 1.05 fixed | actors.lua actorPose: bit 0 = animVel > 1.05 (speed vs walk speed); the isRunning read is gone. lua-tests |
| 287 | Respawn-flagged NPCs (guards) are re-killed at every re-anchor after the 72 h expiry (actorOverrides/lastSnapshot still say dead, the grant loop teleports + writes hp 0), kill tally climbs | actors.lua:421 skip a.dead in the grant loop (deaths re-assert via WorldCellState) fixed | actors.lua MP_ActorAuthorityGrant skips a.dead entries; WorldCellState → noteCellDeaths re-asserts a standing death. lua-tests |
| 288 | NPC/creature attacks, casts and idles are invisible on clients: holder sets no bit 3, puppet.lua has no creature attack branch, MP_CastFx only from player.lua, wander idle variants never travel | companion.lua use-edge → bit 3; puppet.lua creature attack1 branch + spell-stance cast | fixed | mp.isAttacking (CreatureStats::getAttackingOrSpell) + animation.isPlaying(obj,'spellcast') -> bit 3 of the holder's actor pose (actors.lua actorPose); puppet.lua showSwing plays a random attack1..3 the creature has (start/max attack/stop keys) and the existing spell-stance spellcast. Wander idle variants still not carried; needs a bake |
| 289 | Doors opened by NPCs on the peer never reach clients (only the activating PLAYER relays; the poll covers locks only, dummy's cell only): the puppet hits the shut door and snaps through | objects.lua:1140 poll getDoorState over held cells on the peer fixed | objects.lua 1 Hz poll: on the peer the cell list is the 3x3 ∪ actors.heldCells(); a non-teleport door read Idle with a changed open state sends DoorState; MP_DoorState writes doorStateWatch before applying (echo mute). lua-tests |
| 290 | Puppets take local fall/drowning damage (the gate is isPlayer only); a local kill of an NPC puppet is permanent on that client (no MP_Revive sender for NPCs) | character.cpp:2258, actors.cpp:1031 add MWMP::isPuppet fixed | character.cpp fall damage and actors.cpp drowning skip MWMP::isPuppet(refnum) bodies (the holder takes it). NPC MP_Revive sender not added — no local damage means no local kill |
| 291 | Flying/swimming creature puppets move level (pitch streamed as 0; steering is 2D): a cliff racer above its target hovers until the 256-unit teleport | actors.lua:137 pitch = rotation:getPitch() fixed | actors.lua actorPose pitch = obj.rotation:getPitch(); puppet.lua already applies it (#133). lua-tests |
| 292 | NPC combat against an avatar is dropped where vanilla holds it for the player (target in deep water/flying/invisible): wade into water to reset a retaliating NPC; avatar companions are not player allies in engageCombat | aicombat.cpp:195 isPlayerOrAvatar; actors.cpp:675 fixed | aicombat.cpp keeps the fight when the target is isPlayerOrAvatar or its ally set holds one; actors.cpp engageCombat: "sides with a player" = any_of(isPlayerOrAvatar) over the actor's own siding set (siding is symmetric, so no avatar registry walk) |
| 298 | CRITICAL: cell docs are keyed by content-file INDEX (c:<index>:<contentFile>): install/uninstall/reorder/disable ANY plugin (or a Lua .omwscripts mod, or Tribunal off) and every tombstone deletes a different object, every moved/lock/door/memberVar/death applies to the wrong ref — silently, with no reset affordance | ref.ts:13, cellstore.ts:73: stamp the canonical content list in world_global; remap c: keys by name→index on a list change (drop gone files) or refuse to start; long-term key by file name | done (unit) | cellstore.ts: world_global.content = the peer's manifest names in idx order, stamped by WorldState.adoptContentList (connection.ts handleHello, right after setAuthoritative). A boot under a different list rewrites every cell doc (deleted, moved, locks, doors, containers, actorDeaths, memberVars, enabled, follows) by file name → new index in one sync sqlite tx, drops keys whose file is gone and the opaque actorOverrides blob, logs world.content_remapped {changed, dropped}; no stored list (legacy world) = stamp and continue. celllifecycle tests: Tribunal removed, Bloodmoon inserted |
| 299 | Version skew of a same-named plugin is undetectable (manifest carries size=0 and no sha256 for anything; strict ≡ names; hashContentFiles has no callers): a locker player with TR 24 vs the server's 25 passes the gate and desyncs every ref | net.lua:137, manifest.ts:87, gamedata.ts:312: fill sha256 from the locker (index.html:3125) and hash server plugins; or refuse locker boots on modded worlds | fixed | Server half: gamedata.ts hashContentFiles hashes gamedata/ plugins AND enabled mods/<slug>/ plugins (lowercase name → sha256); ContentGate.hashes is read lazily at setAuthoritative so the peer's pinned entries carry sha256 and `strict` refuses a same-named different-version plugin. Locker side: index.html publishes each locker plugin's sha256 as OPENMW_MP_CONTENT_SHA256 (`lowercase-name=sha;...`), mp.getContentHashes (luabindings.cpp, unbaked) hands it to net.lua, whose manifest entries now carry sha256 when the page knew it — so a `strict` world can refuse a locker TR 24 against its 25. Under `names` nothing changes. manifest/gamedata tests; lua-tests "manifest sha256 from the page"; play-front-door "a locker boot hands each plugin sha256 to Lua" |
| 300 | Top-level .omwaddon/.omwscripts in gamedata/ load on the peer but not in the browser (buildLoadOrder takes esm/esp only) → every client BAD_CONTENT | index.html:2181 accept the same set as gamedata.ts:51 | done (unit) | index.html buildLoadOrder: .esm → masters; .esp/.omwaddon/.omwgame/.omwscripts → plugins; .bsa/.ba2 → archives — the same split as gamedata.ts. play-front-door test |
| 302 | Missing master: the browser cascades the plugin out and boots, the peer aborts (esmloader.cpp:48 throws) → peer crash-loop, unsimulated world, no error naming the cause | mods.ts:172 apply the same cascade in resolveMods | done (unit) | mods.ts resolveMods: after orderByMasters, a plugin whose declared master is neither an (enabled) official master nor in the list is dropped, cascading until a pass drops nothing; logs mods.missing_master {plugin, master}. data= lines stay. mods test |
| 303 | fallback-archive order differs for TOP-LEVEL archives (peer sorts all alphabetically incl. official; browser official first then mods): a top-level replacer BSA wins in the browser and loses on the peer → different meshes AND collision | gamedata.ts:143 official first in OFFICIAL order, then the rest | done (unit) | gamedata.ts: archives sort official-first in OFFICIAL order, then the rest by name — the browser's order. gamedata test |
| 306 | The persistent chunk cache keys by pathname@size: a re-installed mod file of identical size serves old bytes, bypassing #178 | streamfs.js:241 add mtime from mwdata-mods.json files[].m | done (unit) | mwdata-routes.ts walk() adds `m` (integer mtime ms) to every files[] entry; index.html mountServerMods passes f.m to StreamFS.mount, whose persistent key is pathname@size[@mtime]@CHUNK. Retail mounts pass no mtime (immutable, keeps every cached base game). mwdata-serve + play-front-door tests |
| 301 | No groundcover= support: a grass plugin becomes content= on both engines (tens of thousands of addressable refs per exterior on the peer, rendered as statics in the browser) | mod-install.ts:414 flag + buildPeerCfg/index.html groundcover=, excluded from the manifest | fixed | mods.ts isGroundcover: filename /grass\|groundcover/i or the dashboard's `groundcover:true` on the plugin (saved by the mods PUT, round-trips the doc); resolveMods puts them in `stack.groundcover`, never `content`; buildPeerCfg emits groundcover=, mwdata-mods.json carries `groundcover`, index.html emits groundcover= for what mounted. Not content= on either engine ⇒ not in core.contentFiles.list ⇒ not in the manifest. mods.test.ts "a grass plugin (by name or by flag)…" |
| 304 | A mod's addHandlerForObject / lazily registered activation handler pre-empts the dialogue lock and pickup veto (per-object handlers run before type handlers); no Lua-mod guidance | activationhandlers.lua:40; docs | documented | MP-COVERAGE-MAP.md §7 "Lua mods: unsupported patterns" |
| 305 | Lua mods that create objects on a client are booked as player drops (onItemActive → requestSpawn fromInventory) — N clients = N spawn requests, each debited/refused | objects.lua:581 gate on a recent local drop event; document unsupported | documented | MP-COVERAGE-MAP.md §7 "Lua mods: unsupported patterns" |
| 307 | Armor/Unarmored/Block skills NEVER progress while the peer rules (hits land on the avatar: I.SkillProgression is player-only, block use gated on == getPlayer()) — permanent progression loss for tanks | combat.cpp:172 isPlayerOrAvatar; local.lua:186 + avatar.lua _onSkillUse → AvatarSkillUse → owner's I.SkillProgression (block/armor only) fixed | combat.cpp block use on isPlayerOrAvatar; omw/combat/local.lua (+ fsroot mirror) calls I.MPAvatar.skillUsed when SkillProgression is absent; avatar.lua exposes MPAvatar + _onSkillUse and forwards the armour/block family only (mpAvatarSkillUse) → global.lua AvatarSkillUse {id, skill, useType} → playerstate.ts handleAvatarSkillUse (world peer only, family + useType 0..3 validated, 10/s per owner) → SelfSkillUse → player.lua MP_SelfSkillUse → I.SkillProgression.skillUsed. avatarskilluse.test.ts; lua-tests |
| 308 | Melee sneak-attack critical still gated on the local player (npc.cpp:662 wraps skill use AND the crit; wave-4 fixed ranged only — row 90 overstated) | npc.cpp:662 split the gate: crit on isPlayerOrAvatar fixed | npc.cpp: skillUsageSucceeded stays == getPlayer(); the unaware check + fCombatCriticalStrikeMult + critical sound run on isPlayerOrAvatar; the sTargetCriticalStrike message box stays player-only |
| 309 | The owner's taps are lost and the owner rubber-bands while the avatar is in hit recovery (useLatch consumed in one frame; kd report covers knockdown only, not hit recovery) | luabindings.cpp:274 kd = knockedDown or hitRecovery; avatar.lua keep useLatch armed while kd fixed | mp.isKnockedDown returns getKnockedDown() or getHitRecovery() (the stats report's kd rides it, so player.lua holds still through a stagger too); avatar.lua clears useLatch only when mp.isKnockedDown is false. lua-tests |
| 310 | Owner hit/miss feedback is an independent coin flip (local roll vs the avatar's roll: 50% mismatch at 50% chance); a landed punch plays "miss" (fatigue-only damage fails the health check) | puppet.lua:239 play on an hp/ft DROP in MP_Stats instead of the local roll fixed | puppet.lua onHitIntercept only records the swing time/hitPos; MP_Stats plays 'Health Damage' on an hp drop (blood at the swing's hitPos if within 1 s, else no blood) and 'Hand To Hand Hit'/'Hand To Hand Hit 2' on a fatigue-only drop within 1 s of a swing (running drains fatigue too, hence the window); 'miss' never plays. lua-tests |
| 311 | Elemental-shield reflection applies locally to a peer-ruled body on the dead swing (no peerRulesBody gate, unlike fall damage) — can kill the local body ahead of the peer | combat.cpp:402 gate like character.cpp:2260 fixed | combat.cpp applyElementalShields skips the setHealth when the attacker is the player and MWMP::peerRulesBody(); the Health Damage sound stays |
| 315 | HIGH: custom-record ids are per WORLD (mp_<kind>_<n> from 1 in each world's RecordStore) while the player doc crosses worlds: a friend's potion/enchanted item taken home is transmuted into another world's record or silently dropped from the doc | server.ts, recordstore.ts, m7.ts | done (unit) | RecordStore opens <sharedDir>/world/records.db; the id is minted inside a BEGIN IMMEDIATE tx from the counter row and reads pull sibling-appended rows by seq; cap 50k deployment-wide. worldtime.test.ts (two stores on one shared dir mint distinct ids; the 50k fill still joins in chunks). Existing per-world records.db files are NOT migrated (their ids collide by construction) |
| 316 | During the host's 90 s crash grace the world thinks it has no owner (journalTarget from the roster): peer-driven journal/globals land in every GUEST's home doc; a guest's quest writes persist nowhere | server.ts | done (unit) | ownerCharLatch set on every join of the owner; ownerCharId()/journalTarget fall back to it when the roster misses. questpeer.test.ts (host socket killed, peer JournalEntry -> host doc 40, guest doc untouched) |
| 317 | A character parked in a removed mod's interior (or a disabled expansion's cell) cannot launch (engine aborts at --start); the F5 path drops them at exterior 0,0 = open sea, then persists the drowning | global.lua, session.ts, connection.ts, index.html | fixed | Welcome flags.respawn = [rules].respawn*; restoreTick cellExists() (pcall getExteriorCell/getCellByName) before the teleport, on a miss moves to flags.respawn and notices; fatalOverlay matching /cell .* not found/i with #mpstart reboots once without the fragment (sessionStorage keyed on the cell). lua-tests source checks; needs an engine rebake |
| 318 | Tombstones fill with useless n: keys (summons, levelled spawns); at 2000 every take in the cell replies ok but never persists — the cell's loot dupes on every reload | worldstate.ts, objects.lua | done (unit) | tombstone() skips n: keys and returns false at the cap; take(): a net ref not in placed -> gone, a content ref the cell cannot tombstone -> cell_full and no relay; client wording added. objecttake.test.ts (deleted seeded at 2000), worldstate.test.ts updated (no net tombstone) |
| 319 | [sharing] factions=false in an owned world: a guest's rank writes still target the HOST's doc (the isShared check comes after the write) — the host logs in demoted | quests.ts | done (unit) | standing.test.ts (factions off: guest rank 1 on the guest doc, host keeps rank 3) |
| 320 | Journal sharing flips: individual mode is a hybrid (host's journalLog + own map, writes to the host); shared→individual→shared replays a stale shared map and regresses the owner's stages every login | quests.ts | done (unit) | owner join seeds shared.journal[q] = max(shared, own) always; individual mode writes, serves journalLog and borrowed=false from player.charId (the peer still routes through journalTarget). quests.test.ts shared->individual->shared keeps stage 30 and both log lines |
| 321 | journalLog cap 2000 drops the BEGINNING of the campaign (TR passes it) | quests.ts | done (unit) | MAX_JOURNAL_LOG 5000 (12 nodes/entry = ~60k + a ~2000-quest map < 65,536). quests.test.ts pins 5000 entries + 700 quests = 61.4k nodes; no chunking needed; the cap itself: quests.test.ts pins entry 1 alive after 4999 later entries and gone at 5001 (MAX_JOURNAL_LOG exported) |
| 322 | never_joined discard never fires: world/world.db exists the moment a world boots (sqlite constructor) — abandoned sign-ins leak directories permanently | worlds.ts, server.ts | done (unit) | <dataDir>/.played written on the first human join (onPlayerJoined) and backfilled at boot when the cell store already has deltas; the reaper tests it instead of world.db. worlds.test.ts (a booted-never-joined world with world.db is discarded; one with .played is stopped) |
| 323 | Custom records never cleaned up; at 10,000 the creator's brew silently fails and the item does not survive a relog | m7.ts MAX_CUSTOM_RECORDS; dedupe (#60) | partial (unit) | dedupe by content (#60) + `RecordCreateRefused {tempId, reason}` on a cap/caps refusal so the client can fail loudly (no client handler yet: OpenMW drops it). worldtime.test.ts. Cleanup of unreferenced records still open |
| 324 | HIGH: a wedged (not crashed) peer holds every cell forever: the reader thread auto-pongs so the socket never drops, review() only logs authority.silent_peer, sweep() acts pre-hello only — NPCs frozen, no melee at all, indefinitely | authority.ts:347 onSilent → simPeers.stop(WORLD_KEY) once per grant | fixed | authority.ts reviewAll: once every actor-bearing cell a holder holds is silent past actorSilenceMs, onSilentPeer(holder, cells) fires (once per silence; a frame or a fresh grant re-arms); server.ts wires it to simPeers.stop(WORLD_KEY) for the managed peer and the ensure() pass respawns it. authority test with a stub clock |
| 325 | HIGH: peerRulesBody() is true for the whole session (want computed from the join-time simulated flag, never SimReady false): during a peer outage the real body takes no fall damage and cannot drown (exploit for the outage length); the promised degraded mode is half real | connection.ts:443 send SimReady{ready:false} at the peer's close; global.lua:1076 read the last SimReady; needs #295 | fixed | connection.ts: a system socket closing sends SimReady{ready: simReady()} to every human (false unless another peer is up); global.lua: `want` reads the LAST SimReady (lastSimReady, set by MP_SimReady, reset on leaving Joined) over the join-time flag, so mp.setLocalSpawns(true) and the peer rules flip during an outage and back on ready:true. #295 still gates it: anyHolder() stays true while any known cell keeps a holder mirror. simready test (peer close -> ready:false) + Lua source check |
| 326 | SIGTERM is swallowed by the headless peer (SDL_QUIT → nullwindowmanager windowClosed is {}): every clean stop is a kill_escalated crash — stale snapshot, named actors purged, no orderly leave | nullwindowmanager.hpp:190 requestQuit() | fixed | nullwindowmanager.hpp windowClosed() -> StateManager::requestQuit(), as the real WindowManager does; includes environment.hpp + statemanager.hpp. Not compiled here (peer rebuild on the builder) |
| 327 | Peer crash backoff is flat 15 s; a post-hello crash loops forever (full retail load per cycle); spawn ENOENT of a configured binary wedges the supervisor silently (entry stays with no helloAt) | simpeer.ts:341/352 escalating backoff + disablePermanently after ~5; delete the entry on error | fixed | simpeer.ts crashed(): a death within startTimeoutMs+60 s of spawn bumps a per-key streak, backoff 15/30/60/120/300 s (max with restartBackoffMs), the 5th early death logs simpeer.crash_loop and disablePermanently; a later death resets to tier 1; the child `error` event (spawn ENOENT) now drops the entry and backs off instead of wedging. simpeer tests with the injected Spawner |
| 328 | An outage kill is a permanent per-screen divergence and counts for nothing (non-holder ActorDeath never sent; on return the NPC is alive for B, a corpse for A; corpse lootable without a kill) | puppet.lua MP_Detach keep the hit intercept armed in degraded mode ("degraded mode is cosmetic") | fixed | actors.lua holder-lost Info sends MP_Detach{degraded=true}; puppet.lua keeps the script attached (no mpPuppetDetached), re-enables AI, and onHitIntercept returns false while degraded so outage swings cancel; the first MP_Pose after the peer returns re-arms (actorKey from degradedKey, AI off, puppet mark on). Lua source check; needs an engine rebake |
| 329 | Peer stderr matching /error|fatal|.../ is forwarded at warn per line (unfilterable; sync stdout + appendFileSync per line): a per-actor-per-frame Lua error starves the 66 ms tick and rotates the 8 MB×5 log away in a minute | simpeer.ts:302 dedupe identical lines per second; log.ts keep the fd open | fixed | simpeer.ts forward(): identical lines within a second are counted (first goes out at once) and flushed as one line with times:N (map capped at 256 distinct lines); log.ts opens the fd once (writeSync per line, reopened on rotate). simpeer test: 10k identical stderr lines -> 2 log calls |
| 330 | Peer Lua tables never shrink: avatarDocs / remoteIdentity kept after MP_PlayerLeaveWorld (one full doc per join on a 24/7 world); no proactive peer restart on a continuously occupied world | global.lua:2300 nil them on leave | fixed | global.lua MP_PlayerLeaveWorld nils avatarDocs[id] and remoteIdentity[id]; both are re-sent on the next join (playerstate.ts). Lua source check |
| 331 | Nothing tells insiders the peer is down; a joiner waits on "Waiting for the world to be simulated" while insiders fight local NPCs; the creature you were fighting vanishes mid-swing (purgeNamedActors) | index.html banner on simReady 0 (needs #325) | fixed | index.html: a 500 ms watcher on the simReady mirror raises a dismissible "The world's simulation is restarting..." banner when it goes 1 -> 0 while Joined; clears itself on 1. Shares simBanner() with the still-starting notice |
| 332 | Dead-code sweep: the human-holder / handoff model no longer exists (canSimulate = system only) but actors.lua:412, puppet.lua:562, combat.lua:40 still describe it | comments | done | actors.lua Grant/Revoke comments say only the sim peer is granted and there is no client handoff; puppet.lua MP_Detach comment names its three callers (departure, cell change, degraded); combat.lua: the relay survives for the test hooks only, degraded mode cancels |
| 333 | HIGH (collision): resume-over-live resolves the sitting session by ACCOUNT (activeForAccount) after #124 legalised two characters per account: A's wifi blip kicks B and resumes A's device AS B | connection.ts:1520 resolve by token / activeForChar done (unit) | SessionIndex.add carries the charId; handleResume resolves the sitting session with roster.activeForChar(live.charId), never activeForAccount. session.test.ts: char A resumes over live while char B (same account) sits -- B untouched, A resumed as A |
| 334 | HIGH (collision): cross-process one-character-one-world kicks by ACCOUNT (presence PK) after #124: char A in world X, char B in Y → X's next 10 s beat kicks A | server.ts:1732 presence char_id column (with #284) done (unit) | presence gains a char_id column (migration list; account stays the PK for the friend list); setPresence writes p.charId; the supersede check kicks only when the foreign row's charId matches (a pre-column row still supersedes). onecharacteroneworld.test.ts: two chars of one account in two worlds, neither kicked |
| 335 | Owner relog replays the whole journalLog through the server as accepted regressions (engine fires one questUpdated per addEntry a frame later; the echo guard is a single slot): hundreds of sync writes + guests see the index bounce | quests.lua:121 per-quest set of applied stages; quests.ts drop a regress already in journalLog done (unit) | quests.lua keeps applied[questId][index] for every network apply and onQuestUpdate drops an echo of any applied stage (the engine fires one per addEntry AND one per setIndex); quests.ts drops an authoritative regress whose (q,i) is already in the owner's journalLog (no write, no relay). questcredit.test.ts: an owner replaying a 3-stage log writes nothing and relays nothing; lua-tests. A frame counter holding `applying` was not added: the set covers both engine events |
| 336 | Refused/unregistrable declarations retry forever with no backoff (level_jump never accepts; registerRecord nil forever for Ingredient/Light/Lockpick/Probe/Repair/Apparatus kinds or past the cap): anomaly + warn per retry | identity.lua:620 cap forgets per kind (3) until the fingerprint changes; world.lua:447 pending only when a record is in flight done (unit) | identity.forgetDeclared counts forgets per kind against the fingerprint it forgot; past 3 of the same fingerprint the cache stands (reset on reset()). world.registerRecord remembers an unsharable id and says so once; pendingDeps is set only when the referenced record is actually in flight. lua-tests |
| 337 | ObjectEnabled has no reach gate and cells.get creates+caches a doc for ANY ≤128-char key: a new doc + DB row per message, unbounded | worldstate.ts:1057 valid-shape far keys only + per-session distinct-far-cell cap done (unit) | farEnableAllowed: a far ObjectEnabled may name a well-formed exterior within MAX_ABS_COORD/8192 cells or an interior the session has been sent a WorldCellState for (Player.knownCells, set in sendCellState), at most 64 distinct far cells per session (Player.farEnableCells); the sim peer is exempt. worldstate.test.ts: a random far key creates no doc, an out-of-bounds exterior neither, a real far exterior persists. NOTE: a Startup disable in an interior the session has never been sent is now dropped (the 213/218 test walks the host through the cave first) |
| 338 | Human actor-spawn forward: any record in any cell (no cellsVisible, count ≤10,000 server-side) — 100 dremora lords/min anywhere | worldstate.ts:915 cellsVisible + count <= 10 done (unit) | a human's actor spawn is refused (reason 'reach') unless cellsVisible(player.cellKey, cellKey) and count <= MAX_HUMAN_ACTOR_SPAWN_COUNT (10). worldstate.test.ts |
| 339 | The netdelay FIFO drops every disconnect frame (ws.close runs synchronously after the delayed send): under OMWMP_NET_DELAY_MS the client only sees the close code (KICKED/SUPERSEDED/sent-home detail lost) | connection.ts:339 queue the close on the FIFO done (unit) | disconnect() pushes ws.close onto outQ behind the disconnect frame when the netdelay FIFO exists. netdelay.test.ts: with Connection.netDelay set, AUTH_FAILED's frame reaches the client before the close |
| 340 | `own` is a client-declared flag that makes any gear wear-proof (condition copied wholesale, peer wear discarded, sticky) | playerstate.ts:494 honour own only for lockpick/probe/repair/light record types done (unit) | no record-type lookup exists server-side (esm.ts reads headers only, content-table.ts is three id lists), so the client tags the kind: identity.lua sends t = Lockpick|Probe|Repair|Light beside own; parseItemStatesL honours own only with one of those tags; the client->doc merge takes own from the client's current entry (not sticky from the doc). avataritemstates.test.ts: a daedric_dai_katana tagged own still takes the peer's wear; lua-tests |
| 341 | The s147 fall probe can never fire: types.Actor.isOnGround takes an LObject, p.obj is a GObject (sol rejects it) | global.lua:731 read it in avatar.lua on self and mirror via mp.set fixed | avatar.lua fallProbe reads types.Actor.isOnGround(self) each update, tracks the airborne top and sends mpAvatarLanded {obj, top, z} on landing; global.lua avatarLanded resolves the puppet id and keeps the print. lua-tests (source) |
| 342 | Crime/witness/sneak exemptions test isPuppet && !hasContentFile: runtime-spawned NPC puppets (levelled, PlaceAtPC, #214) are no crime to assault and never witnesses | mechanicsmanagerimp.cpp:1228/1611, actors.cpp:2106 use a registry isPlayerPuppet fixed | puppets.hpp/.cpp gain setPlayerPuppet/isPlayerPuppet (a second set; setPuppet(false) and clearPuppets clear it); luabindings mp.setPlayerPuppet; puppet.lua markPuppet marks it when playerId is set. The three sites test isAvatar || isPlayerPuppet. Not compiled here (peer rebuild); lua-tests (source) |
| 343 | Forced-dialogue lock denial can close an ARREST dialogue (two players on one guard) | player.lua:1366 skip the lock request for the arrest path fixed | MP_OpenDialogue (the MP_PlayerArrest hop) sets arrestDialoguePending; UiModeChanged consumes it and skips mpDialogueForced for that Dialogue mode. lua-tests (source) |
| 344 | First-open cap (64 stacks) silently rejects any bigger container forever (65+-stack merchant/corpse never canonical, warns every open) | worldstate.ts:1251 cap gold only or raise to MAX_CONTAINER_ENTRIES and reply done (unit) | MAX_FIRST_OPEN_STACKS is gone (parseItems' MAX_CONTAINER_ENTRIES bounds stacks); an implausible gold hoard is logged + noted and the container becomes canonical WITHOUT its gold, and the opener gets its ContainerState either way. worldstate.test.ts: a 70-stack corpse is canonical; adversarial.test.ts updated |
| 345 | Cell-state trim drops the newest placed entries first — which are the net ACTORS the holder still streams | worldstate.ts:145 trim non-actor placed first done (unit) | cellStateBody sorts placed so actor entries lead before the tail-first trim. worldstate.test.ts: 20 actors survive a trim that sheds litter |
| 346 | Misc from the review: gone-credit debit reads before the 0.25 s acquisition lands (nothing debited); drop debit double-subtracts when the post-drop inventory beats the queued spawn; a dead idle avatar flushes sync SQLite at 1 Hz; sendCellSnapshot still uses cellsVisible (far holder keeps stale litter after a reset); over-cap inbound shed keeps non-lossy frames unbounded; I.Combat.spawnBloodEffect is an empty stub (blood half of #130 does nothing); peerEnabledAt/peerMemberAt/actorSpawnsBy never pruned; `local net` shadows the module in global.lua:3230 | per item; partial: playerstate.ts dead-transition flush, worldstate.ts sendCellSnapshot hears(), global.lua `local net` fixed | handleAvatarStatsBatch flushes 'now' only on the alive->dead transition (Player.avatarDead); sendCellSnapshot fans out on hears() so the far holder gets the reset; the three `local net` in the out-mappers are `netId`. Still open here: gone-credit debit timing, drop-debit double subtract, inbound shed of non-lossy frames, spawnBloodEffect stub, peerEnabledAt/peerMemberAt/actorSpawnsBy pruning. lua-tests (source) |
| 347 | Every Rest/Wait in MP pops "Saving is disabled in multiplayer…" (waitdialog autosave fires; the page writes no [Saves] section) | index.html baseline settings: [Saves] autosave = false when mpUrl is set | fixed | index.html baseline settings write [Saves] autosave = false when ENV.OPENMW_MP_URL is set (singleplayer untouched) |
| 348 | Friends (and every peer-moved NPC) vanish beyond 4096 u (3072 low tier): the client's actors processing range hides them while the server streams mid/far LOD tiers up to 12288 | actors.cpp:1790 keep player puppets active; or TIER.actors 7168 on mid/high; lodMidRadius ≤ what the client shows | fixed | actors.cpp: a player puppet (isPuppet && !hasContentFile) is inRange regardless of distance; index.html TIER.actors 7168 on mid/high (low 3072); config.default.toml notes the client radius beside lodMidRadius |
| 349 | Enter / T / O hijacked with an engine window open (dialogue, Rest, message box, name fields): Enter opens chat, t/o typed into a name field toggle panels | index.html:5378 gate on mpState().uiMode | fixed | index.html key handler returns early when mpState().uiMode is set and not 'none' unless chat/social is already open (our overlays push a mode too) |
| 350 | No attention cue for a request/invite/whisper while alt-tabbed: no sound, no Notification, document.title locked by a no-op setter | index.html:37 badge via __omwBadge + a 120 ms oscillator beep when document.hidden | fixed | index.html: title lock exposes window.__omwBadge(n) '(n) OpenMW-Web'; a new request/invite while document.hidden bumps the badge and plays a 120 ms 880 Hz beep at gain 0.05 on the engine's tracked AudioContext; visibilitychange -> visible clears it |
| 352 | A guest who reconnects during the host's 90 s grace is refused "this world is private" and sent home (mayJoinWorld applies to resume too; a shared wifi blip drops both and the guest's reconnect beats the host's) | server.ts mayJoinWorld takes `returning` (op=resume or a doc position in this world, the guestSpawn test); the no-host-no-newcomers refusal skips it | done (unit) | ownerleft.test.ts: a guest resuming inside the host grace is admitted |
| 353 | DESIGN CONTRADICTION: [sharing] crime = true by default → a guest's theft becomes the HOST's persisted bounty (host arrested for it after the guest leaves); in personal mode the guest's bounty is persisted nowhere — in both modes the guest goes home clean ("guests keep loot" incl. stolen loot with no consequence) | config.default.toml crime = false; quests.ts crime(): personal → the writer's own doc, shared → journalTarget as before; tests/s125 that assert the shared record opt in | fixed | standing.test.ts: personal guest CrimeUpdate lands on the guest's doc, not the host's, and the rejoin welcome carries it |
| 354 | A guest's "invite" is a dead end that lies: the page shows invite to everyone (not gated on amHost); the invitee's accept routes to the GUEST's idle home world → not_open; a host inviting from Solo also ends in not_open (invited() only clears kickedUntil) | index.html: invite row only when amHost; server.ts invited(): the owner inviting from Solo flips the world to party through setWorldMode (directory + WorldMode event as the O-panel) | done (unit) | flipworld.test.ts: the owner inviting from a Solo world flips it to party, the friend is admitted |
| 355 | Respawn on retail is "where they fell" with a warn per death (demo default treated as unset) — ship respawnCellKey="" (no warning) and move the demo coordinates to the harness config | config.default.toml respawnCellKey = "" (empty = where you fell, no warning); respawn.ts: the demo-default detection and both warnings are gone, a set key is honoured; mp-harness.mjs already wrote the Example Suite village into [rules] and keeps it | fixed | respawn.test.ts |
| 356 | config says "party members never hit each other" but pvp.ts only checks rules.pvp/zone/safeCells: pvp=true means friends hit each other everywhere (default false → moot) | the sentence is deleted from config.default.toml | fixed |
| 357 | Comment drift: send-home grace is 30 s in code, "5 s" in comments/coverage rows 110/234/243; chargen cells (Census Office, Prison Ship) are permanently local-only for the whole party (two party members there never see each other) — document | server.ts comments say 30 s; coverage rows 110/234/243 say 30 s; a coverage row records chargen cells as local-only for the party by design | done |
| 358 | CRITICAL (cheat): PlayerInventory is a bare trusted declaration — gold is EXEMPT from the 9000-per-id implausibility rule: `{gold_001: 100000000}` in one message, persisted, pushed to the avatar; 249 new ids per message at n≤8999; launder to legit items via drop→friend take (the "cannot drop what you do not have" oracle reads the same declared doc; refuseUnownedDrops false by default) | playerstate.ts:handleInventory gold delta > GOLD_DELTA_MAX (20,000; a merchant purse <= 10k, Creeper 5k/day) per declaration is refused + implausibleGains{inventory_gold} + anomaly; the connection lives, the doc stands. Record-id whitelisting stays a follow-up: core/gamedata.ts deliberately parses no ESM records, so there is no cheap item-id set on any tier | done (unit) | redteam-client.test.ts #358 |
| 359 | CRITICAL (cheat): PlayerActiveSpells applies ANY record's effects to the avatar (no knowsSource, no cost, stackable=true on the peer): a custom Fortify Health 400 ×8 → immortal avatar; retail NPC-only spells free | playerstate.ts:handleActiveSpells: every add needs knowsSource (a spell in the doc, an item in the inventory, or an id that left the inventory in the last 10 s -- the last potion of a kind is drunk before the 2 s inventory diff lands); adds budgeted by Σ(magnitude) per 10 s <= 800 (a custom record's own magnitudeMax per effect via StateCtx.recordOf, nominal 50 for a retail id the server cannot read). global.lua MP_AvatarActiveSpells: stackable only for a distinct record per owner. Needs a bake for the Lua half | done (unit) | redteam-client.test.ts #359; lua-tests source check |
| 360 | HIGH (cheat): RecordCreate caps are per effect: effects count, cost, charge, armor, speed/reach unbounded; magnitude 400 = 4× the spellmaker ceiling | m7.ts recordWithinCaps: effects <= 8, magnitude <= 100, duration <= 1440 (vanilla spellmaker maxima, was 4x), baseArmor <= 200, charge <= 400, speed <= 2, reach <= 2, weapon damage unchanged (<= 200); a non-autocalc SPELL's declared cost must be >= Σ(magnitudeMax x max(duration,1)) / 100 (the vanilla formula is 0.1 x baseCost x avg(mag) x (1+dur) and the server does not know baseCost, so the floor sits under the cheapest effect; above it the engine's figure rules) | done (unit) | redteam-client.test.ts #360; worldtime.test.ts caps |
| 361 | HIGH (cheat): PlayerCellChange is a free teleport and the PEER moves the ruling body there (tryTeleport on MP_PlayerCellChange): any interior, through any wall/locked door; same-cell coordinate jumps pass when both keys are exterior ≤256 u; farTravelPerMin only counts | connection.ts handleCellChange: a SAME-CELL PlayerCellChange further than 1024 u from the last pose is refused (conn.cell_change_refused, omwmp_cell_change_refused_total, anomaly cell_jump) unless the server saw a CombatCast (Recall/Intervention), a DoorState, or DialogueLock traffic (guild guide, fare) from this player within 5 s, a resurrect or the join within 15 s, or chargen. PARTIAL: interior->interior changes are NOT bounded -- a load door emits no server-visible signal and its coordinates are unrelated across cells, so that case stays farTravel-counted; intervention SCROLLS (no CombatCast) landing in the same exterior cell would be refused (rare; the client rubber-bands to the cast spot) | partial (unit) | redteam-client.test.ts #361 |
| 362 | HIGH (cheat): CombatHit with client-declared damage ≤1000 is applied verbatim by the holder (the mpTest gate is on the client send path only): 8000 hp/s on any NPC in the 3x3 or any avatar with PvP; knowsSource for spells is defeated by PlayerSpellbook.add (client-authored) | combat.ts hit(): a human CombatHit is refused (combatDropped{combat_hit_refused}) while the target's cell -- the victim's for a player target -- has a holder; degraded mode keeps the relay. Unit tests that policed the relay machinery through CombatHit now use CombatSpellHit (same resolveOwner path); s51/s58 mpTest melee hits ride the relay no longer and need re-pointing at CombatSpellHit in the next sweep. PlayerSpellbook.add: a custom record minted by ANOTHER account is refused + anomaly spellbook_foreign_record; peer SelfSpells grants land in the doc directly; a retail id cannot be checked (no ESM spell set server-side) and is accepted as before | done (unit) | combat.test.ts, combatlimits.test.ts (#362) |
| 363 | HIGH (cheat): ActorAI position/follow from a non-holder need no dialogue lock (only cellsVisible): teleport every quest NPC into the void; make every guard follow you | worldstate.ts followClaim: a follow claim needs dialogueHolder(ref.key) === player.id (live lock or the 5 s recent-release window) unless the claim is already the player's own (update/dismiss); position claims stay dialogue-free by necessity (216: Dagoth Ur's PositionCell, OnActivate, CellChanged scripts have no conversation) and are rate-bounded instead: 5 per player per minute, anomaly position_claim. position.cell is NOT bounded to the ref's cell (the 216 case is a different interior) | partial (unit) | actor.test.ts follow claims (#363); redteam-client.test.ts #363 (sixth position claim in a minute not relayed to the holder) |
| 364 | HIGH (grief): ObjectTakeRequest/Delete/Move/Lock on ANY content ref in the 3x3 with no type or distance check, persisted: delete Caius, every load door, the Ghostfence pylons; unlock any vault from across the cell | worldstate.ts docAndRef: for a human's take/delete/move/lock, a ref the server knows the position of (doc.placed or doc.moved) must be within 512 u of the player's last pose (same cell, or exterior neighbours -- world coordinates); an ObjectMove destination is bounded the same way; refs the holder streamed in ActorMoveBatch (the 8-byte refs of every 20th accepted frame, kept 10 min per cell) refuse take/delete/move as `actor_ref` + anomaly. A never-moved content ref has no server-side position and keeps the cell-visibility rule alone; doors/statics need a content table the server does not have | done (unit) | redteam-client.test.ts #364 |
| 365 | HIGH (cheat): ContainerOpen first-open is canonical for any never-opened ref (need not be a container): 64 stacks × 10,000 daedric → legit for friends via server take | worldstate.ts containerOpen first-open: a non-gold stack > 100 (MAX_FIRST_OPEN_STACK) refuses the open + anomaly container_first_open (gold keeps MAX_FIRST_OPEN_GOLD); the stack-COUNT cap is #344's | done (unit) | redteam-client.test.ts #365 |
| 366 | HIGH (grief): quest state from a guest — JournalEntry advances the host's doc to any stage (only regress blocked); GlobalVarUpdate any global (live to every human since #224); CrimeUpdate no raise-only (bounty 0 = party-wide forgiveness incl. recordCrimePaid; 1e9 = everyone wanted); a modified client never reports crime = invulnerable to law; MemberVarUpdate any local in own cell | quests.ts: CrimeUpdate from a human that LOWERS the shared bounty is refused (quest.crime_drop_refused, anomaly crime_drop) unless the player had DialogueLock traffic within 10 s (the fine is paid / jail served in the guard's dialogue on the client; the peer never lowers a bounty, so peer-only drops would mean no fine is ever paid). A GUEST's JournalEntry/GlobalVarUpdate (shadowed or world-shared) persists only while the guest holds a dialogue lock or within 10 s of lock traffic; otherwise relay-only + quest.guest_write_unbacked + anomaly guest_quest_write. The host's and the peer's writes are unchanged. MemberVarUpdate untouched | done (unit) | redteam-client.test.ts #366; questcredit/questpeer tests |
| 367 | MEDIUM (grief): DialogueLock on any NPC in any cell, unbounded, never auto-released (keyed on the claimed cell): one loop locks every NPC in the game | quests.ts dialogueLock: cellsVisible(player.cellKey, cellKey) or granted:false; a new lock request releases every other lock the player holds (one window at a time); connection.ts releases ALL of a player's locks on any cell change (the lock's cell was the client's claim) | done (unit) | redteam-client.test.ts #367 |
| 368 | MEDIUM (grief): ObjectEnabled far keys can hide any door/NPC/quest trigger for every entrant (#337 framed it as DoS only) | accept far ObjectEnabled only for script-toggled refs or from the holder | open | skipped in the ops pass: not trivial (needs a script-toggled-ref allowlist or holder-only far enables) and worldstate.ts farEnableAllowed is under concurrent edit |
| 369 | MEDIUM (cheat): skills/attributes any value 0..200 per key with no rate; level +4/10 s → 255 in 10 min; hp base 2000 in 5 min; reputation free-set | playerstate.ts: MAX_STAT_VALUE 100; a key may rise by <= 5 per 10 s window ACCUMULATED across declarations (two +1 skill gains in a window are ordinary; +5 per tick is not); reputation <= +10 per window; LEVEL_JUMP_LIMIT 2 (a jump of two is a declaration), one step per LEVEL_STEP_MS, and the step is stamped only for an ACCEPTED level (a refused jump no longer starts the window against the honest +1 after it). Refused declarations keep the server copy; the first declaration (no baseline) is chargen's | done (unit) | redteam-client.test.ts #369 |
| 370 | LOW: ResyncRequest{cellKey} reads any cell's full doc (canonical container contents everywhere) and creates docs for any key | worldstate.ts ResyncRequest: cellsVisible or dropped (object.out_of_reach); the peer is exempt | done (unit) | redteam-client.test.ts #370 |
| 371 | LOW: InviteSend to any account (not only friends), whisper to any in-world account — spam only | social.ts:460 | open | skipped in the ops pass: social.ts under concurrent edit; a per-sender invite/whisper rate cap is the trivial version when it is free |
| 372 | INFO: hp restore budget 4× max per 10 s (bounded, counted) — moot beside #359 | playerstate.ts:245 | moot (by #359) | — |
| 373 | Verified validated: peer identity (password fail-closed), harness auth off, resume tokens single-use, chat stamped + escaped, movement bounded, object reach, actor holder/epoch, combat proximity/veto/rate, global filters, record kinds, stats raise-only, rate/DoS buckets | — |
| 374 | RELEASE BLOCKER (TR on prod): prod players boot MP on the LOCKER path only — prod's deploy/Caddyfile @gateway proxies no /mwdata/*, /mwdata-manifest.json, /mwdata-mods.json; mountServerMods only runs on the hosted path; the locker refuses non-retail files → a TR peer refuses every prod client BAD_CONTENT | deploy/Caddyfile:107 add the mwdata paths (engine image bakes it); prod [setup] deliveryModel="serve"; install retail+TR via the dashboard over SSH | fixed | deploy/Caddyfile @gateway carries /mwdata/* + the two manifests (mirrors caddy-config.ts @mwdata), comment names the [setup] deliveryModel = "serve" requirement; the gateway's 404-unless-serve keeps the launcher's locker fallback |
| 375 | RELEASE: proto still 2 while the wire grew all day; prod does not pin the engine; deploy-ovh and deploy-mp serialise in undefined order (~35 min skew window) → an old client connects and misbehaves silently | connection.ts:1237 + net.lua:315 + bots/healthcheck.mjs:115 + test/helpers.ts proto=3 | fixed | proto 3 in connection.ts, net.lua, healthcheck.mjs, test helpers/enginepin/wsproxy; PROTOCOL.md SessionHello shows proto 3; subprotocol stays omw-mp.2 (engine rebake needed before a client can connect) |
| 376 | RELEASE: prod workflows trigger on the `ovhcloud` branch, not main; init:true/stop_grace_period only in the dev compose (prod: no init → orphan peers zombie); rollback keeps no previous image tag (tag :prev before pushing); purge list omits /launcher.html | deploy-ovh.yml:13, docker-compose.prod.yml:22, CLAUDE.md process | fixed | docker-compose.prod.yml init: true (stop_grace_period already there); deploy-ovh purge list + /launcher.html; CLAUDE.md: ovhcloud (not main) deploys, tag :prev before pushing |
| 377 | RELEASE (ops table): trustCloudflareIp only after the origin is firewalled to CF ranges (morrowind.caddy has no origin guard); startTimeoutMs must not be overridden at 120000; [notify] events override replaces the list; [content] enforce strict refuses locker clients (no sha256) | per key | ops note | TR-NIGHT-RUNBOOK.md §2 Prod config: trustCloudflareIp only behind a CF-firewalled origin, startTimeoutMs not overridden, [notify] events replaces the list, [content] enforce strict refuses locker clients |
| 378 | DASHBOARD BLOCKER: on the gateway after setup the Mods page is unreachable with no game open, and with one open it hits the WORLD's empty data dir (upload → "That upload has expired") — 2.7 GB upload then a false error | app.js:44 add mods to PLATFORM_PATHS; :186-187 Mod manager/Game files shared:true | fixed | app.js PLATFORM_PATHS += mods; #mods/#gamefiles shared: true; checklist item on the platform too; the gateway's own /admin/api/mods* routes come from adminRoutes({gameDataDir: gameDataDir(sharedDir)}) |
| 379 | DASHBOARD: no player password reset (only SMTP mail or --admin-reset on the box) — soft blocker for "I forgot it" | routes.ts: POST /admin/api/accounts/password (owner) + a row button | done (unit) | POST /admin/api/accounts/password (owner, policy, sessions revoked, admin.password_set) + a "set password" row button; admin-dashboard.test.ts |
| 380 | DASHBOARD: Unban from Accounts on the platform is dead (gateway action stub "Open a game first") — use the Bans tool inside a game meanwhile | gateway/admin.ts:199 unban via BanStore(sharedDir) | done (unit) | gateway action 'unban' -> new BanStore(sharedDir).unbanAccount + close(); other kinds still refused; admin-mp-dashboard.test.ts |
| 381 | DASHBOARD notes: restart banner restarts the whole gateway (offer #rolling); no log filter box; BAD_CONTENT refusals only in the world's log ring; simPeer.enabled is a cosmetic switch (DERIVED); world idle reap 120 s is CLI-only; rolling-restart copy says seconds (TR peer = minutes); install toast lacks the restart banner | per item | partial (unit) | a: restartPrompt on the platform says it restarts every game and links #rolling; b: Logs page `#logFind` box filters the 500 lines client-side (event + fields, case-insensitive); c: OPEN (design -- the gateway would need the worlds' rings); d: `simPeer.enabled` joins DERIVED_FIELDS (server.ts sets it from game data + binary); e: `[worlds] idleReapSec` (0 = 120 s default; `--idle-reap-ms` still wins) with help text; f: rolling copy says seconds for vanilla, minutes for a TR peer; g: the mod install toast is followed by restartPrompt(). admin-mp-dashboard.test.ts #381 |
| 382 | HIGH (TR): every shop run co-anchors interiors (anchorIdleSec 60 s, no count limit; the dummy falls back to a THIRD interior when everyone is indoors) — #265's "do not anchor >1 interior" is not enforced anywhere: NPCs walk into invisible walls in every TR city | server.ts:1560-1606 expire an interior the pass its last human leaves; never stand the dummy indoors | fixed | server.ts simPeerPass: an interior anchor is dropped the pass its last human leaves (occupied = the humans' cellKeys; exteriors keep anchorIdleSec); the dummy only ever stands in an exterior ([simPeer].startCell covers the cold boot). simanchors.test.ts: two humans in two interiors both anchor, one leaving drops its interior next pass, the dummy stays outdoors |
| 383 | MEDIUM-HIGH (TR): a TR region cold-load (synchronous 3x3 of dense exteriors) exceeds actorSilenceMs 15 s → the new wedged-peer restart (#324) kills a healthy peer and a TR reboot is minutes; grant→load→kill loop possible | authority.ts:58 actorSilenceMs 60-90 s; do not judge silence within N s of a SimAnchors change | fixed | authority.ts actorSilenceMs 75 s (config.default.toml actorSilenceSec = 75); noteAnchorsChanged(now) from server.ts on every SimAnchors set change and review() judges nothing within 60 s of it. authority.test.ts |
| 384 | MEDIUM (TR): quest enables into never-visited interiors are refused (farEnableAllowed needs knownCells) — cross-town TR enables work on one client only; same class as #213 reintroduced by #337 | worldstate.ts:1195 drop knownCells for interiors, keep the 64-per-session cap | fixed | worldstate.ts farEnableAllowed: an interior needs no prior WorldCellState (knownCells removed); an exterior still has to be well-formed inside the bound; MAX_FAR_ENABLE_CELLS 64 per session kept. worldstate.test.ts: an unvisited interior persists, the cap bounds the doc count |
| 385 | MEDIUM (conditional): isChargenCell is a substring match on "census" — any TR "Census and Excise" office becomes a sanctuary (never simulated, state never replayed) | movement.ts:48, global.lua:349, objects.lua:175 exact-match the two vanilla names | fixed | movement.ts / global.lua / objects.lua isChargenCell: `k == 'seyda neen, census and excise office' or k == 'imperial prison ship'` (lowercased, exact). The `?nomw` demo has no chargen cell (boots into the Village exterior). chargencells.test.ts: TR offices are not sanctuary; lua-tests source check |
| 386 | LOW-MEDIUM: a non-dialogue bounty drop is refused silently, host included (a script pardon): client keeps 0, peer keeps hunting, nothing corrects the client | quests.ts:683 echo CrimeUpdate{shared bounty} back to the sender | done (unit) | quests.ts crime(): a refused drop echoes CrimeUpdate {bounty: shared, shared: true} to the sender (the client applies it: no byId, so the own-echo guard does not fire). redteam-client.test.ts #366 asserts the echo |
| 387 | LOW: dummy parked at a human's arrival pose fires GetDistance-Player ambush scripts on the peer AND the client → double ambush packs (more TR triggers) | note |
| 388 | LOW: explored-map cap 1024 is FIFO — a Vvardenfell+mainland campaign loses Seyda Neen first | m7.ts:28 8192 | done (unit) | m7.ts MAX_EXPLORED 8192 (~64 KB per doc at the cap; the welcome record stays well under maxMsgBytes) |
| 389 | INFO: first TR boot streams ~1 GB over ~500 serial main-thread range requests (3+ min black screen at 50 Mbps; cached after); worker could prefetch chunk N+1 after a sequential miss | streamfs.js:135 | fixed | fetchChunkSync tags a miss that starts where the previous miss of the same file ended with `next` (the following chunk); the worker fetches it into the Cache API off the critical path, one outstanding, persistable (same-origin, unsigned) URLs only — same cache key as the read path, so a read of the chunk in flight awaits it instead of refetching. play-front-door "streamfs prefetches…" (static); the ~500-serial-requests measurement is the live check |
| 390 | HARNESS COLLISION (#362): human CombatHit refused under a holder kills every hitn:/hitp: scenario (s51 s66 s109 s110 s111 s112 s118 s119 s120 s128 s137 s138 s157 + s164's poke) | testhost.ts limits.harness seam consumed at combat.ts:360 | fixed | [limits] harness = false (config.ts, DERIVED, testhost.ts sets true for the direct and every gateway-spawned world); combat.ts skips the under-a-holder refusal when set |
| 391 | HARNESS COLLISION (#360): mpMintSpell mints cost=1; the cost floor refuses levitate 100×60 → s147 s148 s156 never register | global.lua:3353 cost = max(1, floor(mag×dur/100)) (bake) | fixed (bake) | global.lua mpMintSpell cost = max(1, floor(mag x max(1,dur) / 100)); lua-tests source check |
| 392 | HARNESS COLLISION (#363): follow:/escort: hooks send ActorAI with no dialogue lock → s114 s117 s123 | wrap in dlg:/dlg:release as s124 does | fixed | s114 s117 s123 hold dlg:<rec> across the follow:/escort: hook and dlg:release after, as s124 |
| 393 | HARNESS COLLISION (#369): setskill +7 (s132), setlevel steps of 4 (s143), setskill 100 (s164 — the avatar keeps a fresh Long Blade, misses) refused; real gap: handleNumberMap skips the check for a key omitted from the previous declaration (re-add at 100) | harness seam for budgets; s132 +5, s143 step 1 per 10 s; close the omitted-key gap | fixed | playerstate.ts: rate windows skipped under limits.harness (caps stand); an omitted non-damage key raises from 0; s132 +5, s143 steps of 4 with 11 s between |
| 394 | HARNESS COLLISION (#361): same-cell snapto > 1024 u refused (s112:47, s154:52, s114:52) and — REAL-PLAY BUG — neither InviteAccepted path stamps an "explained" timestamp: a friend accepting an invite from ≥1024 u away in the same exterior cell is refused and rubber-banded | connection.ts:1131 seam; stamp lastDoorAt on both InviteAccepted sends | fixed | connection.ts same-cell rule skipped under limits.harness; social.ts and server.ts stamp lastDoorAt on both InviteAccepted sends; socialux + onecharacteroneworld tests |
| 395 | #280 vs s154: guestSpawn returns null whenever positions[worldId] exists (any guest who ever flushed here) → a returning guest is never placed beside the host | server.ts:978 skip the spawn only when positions[worldId].at is younger than 60 s (a reboot) | fixed | server.ts guestSpawn: only positions[worldId].at younger than 60 s skips the spawn (GUEST_REBOOT_MS); onecharacteroneworld test gains the returning-guest leg |
| 396 | #366 vs #343: the arrest window sends no DialogueLock, so with crime shared paying the fine never lowers the bounty (guards re-arrest forever) | connection.ts:802 stamp target.lastDialogueAt on the PlayerArrest relay | done (unit) | connection.ts PlayerArrest relay stamps target.lastDialogueAt; redteam-client #396 test |
| 397 | Peer image skew: a hand-run harness on a peer image baked before proto 3 = BAD_PROTO everywhere; the Jenkins job rebuilds both | note | done (unit) | connection.ts handleHello: a SYSTEM peer on another proto logs `simpeer.bad_proto` {peerProto, serverProto, hint} and goes through refuseSetup, so the supervisor disables itself (`simpeer.disabled_permanently` BAD_PROTO) instead of respawning the old image every backoff; the detail names both versions. enginepin.test.ts (pre-proto-3 peer) |
| 398 | PROTOCOL.md drifted from the code across ~17 messages (pose window 300 ms vs 2 s; bars fresh 1 s vs 5 s; ActorAI documented shape does not exist; JournalSync borrowed/journalLog, CrimeUpdate raise-only/echo/byId, attackerId/fromId, first-open caps, DialogueLock gates, kd/blk/speed all undocumented; CombatProjectile and ObjectMove C→S dead; ~40 undocumented names) | PROTOCOL.md rewrite per the wire audit line list | fixed | PROTOCOL.md rewritten against the validators/handlers; "proto 3 (2026-09-15)" changelog lists every correction |
| 399 | A human ObjectSpawnRequest{actor} refused for reach/rate prints "That drop was refused." (tempId 0 falls to the drop notice table) | objects.lua:714 notice table for reach/rate | fixed | objects.lua notice table: reach/rate get their own sentences (bake) |
| 400 | StateRefused for PlayerMark / PlayerItemAcquired / PlayerActiveSpells is never re-sent (KIND_OF_EVENT lacks them) | identity.lua:619 | fixed | identity.lua KIND_OF_EVENT + PlayerMark/PlayerActiveSpells/PlayerItemAcquired; the acquisition baseline moved into `last.acquired` so it can be forgotten (bake) |
| 401 | ActorDisposition.ai from the HOLDER path is relayed unvalidated (the dialogue path clamps 0..100) — a compromised peer is trusted anyway, but clamp for symmetry | worldstate.ts:866 | fixed | worldstate.ts holder relay clamps every `ai` value 0..100 |
| 402 | ReportPlayer.voice is a dead field (Lua never sets it; server compares to the string "true") | social.ts / player.lua:583 | fixed | voice dropped from social.ts (interface + parse), server.ts report writer, global.lua mpSocial forwarder; PROTOCOL.md |
| 404 | BLOCKER: a returning character holding any player-made `mp_*` item had its doc overwritten with the engine template on login — restoreTick defers the grant until RecordsSync lands, but chargenTick sent MP_ChargenDone on the first Joined tick (skip-menu boot: chargenstate -1 from frame 1) → markBaselineReady → template inventory/level declared and accepted | global.lua chargenTick gated on `pendingRestore == nil` (declaration hoisted above it); player.lua `levelof` probe | done (bake) | run.lua source check; s153 now relogs twice and asserts item + level 2 survive the second. No server guard: the server has no signal for "restore applied" and a level DROP (5→1) is not what #369's raise/jump limits refuse — adding one means a new client message, not worth it over the client gate |
| 405 | BLOCKER: F5 during chargen made a permanent template — the stashed boot fragment dropped `mpnew`, so the reload booted `--skip-menu`, chargenstate was -1 on frame 1 and ChargenComplete was accepted (frontdoor needsChargen false forever) | index.html bootfrag stash keeps mpnew; the chargenDone watcher strips it from sessionStorage once `chargenDone === '1'` | done | play-front-door.test.ts static check. No server guard: mid-chargen reconnects legitimately re-present a provisional slot (connection.ts:1407) |
| 406 | #317 dead code: launcher's `&mpstart=` → `--start <cell>`; an unknown cell dies in the engine's own "Failed to start new game" message box (statemanagerimp.cpp:200 catches it), so fatalOverlay's `/cell .* not found/` reboot-once retry was unreachable | launcher.html emission deleted; index.html `--start` arg and the fatalOverlay retry deleted (__omwAwaitRestore holds the screen; restoreTick's respawn fallback covers a missing cell). The fragment-rebuild filters still drop a foreign mpstart key | done | play-front-door.test.ts static check; grep: no other reader of mpstart in play/, server/, harness |
| 407 | first-dial 60 s grace never honoured: `not everJoined and reconnectAttempt >= UNREACHABLE_ATTEMPTS` ran before the switchDeadline branch → three refused sockets (~7 s) dead-ended at UNREACHABLE while the world was still booting | net.lua: the UNREACHABLE give-up defers to `switchDeadline`; first-dial grace 45 s (a switch keeps 60); s90 joinTimeoutMs 150 s (boot + 45 + one 30 s jittered redial) | done (bake) | run.lua source check; s90-server-down (runs on the builder) |
| 408 | a stale tab's PARKED resume ticket supersedes the new boot: finishAuth superseded the live session only; the old token resumed inside the window and kicked the fresh session | resume.ts `revokeChar(charId)` (per character, #124); connection.ts finishAuth calls it | done (unit) | admin.test.ts "a fresh login of the character revokes the parked ticket of its old session": old token → AUTH_FAILED, fresh session not superseded. Replaces "resuming an account connected elsewhere still supersedes", which asserted exactly this bug |
| 409 | openmw.data download failure hung silently: emscripten's loadPackage rejects with `Network Error: …/openmw.data` / `<status>: <url>` and nothing else fires | index.html unhandledrejection → `/openmw\.data/` → fatalOverlay('Could not download the game') | done | play-front-door.test.ts static check |
| 410 | `FS.syncfs(false, cb)` callback ignored err — a save that never reached IDBFS was silent | index.html: `function(err){ if (err) append('[syncfs] ' + err, 'err') …` | done | play-front-door.test.ts static check |
| 411 | A lock-less `crime` combat claim (#146) had no rate bound: a modified client aggroes every witness in a visible cell onto itself | worldstate.ts | fixed | crime claims share #363's 5/min bucket (claimWithinRate); redteam-client.test.ts "#146 crime combat claims are rate-bounded" |
| 412 | ActorSay (#217) relayed any holder's unbounded strings: a human holder in degraded mode pushes free text as an NPC subtitle | worldstate.ts | fixed | system peer only, file 128 / text 256 clipped; same test |
| 413 | The script-note drain is one burst at join: 30 s of a town's AI package completions (aidone, #221) become one message each on the first joined frame and the session bucket (60/s, burst 60) kicks the client with RATE — s109 in #102, 70 ms after join_world | global.lua scriptNotesTick, puppets.cpp recordScriptNote | fixed — bake needed | notes queue and go out at 20/s; C++ keeps one pending aidone per ref; run.lua source check |
| 414 | The peer's held-cell script watch (#217) called next() on script.variables, a userdata: global onUpdate threw every 5 s on the peer (116 Lua errors in s114, #102), so every tick after armHeldWatches was skipped that frame — follow claims never reached the puppets | quests.lua armHeldWatches | fixed — bake needed | pcall'd pairs() probe; run.lua source check pins next() absent |
| 415 | The #270 governor refused a friend's world with reason 'cpu' whenever loadavg(1) > cores — true for a minute after any bake or TR peer cold start (s102 in #103: 503 on the second world) | worlds.ts capacity() | fixed | cpu rule gated on [worlds] loadCap (0 = off, default); mem rule unchanged; worlds.test.ts sets loadCap: 8 |
| 430 | The peer's own body dies: it stands where a player stands (MP_SimAnchors place), so s149's drowning player drowned it and s164's kwama bit it; its PlayerDeath ran the respawn plugin, which parked it at the harness's sea cell 26,25, where it held the cell, rolled slaughterfish and died every 30 s (player.death simpeer-world x2 per scenario in #105). The fish drifting into 26,24 made noteCombat's "fights ME" exception fire on the peer for a cell it does not hold — `actor.dropped … combat claim without the conversation` from simpeer-world, 5 per second (s164). The drop itself costs nothing (the fight is with the dummy); the death loop is the bug | player.lua inputTick, actors.lua noteCombat | fixed — bake needed (peer image) | the system peer keeps itself in god mode (openmw.debug, player-only in the engine: avatars and NPCs untouched); a non-holder combat claim never leaves the peer; run.lua source checks |
| 431 | s157 in #105: the restarted peer loads the cell alive and its first ActorStatsDynamic (hp 57) for the recorded corpse reached every puppet a round trip before the post-load ResyncRequest brought the death back and killed the body — B read ALIVE(dead=true,hp=57) for one sample, then dead/dead for 18 s. The grant's own ResyncRequest is answered before the cell is loaded, so its keys resolve to nothing | actors.lua broadcastCell / noteCellDeaths, objects.lua MP_WorldCellState | fixed — bake needed (peer image) | the holder streams no bars for a cell until the WorldCellState that answers the post-load request has arrived (cell.recorded); the record is handed over even when it lists no deaths; run.lua source checks |
| 432 | s59 in #105: 14 real Fire Bite casts at "teleri helvi" and the peer log says nothing — MP_CombatSpellHit printed nothing, so arrival, resolution and the bar are all unobservable. Likely cause: the victim is a Dunmer (Resist Fire 75 %) and wanders (a snapto every ~11 s), so the touch casts that land do ~5 hp; the one `assault bounty +40` at 05:18:20 says at least one hit did apply on the peer | combat.lua MP_CombatSpellHit | diagnostic — bake needed (peer image); the scenario is still open | the peer prints spell/target/resolved/hp per forwarded cast, like CombatHit; next sweep decides between "never arrived" and "resisted" |
| 433 | s62 in #105: `B bounty matches (CrimeUpdate + setCrimeLevel)` timed out because crime became PERSONAL by default (#353, dd6be1ea): the server keeps A's bounty on A's doc and relays nothing, while s62 asserts the shared record's relay | s62-questvars.mjs | fixed (scenario) | `serverRules = [sharing] crime = true`, as s125 already does |
| 403 | HIGH (cheat, closing audit): quests.ts stamped player.lastDialogueAt BEFORE the visibility/held refusals, so a REFUSED DialogueLock (far cell, held by someone else) still bought the 5 s same-cell teleport pass (#361) and the 10 s bounty-drop pass (#366) | quests.ts dialogueLock: the stamp moves to a GRANTED lock or a real release only | done (unit) | redteam-client.test.ts #403 (far-cell refusal then a 3000 u same-cell jump refused and a CrimeUpdate drop not relayed; a lock held by another player likewise) |
| 420 | Every item-state reader used `obj.itemData`, a FIELD -- mwlua/itemdata.cpp binds it as a FUNCTION on types.Item, so it was nil on every GameObject: no condition, charge or soul ever left a client (identity itemState, the drop's state, the avatar report, the restore/peer appliers, setcond:). #105: s160 probed nil/nil/nil, s163's `own` tag went out stateless; rows 39/233/234 were dead code with green unit tests, whose fake items carried the field | identity.lua, objects.lua, global.lua (applyItemStates, snapAvatarItemStates, mpSetItemCondition), player.lua itemstate: | fixed -- bake needed | types.Item.itemData(obj) at all 7 sites; stubs.lua Item.itemData; run.lua source check pins no `.itemData` property read remains |
| 421 | s163 asked for `pick_apprentice`, which Morrowind.esm does not have (LOCK ids: pick_apprentice_01, pick_journeyman_01, pick_master, pick_grandmaster, pick_secretmaster): give: created nothing and the failure read as a lost grant | s163 | fixed | ITEM = pick_apprentice_01 |
| 422 | The example suite's player template ships Acrobatics 125, so the ?nomw bot's raw-template PlayerSkills is refused whole on its first join (state.invalid_body x4, #105 s159). Raised to 255 in #106 → the template's skills (all 5) were stored and restored OVER the party world's class kit (s132 red). | playerstate.ts MAX_STAT_VALUE | wontfix (by design) | bound stays 100 — a player's base never passes it; the refusal of a demo template is harmless. redteam-client.test.ts: 125 refuses the map, 100 lands |
| 423 | #391 priced a minted 100x60 self spell at 60 magicka, but setmp:300 is a raise claim the server caps at the BASE (~40 fresh) and the peer's avatar bar overwrites the local pool at 4 Hz (MP_SelfStats), so every press said 'not enough magicka' (#105 s147/s156: three presses, actives unchanged) | player.lua, s147, s156 | fixed -- bake needed | setmpbase:<n> hook beside sethpbase; castSelf steps the base +60 (one legal MAX_BASE_STEP) and waits for the peer to report a pool that funds the cast |
| 424 | s165 stepped the health base 35->300 in one sethpbase; the server's base_step cap is 60 per 10 s and the harness seam relaxes only the windows, so the max never rose (#105: implausible_gain base_step, 30 s timeout). The seam is right; the scenario was not | s165 | fixed | base+60, the untouched pool, once = +50 and a doubled heal pins at +60 |
| 425 | s161 traded with shared[0] = 'chargen boat guard 1': nobody outdoors at Seyda Neen has a barter purse (Arrille is indoors), so a sale could never LOWER a purse of 0 (#105: 'purse 0', timeout). The stock watch proves the long sale; the purse delta is now proven by a BUY of the guard's own ware (0 -> value), and B reads that figure | s161 | fixed | scenario only |
| 426 | s149 (#105): A's console fell silent at 04:29:07 after streaming three fresh exterior cells (-4,-8..-10) and the next frame had not come 73 s later when the 60 s cmd deadline fired on sethpbase -- no Lua error, no crash, one synchronous-fetch frame on a box running two retail Chromes and the peer (#185 again, one size up) | mp-harness.mjs handle.cmd | fixed | 120 s deadline (CDP round-trip is 300 s) |
| 427 | s159 (#105): strength damage 15 read back 0 after the relog. The server half is unit-proven (a PlayerAttributes map with strength_damage survives a relog into the Welcome record: staledoc.test.ts); the client half cannot be told from the log (an assertion failure prints no client console). The scenario now prints the doc's attribute map and the relogged client's console tail on failure | s159 | open -- diagnosed next sweep | which half: doc without strength_damage = the first session never declared it; doc with it = applyPhase2 did not stick |
| 440 | s160 and s163 in #106 failed on the value they asked for: "the wear did not take on A (100.0/nil/nil)" after setcond:100, "the use did not take on A (10.0/nil/nil)" after setcond:10. player.lua's itemstate: answer is tostring() of the engine's numbers, so a condition of 100 reads "100.0" and `startsWith('100/')` is false. The write, the read and the doc were all correct; only the comparison was | s160, s163 | fixed (scenario) | both compare `Number(state.split('/')[0])`, so either format passes; the drop/refresh halves of those scenarios finally get to run |
| 441 | s149 in #106 timed out on "the max rose" and saw none of the drowning the peer was simulating (the peer's own log: hp 33.5 -> 27.6 -> 0). The server had dropped every bar report: `simpeer.avatar_stats_gated lastInputAgoMs 5895` (8065 in s143). A streamed retail harness bot renders at ~1 fps and its 30 Hz input tick renders with it, so the gap between two input frames passes INPUT_DRIVING_MS (5 s) � the peer's reports were gated AND handleStatsDynamic took the client-authoritative branch, which sends no AvatarRestore, so the claimed base never reached the avatar either | playerstate.ts drivingWindowMs | fixed | the driving window is 60 s under `limits.harness` (the seam that already relaxes the rate windows; the authority rule is untouched), for both the bar and the item-state batches; avatarstats.test.ts pins both halves � a harness bot one input frame old still gets SelfStats, a real one does not |
| 442 | s165 in #106: "the heal never landed (35 -> 35)" with the client's OWN bar also at 35 � no server rule can move that, so the cast never happened. The scenario pressed the use key exactly once; s147 and s156 both need two or three presses on a 1 fps client before the spell is in `actives` | s165 | fixed (scenario) | presses up to four times until `actives` names the minted spell (the s147 castSelf idiom) and fails with the actives list if it never takes |
| 443 | s143 in #106 read "rolls: none" twelve seconds after the guest joined: its wait was `netObjects > 0`, which any netted object satisfies (an item on the ground in the cell the guest started in), so the log was read before the peer had loaded the guest's destination cell and rolled anything there. The party level itself reached the peer correctly (`party level -> 13`) | s143 | fixed (scenario) | waits for the guest's cell to be -2,-7 (as s166 does), then polls the gateway tail up to 180 s for a `rolled at level N (party leader)` line � the measurement, not a proxy for it |
| 444 | s166 in #106: "the retreat did not open the gap (NaN u)". The provoking Fire Bite (15-30) killed the mark � an 8-health scrib, `world.kill "scrib"` in the same second � so the gap probe returned Infinity, which is not JSON: it crossed the eval boundary as undefined and printed as NaN | s166 | fixed (scenario) | provokes with a 1-point relay sting (s138/s164's idiom: it cannot kill and still starts the fight), asserts the mark survived it, and the gap expression answers -1 instead of Infinity so every message names a number |
| 445 | s162 in #106: "the walk barely moved (85 units)" with no rubber-band at all (worst step back 0.0). The engine integrates movement per FRAME with a clamped delta and the bot renders at ~1 fps, so 4 s of wall clock bought ~0.8 s of walking � the same clock skew s149 already allows for by holding its breath 240 s for 20 s of game time | s162 | fixed (scenario) | walks 15 s; the rubber-band and settle checks read the same samples |
| 446 | s47 in #106 died at `eval(window.omw.state.joinError)`: "Cannot read properties of undefined (reading 'state')". Joining a world is a page RELOAD since rebootIntoWorld (nothing of the old world's engine state may leak into the new one), so two seconds after the join there may be no client on the page at all; the scenario's comment still said "no page reload" | s47 | fixed (scenario) | the four diagnostic mirrors are read defensively and log "(reloading)"; the verdict was always the destination world's playerCount |
| 416 | THE AVATAR NEVER RULED AFTER A SNAP (s138/s145/s162/s164/s166 in #105, the #100/#183/#232 "why does a standing client stop driving" family): connection.ts stored the PlayerInput ENVELOPE seq as player.inputSeq and armed teleportPose.seq from it, but the envelope counter (netmanager.cpp mSeq) is shared by every binary frame the client sends and runs 2-3x ahead of the payload seq player.lua stamps and the peer echoes as lastInputSeq -- so `lastInputSeq > teleportPose.seq` took minutes after any cell change and never came true on a ~1 fps harness client (s138: avatar teleported ok=true, standing 396 u from the target, seq 95 vs an envelope seq in the hundreds; simpeer.avatar_never_arrived every 30 s until the deadline). Not a client bug: inputTick is unconditional in onFrame, the forward to the peer is not gated, the avatar's stream carried the right seq | connection.ts handlePlayerInput: the seq is read from the payload (u32 at 0), the envelope seq is not used | fixed -- no bake (server only) | inputauthority.test.ts #416: envelope seq 5000 ahead of the payload seq, a same-cell snap, the owner state batch must arrive (red on the old code: timeout, green now); the seq halves in a01feded's never_arrived line now agree by construction |
| 417 | s123 in #105: the escort claim was dropped as `follow claim without the conversation` 0.5 s before the scenario had even logged asking -- the old scenario guessed 1.5 s for the lock grant while the dlg: path is three frame hops (player cmd -> global event -> activateBy -> onNpcActivate -> DialogueLock) at ~1 fps, so the claim overtook the lock request; a01feded already waits for the grant, and the release could still beat the claim (companion.lua polls at 1 s, three hops after the escort command, against the 5 s recentlyHeld grace) | s123-escort.mjs | fixed (scenario) | s123 waits for the followClaim mirror to read `sent:` before `dlg:release`; LIKELY, not proven: #105 lacked the key/holder fields a01feded added, so a rerun that still drops names which half |
| 450 | s170 phase 4 launcherSignIn timed out on `the launcher answered the sign-in`: after the password POST the launcher sets `#mpticket=...` and calls location.reload(), but play/server.py's gateway relay had spliced the WHOLE keep-alive connection to the gateway. The browser was told keep-alive (the gateway's own response header, relayed verbatim), reused the socket, and the reload's GET /launcher.html was pumped straight into the gateway -- which 404s every path it does not own ("not found"). The reloaded document was the gateway's error body, so the ticket-return path (launcher.html handleSsoReturn ~1575) never ran, onboarding/character-select never showed, and neither ob-bd/cs-bd/onGamePage ever came true. Server side was proven healthy (a booted gateway returned needsProfile:true for the password login) | play/server.py _relay_to_gateway | fixed -- no bake (test play server only; launcher.html served static) | Force `Connection: close` on non-upgrade relayed requests so the gateway closes after one response and the browser opens a fresh connection for the reload, which server.py serves statically; WebSocket (/ws, /w/) upgrades keep the bidirectional splice. Reproduced with headless Chrome driving launcher.html against a gateway booted on the builder: before, the reload's GET /launcher.html = 404 and ob-bd never shows; after, reload = 200 and the onboarding modal shows |
| 451 | RELEASE BLOCKER, found by the s170 fresh-install rehearsal: a wizard-provisioned server closes registration (the owner creates the accounts), and the sim peer has no account until it registers one — SessionRegister refused "registration is disabled", the login fallback found no account, and the world simulated NOTHING (no NPCs, no combat, every client held on the loading screen waiting for a SimReady that could not come). The suite never saw it: it pre-seeds auth. | connection.ts handleRegister | fixed | the allowRegistration and inviteCode gates exempt a peer that already matched [server].password in checkAuthGate (same rule as the SSO gate above them); simready.test.ts "the sim peer can register on a server with registration closed" — the peer gets in, a human is still refused, and a stranger declaring system=true without the password is still refused |
| 452 | The harness LEAKS a browser per client and a peer per scenario: Chrome was spawned without its own process group, so SIGKILL on the launcher pid left its gpu/renderer children running, and the container had no init to reap them. Sweep #107 reached 1847 chrome processes, 2422 zombies and load 39 on a 32-core box by its 80th scenario; every timing assertion after that failed for reasons unrelated to the code (and #103's s114/s123/s138 reds were the same thing) | mp-harness.mjs, ci/jenkins/run-harness.sh | fixed  | Chrome spawns detached and close() kills the GROUP; a hand-spawned peer is pushed to ownedChildren so the finally reaps it; a watched child (a gateway, whose WORLDS are their own process groups and own the peers) is SIGTERMed and given 3 s to stop them before the group SIGKILL; the harness container runs with --init; a settled per-scenario process count prints `[harness] LEAK after <scenario>` when either count climbs |
| 453 | A recruited companion NEVER MOVES when the follow claim lands while the leader is far away (peer log: `Follow on peer: indrele rathryon leader=Generated:0x10 at 1025; dest 1025 away; pos=(-12239,-70993,93)` repeating every 5 s with the position frozen). AiFollow::execute gates the package: while `!mActive` it only ever turns activation ON, and only while the leader is within followDistance + 384 and in line of sight -- miss that window once and the package is inactive for its whole life. Single player recruits face to face; here the claim goes client -> server -> holder and companion.lua polls at 1 Hz, so the player can walk or teleport out of range before the package is issued, and the follower is rooted for good. | openmw/files/data/scripts/mp/companion.lua | fixed -- NEEDS AN ENGINE BAKE (client Lua is baked into openmw.data) | companion.lua re-issues `StartAIPackage{Follow}` from its existing 1 Hz poll when the actor has NOT moved since the last poll and the leader is now within 450 u, so the engine runs its own activation check again; bounded to MP sessions (mp.isEnabled), one attempt per 3 s, and only while stationary. AiEscort is NOT re-issued: aiescort.cpp re-tests isInEscortRange every frame and resumes by itself, so an escort's destination is never touched. Proof: four source checks in wasm-build/lua-tests/run.lua pinning the condition, the bound and the Escort exclusion; s114/s123 recruit at ~80 u and pass either way |
| 460 | s150 `sleeping 8 h healed nothing that stuck (35 -> 35)`: red in #110/#111/#114, green in #105/#106/#107. The sethpbase claim reached the avatar (35/95), the sleep ran (`time.advance hours 7.97-7.99 reason rest`), no `state.` refusal, no AvatarRestore, and the peer reported 35/95 for the whole 40 s. a5b82ebb blamed the frame order (an INLINE rest in onFrame overwritten by the report write queued for applyDelayedActions) and queued the rest; #114 ran that engine and is still red, so that was not it -- and the data never fitted it: the passes are the sweeps whose box was drowning in leaked chrome (#105-#107, load 39, sub-1-fps clients, the sleep 15-18 s after the guest's screen cleared) and the fails are the clean-box sweeps after the leak fixes (9.9 s after, three times) -- a race on the report phase would go the other way. A frame-order model with the real identity.lua (the shape of run.lua #461) puts the queued rest's +24 into a claim in every report phase, so the one client-side path left that drops a banked gain is the stale-peer branch of identity.tick: a report processed in the rest frame's update (pending write of 35, +24 banked by notePeerBars), then >= PEER_RULES_S (5 s) before the next onFrame, and the tick takes the client-authoritative branch, ZEROES the gain and sends the bar it can read -- the pending 35. #114's own clock jump measured 3.8 s between the two global ticks around the rest (#110/#111: 0.8 s); the frame's tail is not in any log | identity.lua, playerstate.ts, s150 | confirmed from #115: the host STOPPED after the claim (478's shape); the heal ladder is exonerated end to end | the claim carries the gain as `d` in both branches (461), so a stale-branch snapshot says the +24 and the server (still inside its own window) applies it; s150/s165 print the client's own bar and the last hp claim (`hpClaim`, `<bar>+<gain>`) beside the peer's word. #115 (that engine): `peer says 35/95; the client's own bar says 59, last hp claim 59+24.0` -- the engine healed, identity.lua measured +24 and BUILT the claim (the mirror is set in the same block as the send). What rules out every server and peer candidate: the peer reports 35 at least every 3 s and MP_SelfStats writes `health.current = 35` unconditionally, yet the host's own bar still read 59 after the 40 s poll -- no report was applied to that engine after the claim tick, i.e. the host took no frame (or no socket read) from then on; with no input frame the gate would only have logged at 60 s, past the 45 s the run had left. The server's log for the block carries no `state.invalid_body`, no `state.implausible_gain` (warn prints on the testhost) and no `simpeer.avatar_stats_gated`; avatarstats.test.ts `the s150 ladder` drives the exact wire sequence (35/35 report, `{c:35,b:95,d:0}` base step, 35/95 report, `{c:59,b:95,d:24}` bare and as the full stale-branch triple with the client's own mp/ft) and both land AvatarRestore 59/95 inside the base-step window -- so handleStatsDynamic is not where it goes; the peer's MP_AvatarRestore -> mpAvatarStats channel is the one s146 walked green in the same sweep from the same 35/95. Whether the claim left the socket is unknowable without the client console (a89876c6 now dumps it on every FAIL). s150 reads the host's actorBatchesIn across the sleep (s114's liveness signal) and a stuck count with a stuck bar fails as `the host's engine stopped after the sleep` with jsErrors/luaErrors/tail, not the heal verdict. No bake needed for this; the open question is 478's: why a harness client's main loop dies mid-run -- #116 (host alive, 105 frames): own bar 35, claim 35+0: the rest and a report share one frame about a third of the time at 1 fps and the report's write wins the read whichever order the two land in. Third try: restHours rests inline and RETURNS what it healed; player.lua banks it into identity (bankGain) and nothing is read (run.lua #460/#461). Bake |
| 461 | s165 `the heal never landed`: +6 (#107), +8 (#111), +10 (#114) of the 50 a minted 10x5 restore gives, with the client's own bar pulled down to the peer's each time. CONFIRMED by running the real identity.lua against a model of the peer round trip (run.lua #461: the same model lands 49 of 85 with the old shape, 85 with the fix). The claim ladder: identity.lua claimed `last report + gain` and zeroed the gain; playerstate.ts applied a claim only if it was ABOVE the doc. The first raise lands, the doc moves, and the avatar's report saying so takes a round trip -- every claim in that window was computed on the OLD report, sat below the doc, was ignored as an echo, and its gain was gone. Lossless only when the client is so slow that one claim carries the whole heal (the 1 fps sweeps). s112/s144/s146 stayed green because they assert `> before`, not the magnitude, and s144 is the helper's spell applied on the peer, not a claim | identity.lua, playerstate.ts | fixed -- bake needed (client Lua); server half is unit-tested | the claim carries the GAIN as `d` beside `c`; the server adds `d` to what its doc holds (gains only for hp/ft, net for mp, capped at the base, the restore budget unchanged) and falls back to `c` for a client without it. avatarstats.test.ts pins it; run.lua #461 runs the real identity.lua through the round trip |
| 462 | s31 in #107/#111 (green in #91): `chest holds 1 item on A` timed out with A holding 0 -- the put worked. The test item is MINTED (?nomw has none), so on A it is a runtime `Generated:` record while the wire, the ContainerUpdate echo, the containerItems mirror and B's take all use the server's `mp_armor_n` id (objects.lua countsToItems / sendContainerOp toNet since 265e4ac2, Sep 15). The scenario asked the mirror for the local id: the s136 trap (#127) | s31 | fixed (scenario) | resolves the wire id from `netRecords` (waits for the registration), checks the mirror by it, and both takes (chesttake goes out by net id untouched: sendContainerOpByNet) name it |
| 463 | s164 in #107/#111: `no living named creature in the probe: []` with three scribs netted in -2,-7 (world.spawn netId 2-4). `netObjects > 0` and `puppetedActors > 0` are true the moment the peer's spawn is netted; actorProbe is a periodic mirror of the OWN cell's actors (actors.lua), rewritten a tick later, so the read landed on an empty probe. s166 shares the read | s164, s166 | fixed (scenario) | both wait for a living netted record to appear IN the probe (the s143 idiom: wait for the thing, not a proxy for it) |
| 464 | s166 in #107/#111: `the retreat did not open the gap (104 u / 227 u)`. The mark is provoked BEFORE the retreat and a rat covers 200+ u in the 2.5 s the scenario slept after the snap: the chase under test had already eaten the gap the scenario then asserted was open | s166 | fixed (scenario) | measures the gap the moment the pose mirror shows the retreat and asserts only that the mark is outside CLOSE (150 u) -- the 15 s close is the measurement |
| 465 | s162 in #111 (green in #107): `divergence after the stop stayed at 65.9 (want < 10)` with `walked 753; divergence 0 0 93 257 385 498 681 681 681`. The avatar on the native peer walks at wall-clock speed while the ~1 fps client integrates 0.2 s a frame, so the avatar ends the walk a few hundred units AHEAD (the same skew as 445, from the other side) and the settle is one 25 % correction (48 u cap) per FRESH sample -- one per frame on this client (onSelfState needs a new input seq). 2 s + 6 x 0.5 s read the gap mid-convergence; #107's slower client had been snapped forward (settled 0.0) | s162 | fixed (scenario) -- LIKELY | the settle loop runs up to 20 s; the < 10 bar is unchanged. Not a product change: a real client integrates the same wall time as the avatar |
| 466 | s151 in #111 (green in #107): `A walked 80 units while over-encumbered on their own screen`, B saw the avatar cover 0, divergence 63. The local engine refuses to walk an over-encumbered body (Npc::getSpeed 0 past capacity), so the 80 u on A's screen is a correction or a snap toward an avatar that was not where A stood -- which the log cannot separate (#107 measured 3 u at 1 fps, where the avatar had been snapped to first). OPEN -- diagnosed next sweep | s151 | fixed (scenario) -- #114 sampled it: A own pose -69676 -> -69752 -> -69795 -> -69764 while the avatar on B stood at -69727, i.e. reconciliation closing a 78 u gap the unburdened walk left and OVERSHOOTING by up to 68 u; the scenario now settles (divergence < 10) before loading up. The overshoot itself is a reconciliation quality item (a stationary player nudged +-70 u), same skew as 445/465 | samples A's own pose, the avatar on B's screen, selfDivergence, selfSnap and selfStale every second through the encumbered walk |
| 467 | s155 in #111 (green in #107 at 31/31/13/26 u, a 30 u bar): `A could not walk in the chargen cell (best 0 units)` -- 0 u in all four directions, divergence 0, NO avatar on the peer (the sanctuary held), and one same-cell PlayerCellChange (a >256 u single-frame jump) announced during the first walk. Not the scenario's hypothesis: whatever moved A back is on A's own engine (a restore hold is impossible for a fresh character: no record). OPEN -- diagnosed next sweep | s155 | diagnostic | prints uiMode / lastKey / selfSnap / selfStale / restorePos / restoreFired / cell / state and the client console tail before the verdict |
| 468 | s149 in #105/#107/#111, identically: A's console falls silent after the third local-map draw that follows streaming the three fresh sea cells (-4,-8..-10) west of the seabed spot (-3,-9), and no frame runs for the rest of the scenario -- the 120 s cmd deadline (426) fired 134 s into the silence in #111, so this is a client HANG at that spot, not a slow fetch. The peer meanwhile drowned A's avatar to death at 22:44:26 (its own drowning path works). No Lua error, no crash line. OPEN -- engine | s149 / the wasm client at (-19264,-72128,-400) | open | reproduce natively or in a desktop browser: snapto that spot from Seyda Neen with ?stream and watch the frame counter; a scenario-side dodge (a deep spot inside an already-loaded cell) needs a depth measurement this box cannot make |
| 469 | s43 in #107/#111: `roster reached 49/64 OTHER players and stopped there` -- the 50th human's socket closed at hello (`socket closed while waiting for json SessionHelloOk`) 2 s into the last wave at load 15, so not the starved-bot theory the scenario carries. Deterministic at 49 others + the observer = 50 humans; the failure block prints no server tail (server.logTail was empty for this scenario), so the refusal code is unknown. testhost's configOverride (maxPlayers 64) wins over the scenario's `[server] maxPlayers = 144`, which is still above 50. OPEN | s43, testhost.ts | fixed -- #114 printed the close (helpers.ts closeInfo):  `1000 SERVER_FULL after SessionDisconnect SERVER_FULL` at 58 others + the observer, i.e. testhost's silent `maxPlayers 64` configOverride beat the scenario's 144 and ~5 slots were parked sessions from earlier waves; testhost now overrides maxPlayers only with an explicit --max-players | next sweep needs the server's own line for that connection (SERVER_FULL / IP_CAP / hello deadline): run s43 alone with the server stdout captured, or count roster.byId for parked sessions the earlier waves left behind |
| 470 | s74 in #111 (the first sweep to reach the s7x tail): `EACCES: permission denied, open /repo/minimap-before-walk.png`. The diagnostic wrote its shots to the checkout ROOT so a person could open them; the harness runs as the jenkins uid and the bind-mounted checkout root is not writable by it (harness-out is) | s74 | fixed (scenario) | shots land in wasm-build/harness-out/, mkdir -p first |
| 471 | s99 in #111 (first sweep to reach it): `timeout (5000ms) waiting for: T opened the chat panel`. The overlay poll absorbs every openChat/openSocial signal while !ready, and ready is chargenDone === '1'; the feature tour the scenario dismisses first shows on Joined, which is EARLIER (chargenDone waits on the restore, 404). The T landed in that gap and was swallowed by design | s99 | fixed (scenario) | waits for chargenDone before the keypress |
| 472 | s998/s999 in #111 (first sweep to reach them): the temporary no-peer CONTROL arms of s148/s149 left behind by a01feded. s998 equipped light_torch_01 -- a wall sconce without the Carry flag, which s148 had already replaced with `torch` (`No suitable slot for (Light, light_torch_01)`); s999 was s149 without a peer. Neither measured anything the real scenario does not | s998, s999 | fixed (deleted) | s148 green in #111; s149 is 468 |
| 473 | s114 in #112/#113 and s117 in #113 (both green in #111): the peer accepted the follow claim and logged `aimAt eldafire -> Generated:0x10 at 1135 u` / `1063 u` / `teleri helvi ... 1081 u`, then `Follow on peer: ... pos=` frozen for 90 s. NOT a stream fault: at the end of the run A's, B's and the peer's copies agreed within 10 u (2366/2355/2362 from the walk-off point), and the y of A's second read matched the peer's to 21 u. The pre-dialogue probe is STALE: an in-suite client runs at a fraction of 1 fps, so the puppet body (near tier, steered/snapped per frame) shows the holder's NPC seconds late, and the snapto's PlayerCellChange reached the server 3-7 s after the command (avatar follow-teleport at 03:54:10.9 for a probe read at 03:54:04.2). A wanderer walking at ~140 u/s covers ~1000 u in that window; fargoth/erene llenim passed because they stood still. The snap-then-re-read of 941db706 could not help: the re-read was just as stale. And the NPC keeps wandering on the peer while the client's game is paused in the dialogue -- in single player the world stops for a conversation; in MP nothing holds the NPC (no product change here: a real client lags ~100 ms, and a companion left behind activates when the player walks up, which is what the scenario now does) | s114, s117 | fixed (scenario) | after the claim (Follow stacked = the NPC stands still, aifollow.cpp !mActive) `_companion.mjs goToCompanion` waits for A's copy to hold still, snaps A beside it and holds 10 s for the avatar's late teleport and AiFollow's 0.5 s check; s117's B follows from the same spot so both take the same door. Confirming evidence on the next run: the `settled ... u from the pre-dialogue read` line |
| 474 | s99 in #114 (with 471 in): still `T opened the chat panel` timeout. The overlay flips `ready` on its OWN poll tick after the chargenDone mirror, and opens a SECOND tour (`unlocked`) the moment it does; the T landed between the mirror and the tick | s99 | fixed (scenario) | waits for the chat element to be shown (chatReady), closes whichever tour is up, sleeps past the 800 ms signal suppression, then presses |
| 475 | s164/s59/s166 in #114: kills and chases that were WORKING ran out of budget -- s164 27 swings in 90 s with the forager at 3 hp (31 -> 3), s59 20 casts in 150 s with 6 landing on the peer (40 -> 14 hp), s166 the forager closing 422 -> 207 u in 15 s (a kwama forager walks ~15 u/s) | s164, s59, s166 | fixed (scenario) | 180 s / 300 s / 30 s; the assertions (dies, dies, closes to 150 u) are unchanged |
| 476 | s42 in #114: `clients diverged before any crowd load: 138.6 units (vodunius nuccius)`, never under 80 in 30 s. Two probes one mirror tick apart on clients at a frame a second: a wanderer mid-stride reads 100+ u apart from sampling phase alone | s42 | fixed (scenario) | worstDisagreement compares only records that stood still (< 5 u) between two reads on client 0 |
| 477 | s97 in #114: `Runtime.evaluate: Inspected target navigated or closed` right after the block. Blocking sends the guest home (s139) and the trip is a page navigation; the scenario went straight to the next eval | s97 | fixed (scenario) | waits for the guest to be Joined in their own world with worldClosed clear before knocking again |
| 478 | s114 in #115 (green in #114 with the same code): `companion distance ... on A 0, on B 826 (started 2821 away)`, B's read identical for all 90 polls. NOT the follow chain: the peer had the recruit (`Follow on peer: indrele rathryon leader=Generated:0x10 at 0; pos=(-12288,-69632,87)`, the walk-off spot exactly, carried by global.lua carryFollowers on A's PlayerCellChange) and A's copy sat on those same integers -- a puppet.lua `distance` snap (256 u near tier -> mpSnapRequest -> actors.snapActor teleport), the path B's copy takes too (no tier is stamped on an ActorMoveBatch, no cooldown past 1 s, no detach: no ActorCellChange inside one cell). B's 826 is the spot the NPC settled at (832 u from the walk-off spot): B's ENGINE STOPPED six seconds before the walk -- its last input frame reached the server at 12:33:52 (`simpeer.avatar_stats_gated id 3 lastInputAgoMs 62431` at 12:34:54; player.lua sends one every frame) and A's dest snap landed at 12:33:58 -- so actorProbe was a frozen mirror and the assertion blamed the relay | s114 | fixed (scenario) | s114 reads B's actorBatchesIn across the walk (s42's liveness signal); stuck for the 90 s = `B's engine stopped before the walk` with B's jsErrors/luaErrors/tail, not the relay verdict. Product path unchanged: nothing in the trace needs a bake. Open question for the next red of this shape: WHY a harness client's main loop dies mid-run (a wasm trap or OOM would show in the jsErrors the scenario now prints) |
| 479 | s166 in #114/#115: the far forager CLOSES but at a crawl (422 -> 207 u in 15 s, 426 -> 234 u in 30 s; a kwama forager runs ~15+ u/s). Not the pathTo gate (285, fixed): the NAVMESH was still dummy-centric. Scene::changeCellGrid loads a 3x3 around every anchor and mechanics processes actors by nearestSimDistanceSqr, but `mNavigator.updateBounds` got only the PLAYER's CellGridBounds, NavMeshManager clipped its tile range to that rectangle, and every `shouldAddTile(tile, playerTile, maxTiles)` gate (NavMeshManager::update, SpatialJobQueue/JobQueue::update, AsyncNavMeshUpdater::processJob) measured from the dummy's tile alone -- so an anchored cell outside the dummy's 3x3 had no navmesh, buildLimitedPath fell back to a straight line and the AI walked into terrain | scene.cpp changeCellGrid; detournavigator navigator.hpp / navigatorimpl / navmeshmanager / asyncnavmeshupdater / makenavmesh.hpp; cellgridbounds.hpp | fixed (engine + tier2 bake needed) | Navigator::setSimAnchorGrids(vector<CellGridBounds>) (default no-op: stub and browser client untouched); changeCellGrid passes one grid per mSimAnchors before updateBounds (cleared on the interior path). NavMeshManager keeps the anchor grids, sets the recast range to the union of the player range and every anchor grid, walks only the player's part and each anchor grid's part of it (never the empty span between far anchors), and gates with shouldAddTile(tile, playerTile, maxTiles, simAnchorTiles) = near the player OR near any anchor; the async updater holds the same list (setSimAnchorTiles) so worker threads stop dropping far anchors' jobs as "too far from player". No anchors = the old single-centre gate, byte for byte. Tile budget: each anchor grid is ~36 tiles of the 1024 the 22-bit tile id allows (~28 anchors); the peer's dummy far from every anchor costs its own grid only. Proof: s166 logs the forager's u/s over the chase window; the next run shows the difference |
| 480 | s170 fresh5/fresh6 phase 6 (and any far teleport in play): the friend's PUPPET of the host stops following once the host snaps two cells away. fresh6 guest: `puppet snap #1.0 (attach)` then `(distance)` every ~3.5 s for 45 s while the `puppets` mirror held the host's body at the OLD spot (-12288,-69632, cell -2,-9) the whole time and the host stood at (-12500,-52970, cell -2,-7). Two client-side faults, both in the puppet spawn/despawn bookkeeping, neither in the engine teleport (objectbindings.cpp teleport + World::moveObject move an object between a loaded-inactive and an active cell correctly and set RefData position first): (a) `despawnPuppet` did `pcall(p.obj:remove())` and forgot the body. `remove()` throws `Can't remove 0 of 0 items` while a teleport is in flight on that body (teleport zeroes the count until its delayed action lands; the spawn's own placement and the PlayerCellChange follow-teleport both do this), and at a frame a second every event of the last 1-2 s lands in ONE frame, so a spawn/follow-teleport and a despawn (guest's own cell change -> refreshVisibility, or the server's PlayerLeaveView sweep from the window where guest=-2,-9 and host=-2,-7) share a frame routinely. The body survived, untracked, with puppet.lua attached and running at the NEW spot, its interp target still the old-spot pose; the next MoveBatch spawned a fresh tracked body; the orphan's `mpSnapRequest` (keyed by player id only) teleported the TRACKED body to the orphan's stale target -- attach once, then distance every 3 s (far-tier cooldown: the pose 16k units from the guest) -- so the tracked body could never leave the old spot. (b) `MP_MoveBatch` spawns with no visibility test and `spawnPuppet` places the body by the POSE's coordinates (destCellArg '' = default worldspace, cell from x,y), but the server relays by the mover's ANNOUNCED cellKey while the pose comes from the peer's avatar, which follows a far teleport late (fresh6: the peer's `avatar #3 follow-teleport` came 17 s after the server logged the cell change -- a cold load of -2,-7). A body placed by such a pose lands in an UNLOADED cell (radius-1 grid), where LuaManager never activates its script (addCustomLocalScript inserts into mActiveLocalScripts only for an active cell; only addObjectToScene does later) -- it receives MP_Pose (events are delivered regardless) but never runs onUpdate, so it never asks for a snap: a ghost until the owner's next border crossing teleports it. s114/#478 is NOT this: NPC puppets snap by refKey (actors.snapActor), no successor body, same cell | puppet.lua requestSnap; global.lua despawnPuppet / spawnPuppet / mpSnapRequest / tick | fixed (client Lua -> engine bake) | puppet.lua sends `obj = self.object` with every snap; `mpSnapRequest` ignores a request whose body is not the one it tracks; `despawnPuppet` keeps a refused remove() in `removeRetry` (30 s) and `removeRetryTick` lands it once the teleport has; `spawnPuppet` skips a pose outside the client's loaded neighbourhood (`poseInView`: floor(x/8192),floor(y/8192) vs ownCellKeyCache through visibleFrom; peer and interiors exempt) so the next in-range pose spawns the body in a cell whose scripts run. run.lua #480 (5 checks). Proof: s170 phase 6 `settled(guest, hostId)` after the snap, then the walk; s122/s154 unchanged (same-cell snaps never hit either fault) |
| 481 | s99 step 6 in #115/#116/#117, and the live "escape must be pressed twice to open the menu" (s64-real-input report 6): after T, Escape, O, Escape the engine sits in the Interface UI mode with no overlay up (`uiMode=Interface` for 5 s+; `lastKey=undefined` is by design -- index.html's window-capture handler owns T/O and the Escape that closes a panel, so no key reaches SDL there). CONFIRMED from code: index.html's `uiHold` is refcounted and pairs every open with a close, but player.lua answered `uimode:on` with `I.UI.setMode('Interface')`, which only QUEUES the engine push (uibindings.cpp `_setUiModeStack` is a delayed action) and leaves omw/ui.lua's own `modeStack` mirror to the `_onUiModeChanged` callback a frame later; `uimode:off` = `I.UI.removeMode('Interface')` walks that mirror, so an on and an off drained by the same `pollCommands` frame (Escape/O/Escape land at one timestamp at the harness frame rate; a quick T-Escape or a hidden-tab pump in play) found nothing to remove, queued nothing, and the engine kept the push. Stuck in Interface: no movement, no attack, and the next Escape pops the stale mode instead of opening the menu | player.lua `uimode:` handler; omw/ui.lua setMode vs addMode | fixed (client Lua -> engine bake) | `uimode:on` uses `I.UI.addMode('Interface', { windows = {} })`, which records the mode in the mirror at once, so the off always finds it (and a window the engine had open underneath survives the hold). run.lua #481 (6 checks: the real omw/ui.lua against a model of the engine's deferral, incl. the setMode regression). s99 unchanged: its step-6 expectation was right |
| 482 | s170 fresh14/fresh15 phase 6: after `snapto:-12500,-53100,512` (SPOT, cell -2,-7) the host sits at z=-121/-135 for the rest of the run and the friend's puppet of the host follows it down (482 -> -127) while the peer's creatures in the same cell stand at z 960-1370; a scripted climb to z=265 falls straight back. Read as "the destination terrain never streamed in". It is not a streaming or timing fault and native OpenMW does the same: the LAND record for -2,-7 in Morrowind.esm puts the surface at that vertex at z=872 (944 at the guest's -12300,-53100), so z=512 is 360 u UNDER the heightfield. Scene::changeCellGrid loads the destination grid synchronously on every build (scene.cpp loadCell -> LandManager::getLand -> Land::loadData, a spin-waited streamfs read on the web, then PhysicsSystem::addHeightField before the teleport's moveObject), so the heightfield IS there when physics steps; a capsule that starts below a btHeightfieldTerrainShape never meets it from underneath, falls to the water plane and swims there (the MovementSolver stops gravity under swimlevel), and every later leg that keeps a z under 872 falls again. The puppet fell for no reason of its own: it is placed by the owner's pose. Earlier fresh12/13 ("no mark within 300/600 u of the host") were the same fall. The sibling scenarios carry the same 512 and only got away with it because their next move was a hop to a creature's real z | player.lua `snapto:`; global.lua mpSelfSnap; objectbindings.cpp teleportPlayer -> World::adjustPosition(force=true) | fixed (client Lua -> engine bake) | The engine already has the guard: the Lua teleport's `onGround` option runs World::adjustPosition(force=true), which raises the position to `getTerrainHeightAt` (ESMTerrain::Storage::getHeightAt reads the LAND data itself, not the render chunk, so it holds on the streaming build the moment the cell is active) and traces it down onto whatever stands there. `snapto:` now sends `onGround = true` and mpSelfSnap forwards it; the reconciliation snap and `tpz:` send nothing and keep a physics-ruled (possibly levitating) pose or a deliberate fall. Puppets need no guard of their own: with the owner on the ground its pose is. No C++ change: a heightfield you are under is not a heightfield you are waiting for. s170: SPOT z is the LAND height (880) so hopTo's 3D last leg closes, the climb-back block is gone. run.lua #482 (4 checks: the real snapto handler run, the forward, the two exemptions). Proof: fresh16 host z ~872 after the snap, no climb |
| 483 | s166 in #119 (479 baked from #116): the scrib covered 9 u in 30 s = 0.3 u/s, gap 451 -> 444, the same 450 u east retreat over flat LAND (z 968-976 along y=-52744, no water, no pathgrid in -2,-7); #116 scrib 8 u/s, #117 rat PASS. Not a 479 regression of the gate logic, and not fleeing (Scrib AIDT flee 0) or a mirror stall (hp 7 -> 6 arrived): 479 CANNOT work as sized. `max tiles number` (1024) is both the shouldAddTile gate and the dtNavMesh tile POOL (initEmptyNavMesh params.maxTiles; 22 id bits minus 4096 polys = 1024), and the vanilla gate fills the pool from ONE centre: a tile is 435 u (64 * 0.2 / 0.0294), a cell 18.8 tiles, pi * 18.05^2 admits 1033 tiles, all non-empty outdoors (terrain + water), so the dummy alone overfills it by 9 (479 wrote "~36 tiles per grid": a 3x3 grid is 56 tiles ACROSS, 3136 tiles). Gating each anchor with the full 1024 too asked 2066 of a 1024 pool; SpatialJobQueue pops nearest-the-player first, so the dummy's circle takes every slot and the anchor's adds fail DT_OUT_OF_MEMORY (navmeshcacheitem updateTile -> failed -> JobStatus::Fail, re-posted and re-failed on every update). Which anchor tiles win is a race between the peer's boot (1033 generations on one idle-priority thread) and the anchor's arrival: #116/#117 got some, #119 none. And a navmesh with no tile under the actor is WORSE than none: PathFinder::buildPath walks a straight line only on NavMeshNotFound; StartPolygonNotFound + no pathgrid = empty path, and AiCombat::attack answers !isPathConstructed with ActionFlee -> FleeState_Idle until the target is inside its trigger distance -- the creature stands. That is also #114/#115 pre-479 (the forager sat at -12610/-12625,-52858 in both runs; the "closing" was B drifting) | makenavmesh.hpp shouldAddTile (4-arg); scene.cpp changeCellGrid; navmeshmanager.cpp (comment) | fixed (engine + tier2 bake needed) | The pool is split evenly between the centres: shouldAddTile gates each centre with maxTiles / (1 + anchors) -- two centres 512 each (509 tiles, radius 12.8 tiles = 5556 u from the anchored cell's centre: the whole cell plus ~1400 u into the neighbours), three 341, four 256 (3929 u, a cell minus its corners); the dummy's circle shrinks by removes (mRemoving runs first) and the freed slots take the anchor's tiles. No anchors = 1024 = vanilla. changeCellGrid skips an anchor on the player's own cell (it would draw the dummy's circle twice and halve every share). Peer follow-up when four clusters are the norm: `max polygons per tile = 2048` + `max tiles number = 2048` in buildPeerSettings doubles the pool (not done: a 2048-poly tile would corrupt poly refs and Detour does not check). Proof: s166's u/s line reads a scrib at ~40 u/s and the gap closes >= 150 u on the next bake; the 30 s window fits the slowest mark |
| 484 | s170 fresh21 phase 7 (the peer-side twin of 480): the guest snapped to the -3,-9 seabed never drowned -- no `player.death` for #3 in five minutes, the host's puppet of the guest never read dead -- while the suite's s149 drowns the same spot in ~60 s. Peer log: `avatar spawned for #3 at (-19264,-72128,-1135) cell=-3,-9` (07:37:17.977), `authority GRANTED for -3,-9`, then `puppet despawned for pal6n5d1u (#3)` ONE ms later (07:37:17.982) -- and no `avatar #3 follow-teleport`, because there was no avatar to move: the same thing had already taken both avatars at 07:35:32/33 (`follow-teleport ... ok=true` then `puppet despawned` for #1 and #3, respawned only on the next PlayerCellChange: #1 at 07:36:09, #3 at 07:37:17). The despawns are the server's interest SWEEP (movement.ts sweep -> `PlayerLeaveView`): the sim peer is a RECIPIENT whose candidates come from `index.candidates(recipient.cellKey)`, i.e. the peer's OWN parked cell, and the peer parks in one anchored cell (`sim peer moved to -2,-7 to simulate it`, the host's). Host and guest are cell-visible to the peer only while within Chebyshev 1 of that spot; at 07:35:31 the peer's own cell_change to -2,-7 reached the server 1.9 s AFTER the host's (both players out of view of -2,-9 for that window), and at 07:37:17 the guest went to -3,-9, two cells from -2,-7, for good. `MP_PlayerLeaveView` on the peer then removed the AVATAR (removeRetry lands it once the spawn teleport has) and cleared `remoteCell`, and `spawnPuppet` on the peer refuses a MoveBatch spawn without remoteCell, so nothing could respawn it until the next cell change. The avatar registry `+ (now 4)` is stale entries, not four live bodies: `mp.setAvatar(false)` runs only from mpAvatarDetach, never from despawnPuppet's remove(). s149 never hit it because both clients start in -2,-9, where the peer parks, and -3,-9 is adjacent. 453/478 are follow-claim/NPC-snap rows, not this | global.lua MP_PlayerLeaveView; server/src/core/movement.ts sweep (root, untouched) | fixed -- peer Lua only, no bake for the fresh-install run (peer Lua is bind-mounted); an engine bake only carries it to clients, which never take this branch | `MP_PlayerLeaveView` returns at once on the sim peer (`visibleFrom` already says every player is visible there; the sweep has to agree). run.lua #484 (2 checks: the cut handler run as the peer keeps the body, remoteCell and lastPose; as a client it still despawns and forgets). Proof: s170 fresh-install phase 7 `player.death` for the guest within the drown budget, and one `avatar #3 follow-teleport` per far cell change with no `puppet despawned` behind it |
| 485 | fresh25 (s170 phase 6): with 484 in, a body held under water drowns, respawns IN PLACE with health restored (stock rules, no respawn point), is still under water, and drowns again -- six player.death in three minutes. A real player who dies swimming would loop the same way until they swim out on the few seconds of breath a respawn grants | rules (respawn), player.lua | open (design) | a respawn where you fell should land on the nearest shore when the fall was under water (or the respawn rule should default to the last dry position); the scenario now stops holding under after the first death |

## Open (found, not fixed)

| # | Item | Notes |
|---|---|---|
| 28 | Magnitude re-roll on the avatar (spell with a range is rolled again on the peer) | no engine API to inject a magnitude; would need a binding or a per-cast fixed record |
| 29 | "Your guests were sent home while you were away" narration for a returning host | fixed: server.ts guestsSentHome -> WorldMode.sentHome once to the returning owner; global.lua narrates it; ownerleft.test.ts |
| 30 | Gateway restart with a party: revived as private, guests get 502 then not_open | fixed: worlds.ts `.mode` written on every flip/observed mode, read at start -> OMW_WORLD_LAST_MODE resumes party (boot mode stays private, #9); worlds.test.ts |
| 31 | Half-open socket: client has no pong watchdog | net.lua | done (unit) — bake needed | net.lua lastRecvAt stamped in onOpen/onJson; tick hangs up after 75 s of silence while Joined (onClose redials); lua-tests source check |
| 32 | Social OK notices ("Invitation sent.", "Sent home.") never mirrored to the feed | fixed: MP_SocialResult notices every non-silent result; lua-tests |
| 33 | Dying inside a dialogue: lock release unverified | quests.lua | done (unit) — bake needed | server.ts onPlayerDeath → quests.releaseDialogueLocks (quests.test.ts "the holder dying releases the lock"); identity.lua death edge removeMode('Dialogue') (lua-tests source check) |
| 34 | No harness hook opens Training/Travel/SpellCreation/Enchanting (`svc:open:<Mode>`) | player.lua | done (unit) — bake needed | player.lua svc:open:<Mode>[:<recordId>] / svc:close:<Mode> (Barter, Training, Travel, SpellCreation, Enchanting); barter:open/close kept as aliases; lua-tests source check |
| 35 | PvP kill: no attribution; no scenario kills a player with PvP on | combat.ts | done (unit) | resolveOwner stamps victim.lastHitBy on a relayed harmful hit/spell; server.ts onPlayerDeath logs `killedBy` when it is < 10 s old. combat.test.ts. No harness scenario |
| 44 | Resist arrest: MP_OpenDialogue bypasses the dialogue lock; guard puppets never report "fights me" | global.lua, puppet.lua, quests.lua. SERVER HALF DONE (unit): connection.ts PlayerArrest relay calls quests.noteForcedDialogue(guardRefKey, targetId) (recentlyHeld with a 120 s ttl), so the arrested player's ActorAI combat claim for THAT guard passes worldstate's dialogueHolder gate (another guard is still refused) -- redteam-client.test.ts #44. Client half (the puppet reporting the fight after the forced dialogue) still open |
| 45 | Jail: time skip is a local advanceTime, not through the server clock; skill loss/confiscation unverified | jailscreen path, world.lua |
| 46 | Pickpocket caught: victim's startCombat happens on an AI-off puppet, never reported | puppet.lua | dup | dup of #146 (moot) |
| 47 | Guest rest under timeSkip=owner: the clock is refused but the local heal is claimed (free full heal) | superseded by #69 / #256 |
| 48 | Guest's local advanceTime changes weather; holder re-sends only on change | world.lua tickWeather (clear lastWeatherSent every ~60 s) | fixed | 895b0d9b (WEATHER_RESAY) |
| 49 | dayspassed never written by writeLocalTime: TimeStamp goes backwards (powers/day, corpse timers, disease) | superseded by #150 |
| 50 | Map exploration/markers not persisted (blank map every login, solo included); custom markers not shared | superseded by #260 (exploration); custom markers still not shared |
| 51 | Dialogue topics relayed, never persisted | quests.ts | done (unit) | quests.ts topics(): doc.topics on the journal's doc (cap 4096), sent in JournalSync; quests.lua MP_JournalSync -> quests.applyTopics (baseline first, so never re-announced). lua-tests source check |
| 52 | Cell reset: vanilla loot never reappears on a running engine (only future joiners) | objects.lua |
| 53 | Weather region freezes when the holder goes indoors (no handoff) | weather.ts | done (unit) | a holder silent for 3x the 60 s re-say (HOLDER_SILENCE_MS) with another occupant present is leave-and-re-entered on m7's 1 s tick: the longest-present other occupant inherits (authority.onLeave); a lone holder keeps the seat. weather.test.ts |
| 54 | Item condition: peer wholesale report can undo a fresh repair in the merge window | superseded by #163 |
| 55 | Enchant charge oscillates (both engines run passive recharge) | cosmetic |
| 56 | Mixed stacks (one damaged of five) collapse into one stack on relog | superseded by #234 |
| 57 | Container put/take carries no item state | objects.lua snapshotContainer, worldstate.ts |
| 58 | Bound items recorded in the inventory doc; a relog inside the window grants a permanent one | superseded by #70 |
| 59 | Summon: holder loss mid-effect leaves no local creature until recast | global.lua |
| 60 | Alchemy: identical potions brewed by two players are two records (doc bloat) | m7.ts (dedupe by content) | done (unit) | recordCreate acks an existing record of the same kind + identical data (JSON) with its id, mints nothing, syncs nobody; O(n) scan inside the create queue. worldtime.test.ts |
| 73 | Knockdown/hit-recoil on the avatar not relayed: owner rubber-bands instead of "cannot move" | superseded by #116 / #309 |
| 74 | Long fall: self-snap → PlayerCellChange → avatar teleported mid-air → fall height reset → free fall | player.lua jump detector vs falling | dup | dup of #231 |
| 75 | Peer restart mid-swim spawns the avatar at the last cell-change point; owner snapped there | connection.ts teleportPose gate |
| 76 | threat.lua is dead code (relayed CombatHit shape never matches); NPCs strobe between two attackers | combat.lua, threat.lua | done (unit) — bake needed | threat.lua deleted with its call sites (actors.lua decay/targetOf/import, combat.lua creditThreat) and its CMakeLists entry; lua-tests source check |
| 77 | Dialogue holder's NPC keeps wandering on the peer while talking; the other player watches it walk off | actors.lua (Wander distance 0 on lock) |
| 78 | Greeting/idle on the peer targets the parked dummy: NPCs near the park spot face nothing | actors.cpp (skip when peerRulesBody) |
| 79 | Escort claim needs no dialogue lock: any client can send any NPC walking | superseded by #363 (an escort is a follow claim with a destination) |
| 80 | "npc"->AddItem in a dialogue result lands on the local puppet only | no relay for non-holder NPC inventory |
| 81 | Level +1 per message at 60/s reaches 255 in 4 s; attributes/skills unbounded | superseded by #99 / #369 |
| 82 | Gold placement with fromInventory=false is counted, never refused (party worlds) | fixed (unit): worldstate.ts spawn: a human's `gold_001` is always fromInventory (no script or tool of a human's places a purse), so it is refused under refuseUnownedDrops flag or no flag; the default stays off. worldstate.test.ts "a gold drop without fromInventory is still a drop" |
| 83 | Fabricated first ContainerOpen becomes canonical (poison every container in a town) | superseded by #99 / #365 |
| 84 | ActorAI travel destination bounded only by MAX_ABS_COORD | fixed (unit): worldstate.ts travel claim: floor(x/8192), floor(y/8192) must be within ±2 of the actor's exterior cell or the claim is dropped (`travel destination beyond reach`); an interior's coordinates are local, so its cell is the origin and the same ±2-cell box applies. worldstate.test.ts "ActorAI travel is bounded to two cells of the actor" |
| 86 | Memory governor prices a world at 640 MB regardless of anchors/party size | worlds.ts /status anchors |
| 87 | No harness run beyond 4 browsers; soak bots never touch containers or a peer | bots/soak.ts — PARTIAL: bots now open the shared chest (ContainerOpen, first opener rolls it), take/put one item (ContainerOpRequest) every 8 s and drop-then-pick a loose item (ObjectSpawnRequest → ack → ObjectTakeRequest by net id) every 7 s; `--server-password <pw>` stands up a fake `system` peer holding the cell (answers the wire, simulates nothing), only when a password is given. Messages are built in bots/soak-ops.ts and soak-ops.test.ts proves each is accepted by a real server. Still a smoke tool; >4 browsers stays open |
| 88 | BAD_CONTENT terminal: nothing tells the player that a reload after the mod mounts is the remedy | fixed: index.html one automatic reload (sessionStorage guard, cleared on Joined), then the remedy sentence + Reload button; mp-ux-pages.test.ts |
| 89 | Launcher "friends playing now" lists party+occupied only; an online-but-solo friend is invisible | fixed: /auth/friends-playing lists an occupied solo world with `mode`; launcher rows "is playing solo", no join; friendsplaying.test.ts |
| 100 | s147 on the managed peer: avatar follow-teleported to z=1453 on release and no fall damage reported; server logged avatar_stats_gated (client sent no input for 6 s mid-ritual) -- why does a standing client stop driving? | player.lua inputTick / harness; defensive: mp-harness.mjs launches Chrome with --disable-renderer-backgrounding --disable-background-timer-throttling so a hidden tab cannot stop driving the input tier |
| 101 | Nametags: none rendered over puppets (only the crosshair tooltip); stale name after a rename | needs an OSG text node |
| 106 | Companions not persisted across a world restart (follow claims are memory only) | superseded by #117 |
| 107 | memberVars (per-object MWScript locals) persisted but never replayed to joiners/peer | superseded by #117 |
| 108 | Player and cell docs sweep on independent 45 s timers: a crash can dupe or lose a container take | playerstore/cellstore |
| 109 | Arrows land twice (owner's local miss + the peer's); missed arrows stored in the puppet copy | superseded by #116 |
| 110 | Essential-NPC message shown on the peer, never the owner | done — bake needed: global.lua actorDeathFn notices sKilledEssential for an essential NPC record on every MP_ActorDeath (pcall-guarded); lua-tests source check |
| 111 | hitn: bypasses the real swing path (combat.lua ignores non-test Hits); most kill scenarios prove the relay, not the swing | superseded by #238 |
| 112 | sethp:/rest: hooks are direct writes; no scenario exercises death-from-damage, the wait dialog, training, level-up dialog | superseded by #241 (death from damage), #242 (real casts), #256 (level-up hook); the wait dialog and training still unexercised |
| 113 | Mirrors that are never cleared (hitFwd, spellFwd, castAt, doorEnter, takeOwned, chestOp) let a second wait pass instantly | scenarios: clear before waiting |
| 114 | SKIP detection by log text; a sweep of skips exits 0 | superseded by #244 |
| 115 | Peer clock is load-dependent (fixed dt 1/20 per tick): duration-based asserts compare two clocks | engine.cpp headless — fixed (unbaked, uncompiled here): the headless loop's dt is now max(real elapsed, 1/limit) capped at 250 ms (headlessMaxDt), so an overrunning tick catches game time up to wall-clock instead of falling behind; a tick within budget keeps the fixed step. Needs a tier2 rebuild before a harness verdict counts |
| 125 | /auth/link puts the session token in a query the edge logs; dead in gateway mode | routes.ts |
| 126 | ActorBatch is one uncapped message per cell per frame: a TR metropolis exterior is ~100 KB/s per client | actors.lua |
| 127 | No TR scenario in the harness; builder gamedata has no mods | harness — PARTIAL: TR itself cannot run on the builder; s167-mod-content-gate (new, unrun) exercises what the gate decides instead: two ?nomw browsers with the same list both join, then a protocol client reporting a hand-written one-record TES3 plugin (written into the server's gamedata as a dashboard upload would land) is refused BAD_CONTENT naming that plugin, and the roster is untouched. A real serve-mode boot of an extra plugin (browser mounts it via mwdata-mods.json) stays open |
| 137 | Puppet bow: `shoot attach` runs, `shoot release` gated on mReadyToHit: the arrow may stay glued to the hand after a shot | done — bake needed: character.cpp `shoot release` on a puppet with mReadyToHit cleared calls detachArrow() instead of leaving it glued |
| 138 | Revive is despawn+spawn: no get-up animation on the puppet | global.lua:2383 | done (unit) — bake needed | global.lua revivePuppet: mp.resurrect(obj) (#293) + tryTeleport to the pose + MP_Revive + pushStatsToPuppet, from MP_AvatarResurrect (peer) and the MP_PlayerStatsDynamic revived branch (clients); lua-tests source check |
| 139 | Feature gaps: no chat bubbles, emotes, party marker on map/compass, friend highlight beyond the crosshair name | — |
| 145 | Peer NPC aggression toward an avatar is computed against the parked dummy (race/Personality/faction of the dummy, not the player) | mechanicsmanagerimp.cpp:493 getDerivedDisposition per target; apply doc.factions to the avatar | fixed | getDerivedDisposition(ptr, toward) overload: race/Personality/factions/disease/weapon read off `toward`, bounty from the avatar registry; combat.cpp getFightTerm passes the target when it is an avatar. mp.setAvatarFactions (NpcStats join/rank/reputation/expel) applied from doc.factions in applyAvatarDoc. Needs a tier2 rebuild |
| 146 | Theft/trespass victim reactions (startCombat, disposition drop) land on the thief's AI-off puppet copy; a Fight-70 NPC that would attack a thief just barks | generalises #46 | fixed | commitCrime records a 'crimecombat' ScriptNote on the client for each witness that reached startCombat; actors.noteCrimeCombat claims ActorAI {combat=self, crime=true}; worldstate.ts admits a self-targeted crime claim without the dialogue lock; the holder starts the real fight. Disposition drop still local; needs a bake |
| 148 | mStolenItems not persisted: after a relog stolen goods sell back to the victim and are not confiscated on arrest | needs a binding |
| 157 | Corprus worsening resets on relog (worsen count not on the wire) | activespells.cpp:188 |

## Wontfix / by design

| Item | Reason |
|---|---|
| Guests keep loot, not quests | product decision (mp-overhaul) |
| MP tile hidden on prod (`?experimental=1`) | until the human playtest |
| 85 · Guest can set the host's quest globals | by design of the shared journal: one campaign per instance, the owner's doc is the record (guests keep loot, not quests) |
| 55 · Enchant charge oscillates (both engines run passive recharge) | cosmetic; item STATE follows the peer under the 4D one-writer rule and settles on the next report |
