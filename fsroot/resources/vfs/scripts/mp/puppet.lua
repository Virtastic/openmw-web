-- Multiplayer remote-player puppet (CUSTOM script). M1: attached to spawned NPC records to
-- mirror remote players' movement (PlayerMoveBatch). Stub for M0 so the content file layout
-- (mp.omwscripts) is stable across milestones.
return {}
